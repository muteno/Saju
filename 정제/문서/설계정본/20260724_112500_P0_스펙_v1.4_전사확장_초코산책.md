# P0 스펙 v1.4 — 전사 확장: 초코서당·산책처럼 (2026-07-24)

> **v1(기본 스키마)·v1.3(전사 규칙)은 전부 유효.** 이 문서는 신규 2채널에 대한 **차이분만** 기재한다.
> 근거 결정: 운영자 260724 "원본 라이브러리를 기반으로 로직 만들자"(정제 입력 편입) + "밤새, 유의미하게(뉴런)".

## 1. 입력 스냅샷 (불변 동결 260724 11:22)

| 채널 | 스냅샷 | 편수 | post_id | 정렬 |
|---|---|---|---|---|
| 초코서당(@choco85) | `P0_인벤토리\transcript_choco_v1\` | 249 | **CT-P###** (3자리) | 파일명 오름차순 1~249 |
| 산책처럼, 사주(@stan4pillars) | `P0_인벤토리\transcript_sanchaek_v1\` | 1,231 | **ST-P####** (4자리) | 파일명 오름차순 1~1231 |

- 헤더 형식은 도화도레 transcript_v1과 **완전 동일 실측**(제목 / 메타 6행 / 경고 블록 / `---` / 캡션 줄) → v1.3의 캡션 재조립·타일링 규칙 그대로 적용.
- 검수기 `p0_verify.py`의 SNAPS에 두 폴더 등재 완료(260724).

## 2. 저자·클러스터

- **초코서당 = 기존 클러스터 '초코서당(초명)'.** ⚠슬롯2 웹 스크랩(초코서당-* 13파일)과 **동일 저자** — 유튜브에서 웹 글과 같은 내용을 말하면 report에 기록(P1 반복 접기·계보 보정 재료).
- **산책처럼 = 신규 5번째 저자 클러스터 `산책처럼`** (기존: 도화도레·초코서당·현묘·사공 + 기타). 관법 미상 — **저자 자백 문장(관법 선언·스승/계보 언급) 수집이 1순위 과제.**

## 3. 멤버십·라이선스

- 초코 수록 249편 = 수집 시점에 접근 가능했던 것(공개+반가워요+넘멋져요 티어). 전문가 티어 30편은 미수록(승급 대기). 멤버십 티어 언급이 본문에 있으면 flags에 기록.
- 산책처럼 1,231편 = **전량 공개**(멤버십 차단 0 실측).

## 4. 감시 항목 (v1.3 §5 갱신 — 발견 시 report 최상단 ★)

1. **환절기 토 정량 규칙**(조토/습토 점수화) — 질적 규칙은 도레에서 확보, 정량은 미확보. 초명은 수치표의 저자(지장간 비율)라 최유력.
2. **신강약 점수제** — 도레 실측(총 110점: 천간 각10·시지15·일지15·월지30·연지10, 50↑=과다)과 **대조**. 초코 웹의 점수 언급과 유튜브 강의 버전이 갈리는지.
3. **용신 추출·대세운 해석 절차**(초코 중급 심화 강의 계열 — 웹에 없던 강의 본체가 유튜브에 있을 가능성. 증류공백진단의 '판단 과정' 최우선 표적).
4. 야자시/자시(택일 예외 포함) · 하건충 언급 · 십이운성 stance.
5. 역추론·콜드리딩 실전 장면(게스트 상담 실황이 있으면 화자 분리 명확히 — P4 시험 문제 재료).
6. **산책처럼 전용: 저자 정체·관법 프로파일**(자백 문장·인용 계보·기존 4저자와의 stance 대립축).

## 5. 뉴런 요구 (운영자 260724, 보드 1-18)

- gist에 **개념 키가 명시적으로 드러나게** 쓸 것(예: "갑목 발달=추진력, 과다=고집" — 개념지도 TAXONOMY 용어 사용). 문단 하나가 여러 개념을 잇는 경우(예: 갑목×비견×신강) **모두 드러나게** — 이 gist가 P1 링크망(키워드에 다리 100개)의 발판이다.
- flags `결정론`(조건→결과 규칙)·`프로브`(확인 질문)·`게이트`(단정 금지 문장)는 링크망의 우선 간선이므로 적극 부착.

## 6. 배치 편성

- **초코 = CH-A~J 10배치**(크기 균형 ~600KB, 분담표 = `scratchpad\choco_batches.txt`). 260724 발사.
- **산책처럼 = SN 배치 약 23~25개, 웨이브 분할**(웨이브당 8~10배치) — 초코 라운드의 검수 결과로 스펙 검증 후 발사.
- 절차 동일: 산출 `p0_out\{배치}_{posts,paras}.jsonl+_report.md` → 메인 6게이트 검수 → 합격분만 반입.
