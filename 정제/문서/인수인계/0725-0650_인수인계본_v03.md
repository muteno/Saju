# 인수인계본 v03 — 2026-07-25 06:50 KST (맥 세션)

> **윈도우 세션이 이걸 읽고 이어받는다.** 지금 운영자가 윈도우로 넘어가서 "7/24에 날아간 산출물이 진짜 있는지" 확인 중.

---

## 1. 지금 상황 한 줄

**산책처럼 사주 1,231편 문단화가 582편에서 멈춰 있다.** 7/24에 쏜 웨이브2(583~1081)는 **산출물이 하나도 남지 않았다** — 윈도우 임시폴더(`AppData\Local\Temp`)에 있었고 그건 OneDrive 동기화 대상이 아니라서.

## 2. 실측 상태 (2026-07-25 06:48)

| 항목 | 수치 |
|---|---|
| 산책처럼 P0 | **582 / 1,231편** (SN-A~J, 인덱스 1~582 **빈틈 0**) |
| 잔여 | **583~1231 = 649편 / 9.45MB** |
| 도화도레 | 246/246 완결 |
| 초코서당 | 249/249 완결 |
| 웹 | 57/76 (보류 19 제외 완료) |

## 3. 🔴 윈도우에서 확인할 것 (운영자 진행 중)

```powershell
Get-ChildItem -Path "$env:LOCALAPPDATA\Temp\claude" -Recurse -Filter "SN-*.jsonl" -ErrorAction SilentlyContinue | Select-Object FullName, Length, LastWriteTime
```

- **나오면**: 그 파일들을 `2. 정제작업\P0_인벤토리\_미완_부분산출_백업\260724_윈도우복구분\`에 복사 → 6게이트 검수 → 합격분 반입. **절대 임시폴더에 둔 채로 작업하지 말 것**(그게 이번 유실의 원인).
- **안 나오면**: 청소된 것. 아래 4번대로 재발사.

## 4. 재발사 설계 (이미 준비 완료)

- **재편성 = SN-K~Z 16배치**(연속·용량균형 ~600KB). 분담표 실물 = `P0_인벤토리\_미완_부분산출_백업\260725_SN웨이브2_중단분\분담표_SN-K~Z.json`
  | 배치 | 인덱스 | 편수 | | 배치 | 인덱스 | 편수 |
  |---|---|---:|---|---|---|---:|
  | SN-K | 583-639 | 57 | | SN-S | 946-985 | 40 |
  | SN-L | 640-695 | 56 | | SN-T | 986-1028 | 43 |
  | SN-M | 696-736 | 41 | | SN-U | 1029-1069 | 41 |
  | SN-N | 737-770 | 34 | | SN-V | 1070-1110 | 41 |
  | SN-O | 771-813 | 43 | | SN-W | 1111-1152 | 42 |
  | SN-P | 814-860 | 47 | | SN-X | 1153-1193 | 41 |
  | SN-Q | 861-901 | 41 | | SN-Y | 1194-1217 | 24 |
  | SN-R | 902-945 | 44 | | SN-Z | 1218-1231 | 14 |
- post_id = `ST-P####`(4자리, 번호 = `sorted(glob)` 1-based 인덱스). **전역 고정이라 배치 재편해도 충돌 없음.**
- 스펙 = v1(기본) + v1.3(전사) + v1.4(초코·산책). **에이전트에겐 스펙 파일을 읽히지 말고 프롬프트에 인라인**(운영자 260725 토큰 절약 지시 — 16명 × 19KB 읽기 제거).

## 5. 오늘(7/25) 맥 세션이 한 것

1. **검수기 맥 경로 수리** — `p0_verify.py`가 윈도우 경로만 알아서 맥에서는 전 배치 "스냅샷에 없음" 오탐이 나던 것. `Path.home()/Library/CloudStorage/...` 후보를 최상단에 추가. **정본(`_tools\p0_verify.py`)·MAIN 사본 둘 다 수리 완료.** 회귀시험 = SN-J로 돌려 **인용 323/323 통과·미커버 0** 확인.
2. **웨이브A 8배치 발사 → 운영자 지시로 전량 중지**(7/24 산출물을 먼저 찾아보기로).
3. **중단분 19편·167문단을 OneDrive로 백업** (`_미완_부분산출_백업\260725_SN웨이브2_중단분\`): SN-K 583~588(6편·57문단) · SN-M 696~701(6편·51문단) · SN-O 771~774(4편·34문단) · SN-P 814~816(3편·25문단). **재개 시 그 다음 인덱스부터 이어가면 된다.**

## 6. ★다음 세션이 반드시 지킬 것 (오늘의 교훈)

1. **산출물을 임시폴더에만 두지 마라.** 배치 하나 끝날 때마다(또는 최소 30분마다) OneDrive로 복사해라. 7/24 웨이브2가 통째로 증발한 유일한 이유가 이것이다. 재작업 = 같은 9.45MB를 두 번 읽는 것 = 토큰 최대 낭비.
2. **인용은 스크립트로 추출시켜라.** 오늘 배치 하나가 스스로 채택한 방식(`quotes are EXTRACTED from source, never retyped`, 실물 = `인용추출기_gen.py`)을 **전 배치 규격으로 박아라.** 손으로 다시 치면 CJK 호환한자·오전사가 조용히 깨진다. 이건 예전에 이미 "권장 기법"으로 승격된 방법이다.
3. **싼 모델로 내리지 마라 — 운영자 직접 지시(260725):** "싼모델로 돌려서 품질이 떨어지는거는 안됌." P0의 문단 경계·gist·태그가 P1 색인과 P2 증류 품질의 **천장**을 정한다.
4. **토큰 절약은 '낭비 제거'로 하고 '품질 절감'으로 하지 마라(운영자 260725).** 실효 수단 = ①재작업 0(=중간 저장) ②스펙 인라인 ③파일 목록은 스크립트로 도출 ④"각 파일 한 번만 읽어라" 명시. 이 코퍼스는 9.45MB라 순수 읽기만 400~500만 토큰 급이며 **그건 낭비가 아니라 바닥값**이다.
5. **도구는 계정·OS를 탄다.** 맥/윈도우를 오갈 때 검수기·갱신 스크립트 경로부터 확인하고 시작해라(오늘도 그것부터 걸렸다).

## 7. 절차 (불변)

에이전트 산출 → **메인이 `p0_verify` 6게이트 독립 검수**(미커버 0·quote 불일치 0·범위이탈 0·타일링 불량 0·ID중복 0) → 합격분만 `P0_인벤토리\` 반입 → 전 배치 후 `_tools\p0_global.py` + 현황판·개념지도 재생성.
