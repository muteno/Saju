# 인수인계본 v02 — 260722 22:30 (KST)

> 앞 인수인계본 = `0722-0435_인수인계본_v01.md`(P0 웹 코퍼스 종결 시점). **이 문서는 그 이후 = "전사 대확장" 분기만 다룬다.**
> 새 세션은 ①★직관보드 원문 → ②CLAUDE.md → ③v01 인수인계본 → ④이 문서 순으로 읽으면 현재 상태가 복원된다.

## 1. 한 줄 요약

운영자가 유튜브 채널 5개(산책처럼사주 + 신규 4개)를 전사 지시 → **총 6,279편·2,599시간 코퍼스**를 자동 큐로 걸어둠. 기존 전사 자산(도화도레 246 + 초코 249 = 495편)의 **13배**가 들어온다.

## 2. 지금 이 순간의 기계 상태 (22:30 기준)

| 항목 | 상태 |
|---|---|
| **가동 중** | `산책처럼, 사주`(@stan4pillars) 1,231편 2스트림 전사 — 22:19 시작, **완료 예정 7/23 밤(~25.5h)** |
| **대기 큐** | `queue_saju_260722.sh`가 22:27 기동해 `wait_idle` 대기 중 — 앞 작업 끝나면 자동 승계 |
| 큐 순서 | **석우당(132h) → 사람공부(3h) → 효명작명(19h) → 남석(57h)** ※운영자 지시 "석우당부터, 가치 우선" |
| 진행보드 | http://localhost:8765 (채널 전환 시 board_config 자동 교체) |
| 큐 로그 | `0. 전사프로그램\전사 내용\_전사큐_실행로그.log` |
| 전체 완료 예상 | 지금부터 **약 9.8일** (맥 연속 가동) |
| 초코서당 | 249/279에서 동결 — 잔여 30편은 전문가 티어 승급 대기(변동 없음) |

## 3. 새로 확립된 규칙 3가지 (이번 분기의 교훈)

1. **채널 전사는 하위 탭을 따로 열거해야 한다.** 엔진은 `/videos`만 본다 → 쇼츠·실시간 누락. 이번에 쇼츠 30·실시간 2편을 추가 발굴했다. 큐 스크립트는 `/shorts`·`/streams` URL을 별도 패스로 돌려 같은 폴더에 담는다.
2. **ETA 계산은 2스트림 합산 11배속.** 런처가 쓰는 `HOURS/5.0`은 1파이프라인 가정이라 2배 과대추정한다(56h로 나왔지만 실제 25.5h). 실측 = 스트림당 5.5배속 × 2 병렬.
3. **채널명으로 내용을 판단하지 말 것.** `효명작명`은 이름만 작명이고 실제론 `[하원기초명리] N강` 커리큘럼 채널이었다. 제목 샘플을 반드시 까볼 것.

## 4. 신규 코퍼스의 값어치 (P2 증류 관점 — 미리 봐둘 것)

- **석우당 = 이번 최대어.** `신육십갑자일주론 79강 월지별 戊寅일주4` — **일주 × 월지 격자**를 79강 이상 전개. P0에서 확보한 현묘 일주론 61편(2019, 십이운성 사용기)과 **같은 주제·다른 저자·다른 축**이라 교차 확증과 stance 대조가 동시에 된다. 편당 45분 장편이라 밀도도 높다.
- **효명작명 = 두 번째 커리큘럼 스파인 후보.** `[하원기초명리] 10강 갑목 → 11강 을목` 순서가 명확 → 설계 v2.1 "저자 집필 순서 = 교육 설계도" 규칙을 적용할 실물이 하나 더 생긴다.
- **사람공부 = 신살 특화.** `[고급강의] 괴강살·역마살` — 우리 신살 지식의 공백을 정면으로 메운다. 3시간이면 끝나는데 밀도가 제일 높을 수 있다.
- **남석 = 양은 최대(2,473편), 밀도는 미지수.** 데일리 15분 강의라 반복·홍보가 섞였을 가능성. **반입 후 반복 접기(P1 규칙)를 반드시 적용**할 것.

## 5. 함정·주의 (다음 세션이 밟기 쉬운 것)

- **엔진은 반드시 런처 PATH로 발사** (`SKILL/.venv/bin` 선두). 안 그러면 번들 ffmpeg를 못 찾아 전량 FileNotFoundError로 죽는다(7/22 전멸의 진범). 큐 스크립트엔 `which ffmpeg` 게이트를 넣어놨다.
- **쿠키 + 2스트림 동시 = 금지.** 지금 큐는 전부 `--no-cookies`라 안전. 멤버십 영상은 로그에 FAIL로 남으니, 전 채널 완주 후 FAIL만 모아 **1스트림+쿠키**로 수확할 것.
- **보드의 [재시도]·[일괄 재시도]를 메인 엔진 가동 중에 누르지 말 것** (쿠키 2중 사용 사고 패턴).
- **큐를 중간에 끊어도 손실 0** — 재실행하면 안 한 것만 이어간다. 순서를 바꾸려면 스크립트의 `run_channel` 호출 순서만 바꿔 재실행.
- 🚨**보안**: 운영자가 첨부한 쿠키 파일이 **브라우저 통짜 덤프**였다(유튜브뿐 아니라 proton메일·GitHub·X·페이스북·네이버·claude.ai 세션 포함). 금고로 옮기고 다운로드 폴더에서 제거했다. 앞으로 쿠키를 받으면 **값을 절대 출력하지 말고**, 도메인·쿠키명만으로 검사한 뒤 즉시 `~/.whisper_cookies/`(chmod 600)로 옮길 것.

## 6. 전사 완주 후 할 일 (순서대로)

1. **전수 무결성 검사** — BOM/CRLF/파일명 NFC/과소파일(400자 미만·U+FFFD)/동일줄 4회 이상 반복. 초코 249편에 쓴 절차 그대로.
2. **운영자 확인** — 슬롯4 편입 여부(신규 5채널을 정제 입력에 넣을지). ※현 정제 입력 범위는 슬롯 1·2·3.
3. **P0 전사 라운드 확장** — 스펙 v1.3(캡션 재조립·오전사 원문 보존·flags 3종)을 그대로 적용하되, **채널별 저자 클러스터를 새로 부여**(현 4클러스터 → 최대 9클러스터). 오전사 정규화 사전 v2에 채널별 변이를 추가 수집.
4. **P1 착수 판단** — 전사가 9.8일 걸리므로, **전사 완주를 기다리지 말고 P1(웹 코퍼스 색인)을 병행**하는 것이 맞다. P1 스펙 초안 = `20260722_031500_P1_스펙_초안_v0.9.md`(운영자 확인 후 정본 승격).
