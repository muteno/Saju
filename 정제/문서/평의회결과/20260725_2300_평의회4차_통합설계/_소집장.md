# 평의회 4차 — 소집장 (2026-07-25 23:00 KST)

> **의제: 흩어진 산출물을 하나로 엮는 설계.**
> 운영자(도령님) 원문 —
> *"너가 이미 그 만든거 많더라고, **그것들이 다 별개로 놀거는 아니라는거지**"*
> *"근데 지금 이거는 트리구조화 이잖아, 근데 실제로는 **뉴런구조화**하면서 뉴론도 연결만되는게 아니라,
> 그걸 **거리상 연계, 뉴런 연결 빛의 강도, 신뢰도를 나타내는 정도** 이런식으로 해야되는데
> 원래 지식망인가? 원래 거있었잖아. **그거날릴게아닌거같음**"*
> *"이게 하나로 엮는게 굉장히 어려울건데, 페이블 5인 소환, 오퍼스 10인 소환"*

---

## 0. 평의회 규칙 (어기면 그 위원 의견서는 폐기)

1. **파일을 수정하지 마라. 읽기 전용이다.** 지금 다른 담당 3인이 `build_neuron_map.py`·
   `build_graph_view.py`·신규 문서를 동시에 고치고 있다. 너는 **자기 의견서 1장만** 쓴다.
2. **숫자를 인용하려면 직접 재라.** 이 소집장의 숫자도 의심해라 —
   이번 회차 3차 평의회에서만 **틀린 인계 수치가 8건 확정**됐다(반전5→8 · hits82→84 ·
   16,161→16,247 · 43/68 재현불가 · 1,305자는 JSON직렬화길이 · 1,504→1,614 · 게이트 6종→13종).
   실측한 건 `[실측]`, 계산·판단은 `[추론]`, 안 잰 건 `[미측정]`이라고 **반드시 표기**하라.
3. **이 프로젝트 사고는 100% "조용한 오작동"이었다. 단 한 번의 예외도 없었다.**
   터지는 버그는 없었고 전부 **통과하면서 틀렸다.** 그러니 "에러 안 나니까 괜찮다"는 근거가 아니다.
4. **다수결로 뭉개지 마라.** 다른 위원과 갈리면 갈린 채로 근거를 대라. 의장이 판정한다.
5. **기존 자산을 날리자는 제안은 금지.** 운영자가 *"그거날릴게아닌거같음"*이라고 명시했다.
   접자는 제안은 "무엇으로 대체되는가 + 되돌릴 수 있는가"를 같이 써라.
6. 운영자는 **비전문가**다. 결론은 쉬운 말로. 사극 말투 금지, 전문용어 남발 금지.

---

## 1. 지금 있는 것 — 전수 목록 [실측 260725 23:00]

### 화면 5개 (전부 자립형 HTML, 외부 참조 0)
| 파일 | 생성기 | 무엇 | 갱신 |
|---|---|---|---|
| `현황판.html` | `build_dashboard.py` | 공정 진척·수치·커버리지 | 08:32 |
| `개념지도.html` | `build_concept_map.py` | 대·중·소 주제 트리 + 개념별 자료량 | 14:35 |
| `개념트리.html` | `build_concept_tree.py` | 리프트 기반 계층 (5.7MB) | 09:08 |
| `뉴런망.html` | `build_neuron_map.py` | 개념별 다리 수 **표** | 14:34 |
| **`지식망.html`** | `build_graph_view.py` (77KB) | **★그래프 뷰 — 운영자가 "원래 있던 거, 날리지 마라"고 지목한 그것** | 14:35 |

### 데이터 층
```
P0 배치 산출물 → build_dashboard → posts_all.jsonl(3,845) · paras_all.jsonl(38,072) · stats.json
                                        ↓
   ┌────────────────────────────────────┼─────────────────────────────┐
   ↓                                    ↓                             ↓
build_concept_map              build_concept_tree            build_neuron_map
 (TAXONOMY 정본)                (concept_tree.json)          (neuron_nodes 249 · neuron_edges 21,916)
                                                                      ↓
                                              build_relation_edges (relation_edges.jsonl) ★260725 신설
                                                                      ↓
                                              build_graph_view (지식망.html)
                                              measure_knowledge_map (knowledge_metrics.jsonl) ★신설
```

### 판단 유닛
`P2_유닛\pilot_UI.jsonl`(U-INV 6) · `pilot_UJ.jsonl`(U-JUDGE 6) = **12개**.
chain 73단계(반전 8) · 축자 4,387자 · 횡단 링크 92 · 개념링크 68.

---

## 2. 오늘 새로 잰 것 — 이 숫자들이 의제의 근거다 [실측]

### ★거리를 세 그래프에서 따로 재봤다
```
공기(동시출현)  밀도 70.8%  →  평균거리 1.29 · 지름 3  · 도달불가  0.0%
관계(결정론)    밀도  1.9%  →  평균거리 5.51 · 지름 12 · 도달불가 52.1%
계층(트리)                  →  평균거리 3.14 · 지름 4
```
**공기 평균거리가 1.29다.** 249노드 중 아무 둘이나 골라도 한 다리 건너 닿는다.
= "가깝다"가 아무 정보도 안 나른다. **이게 운영자가 화면에서 본 털뭉치의 정체다.**
간선 21,916개 중 **w==1(딱 한 문단에서 우연히 같이 나옴)이 3,404개 = 15.5%**인데
`목생화`와 **같은 굵기**로 그려진다.

### ★결정론으로 아무 데도 못 닿는 개념 59개 — 그 목록이 결정적이다
```
재물·돈 · 직업·직무 · 관계·결혼 · 사업·창업 · 학업·시험 · 적성·재능 · 이동·이사
운 리딩 문법 · 라포·공감 · 좋다·나쁘다 배격 · 행동·환경 개운 · 신년운세 …
```
**사람이 실제로 묻는 것이 전부 여기 있다.** 기초(글자·구조)는 자기들끼리 촘촘히 이어져
있는데 "그래서 재물은?"으로 건너가는 다리가 **0개**다.
운영자 원문(방법론 보드를 두고): *"그게 완~~~~~~~~전 기초 근데 **그거보고는 사주를 못풀어.**"*
→ 그 문장의 기계 번역이 **도달불가 52.1%**다.

### 260725 저녁에 새로 넣은 것
- **계층 간선 300** (대주제 15 → 중주제 51 → 개념 249) — 전엔 노드의 `big`/`mid` **문자열로만** 있었고
  간선이 없어서 화면에서 `오행(총칭)`과 `신금(辛)`이 형제처럼 떴다. 고아 노드 **0개**.
- **결정론 관계 간선 587** — 생21·극21·합33·충10·형7·파6·해6·원진6·귀문6·지장간33·소속22·
  롤업61·구성227·전제69·귀결15·육친12·순서8·발현5·인접3·비교8·관계4·적용4.
  `{kind, dir, polarity, weight, 근거유형, layer, source, author, as_of}` 스키마.
  weight 고유값 **12종**(최빈 0.9가 46.7%) — 전엔 0.7 하나가 **90.2%**였다.
- 관계가 붙은 개념 **190/249**. 나머지 59는 위 목록.

---

## 3. 알려진 병 — 통합 설계는 이걸 고치거나, 최소한 재발시키지 않아야 한다

| # | 병 | 실측 |
|---|---|---|
| R1 | **노드 `해`(害) 오염** | 2,740문단 중 **96.0%가 亥**(해수) 문단. 害 적중 110건 = **4.0%**. 그 위에 다리 237개가 깔림 |
| R2 | **생산만 되고 아무도 안 읽음** | `별칭_판정.json` 판정 658건 · 소비자 **0**이었다(260725 배선으로 해소). `stats.json`은 "커버리지 0으로 찍힌다"는 알려진 결함이 있는데 **소비자 0이라 아무 일도 안 일어났다**. `concept_tree.json`·`knowledge_metrics.jsonl`도 현재 소비자 0 |
| R3 | **인용 자격** | 유닛 개념링크 68건 중 **26건(38.2%)이 저자 축자에 없다**(D5 정의). 260725 게이트 투입 |
| R4 | **쓰기 전용 필드** | `adjacent` true 26/92 저장 · **읽는 코드 0줄** / `오전사` 12/12 유닛 보유 · **소비자 0** |
| R5 | **파이프라인 미일원화** | `현황판_갱신.bat`이 부르는 건 **2개**인데 실제 단계는 **8개**. 그래서 실측 22:51 시점에 data/가 **네 시대**에 걸쳐 있었다(08:32 / 09:08 / 14:35 / 22:51) |
| R6 | **계정 하드코딩** | 빌더 2개의 `BASE`가 없는 계정 경로(`C:\Users\[가림-계정]\`)라 **실행 자체가 불가**였다. 과거 `p0_verify.py`·`p0_global.py`도 같은 이유로 죽은 전례 |
| R7 | **전사 유실** | 석우당 1,921편 중 **130편이 권한도 오류도 아닌 작업 유실**. 로그는 `🏁 전량 종료 847편`으로 찍혔다 |

---

## 4. 운영자가 이름 붙인 축 6개 — 통합 화면은 이걸 **동시에** 보여야 한다

| 축 | 운영자 원문 | 지금 상태 |
|---|---|---|
| ① 트리구조 계층화·카테고라이징 | *"뉴런의 핵심은 트리구조로 다 카테고라이징되고 계층화 되어있을거고"* | 계층 간선 300 신설 · 고아 0 ✅ |
| ② 연결 강도 | *"뉴런(키워드)간 연결의 강도는 얼마나 이게 강하게 붙어있느냐"* · *"굵기, 채색으로도 놔눌수있는"* | weight 12종 ✅ |
| ③ 붙어있는 지식 | *"붙어있는 지식"* | 문단 중앙 303 · 얇음(<100) 75개 |
| ④ **거리상 연계** | *"지식 간 거리"* · *"그걸 거리상 연계"* | 오늘 신설. **세 그래프가 따로 논다** |
| ⑤ **연결 빛의 강도** | *"뉴런 연결 빛의 강도"* | ⬜미착수 — weight와 별개 축인가? |
| ⑥ **신뢰도** | *"신뢰도를 나타내는 정도"* · *"이를 신뢰할수있게 만들어가야되는거지"* | 근거유형은 있으나 결정론/축자/가설이 한 축에 섞일 위험 |

**⚠④⑤⑥은 서로 다른 축인가, 같은 축의 다른 이름인가 — 이게 이번 평의회의 핵심 쟁점 중 하나다.**

---

## 5. 절대 원칙 (프로젝트 헌법 — 뒤집으려면 운영자 승인)

- 검색(RAG) 금지 · **계산된 키셋의 결정론 조회** · 모든 주장에 **축자 인용 + 출처** ·
  이견은 합치지 말고 **관점 병기** · LLM은 서술층일 뿐
- 임베딩/RAG 금지 · 형태소 분석기 금지 · 동시출현 `w`를 L2 가중치로 승격 금지
- **동시출현 간선 21,916개를 삭제하지 마라** — 잘라내면 "옆으로 샐" 다리가 0.42%로 죽어
  보드 1-19④(*"관계된 것과 일치해서 그 방향으로 언제든 새어 나갈 수 있어야함"*)가 죽는다.
  처분은 **`kind:"공기"` 강등**이지 삭제가 아니다
- 화면은 **자립형**(외부 라이브러리·CDN·네트워크 요청 0). 오프라인 더블클릭으로 열려야 한다
- `1. 원본 라이브러리\` 수정 금지 · `★직관보드.md` append-only · 슬롯1(운영자 정리본)은 **가설로만**

---

## 6. 제출물

`02_평의회결과\20260725_2300_평의회4차_통합설계\<위원ID>_<역할>.md` 1장.

**반드시 포함:**
1. **한 줄 결론** — 통합의 핵심이 무엇인가
2. **근거 숫자** — 직접 잰 것만. `[실측]`/`[추론]`/`[미측정]` 표기
3. **네가 틀렸을 수 있는 지점** — 자기 반증 1개 이상
4. **다른 위원과 갈릴 것 같은 지점** — 예상되는 반대 논거와 그에 대한 답
5. **하지 말아야 할 것** — 네 관점에서 본 금지 목록

분량 제한 없음. 단 **인용 없는 숫자는 쓰지 마라.**
