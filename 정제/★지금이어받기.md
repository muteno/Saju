# ★지금 이어받기 — 사주 정제 프로젝트 (2026-07-27 작성)

> **이 파일 하나만 읽고 이어받으면 된다.** 다른 문서를 먼저 열 필요 없다.
> 세션·계정·모델이 바뀌어도 여기부터 시작한다.

---

## 0. 30초 요약

사주 개념 **262개**를 노드로 세우고 그 사이 **관계 6,737개**를 층으로 나눈 지도가 있다.
오늘 그 지도가 처음으로 깃 리포에 들어갔다. **그런데 아직 앱 화면에는 안 뜨고, 도구도 옛 폴더를 보고 있다.**

| | |
|---|---|
| **작업할 곳** | `C:\dev\muteno-Saju` (= `github.com/muteno/Saju`, **공개 리포**) |
| **최신 커밋** | `c91293c` — 이게 안 보이면 폴더나 시점이 틀렸다 |
| **당장 할 일** | 아래 §2의 1번(경로 고치기)부터 |
| **원본 보관소** | OneDrive `…\6.  Nomute\3. 사주\` — **읽기 전용으로만 써라. 작업은 리포에서 한다.** |

---

## 1. 오늘 무슨 일이 있었나 (배경 — 안 읽으면 같은 사고를 반복한다)

1. 지도가 OneDrive 폴더에만 있었다 → 깃으로 옮기기로 했다.
2. 처음엔 앱 코드까지 통째로 옮기려 했는데, **리포가 살아 있는 활성 리포**(오늘만 PR #141·#142·#143)였고 **옮기려던 앱 코드는 그 리포의 낡은 스냅샷**이었다. 그대로 넣었으면 오늘 작업을 덮을 뻔했다. → **`정제/`와 새 파일 3개만** 넣었다.
3. 리포가 **공개(Public)**라 개인정보·저작물을 걷어냈다(§3).
4. 머지·푸시 완료. 설정도 잡았다(Opus 5 · high effort · 권한 열림).

**남은 미완 2건이 §2의 1·2번이다.**

---

## 2. 할 일 — 순서대로

### 1️⃣ 경로 9곳 고치기 (제일 먼저)

파일 9개에 `C:\Users\Hwang\OneDrive - GS…` **절대경로가 박혀 있다.**

```
정제/_현황판/build_linkweb.py
정제/_현황판/build_neuralnet.py
정제/_현황판/build_neuralnet3d.py
정제/_현황판/build_obsidian_vault.py
정제/_현황판/distill_definitions.py
정제/_현황판/_피그잼정제/_도구/cmp_engine.py
정제/_현황판/_피그잼정제/_도구/extract_full2.py
정제/_현황판/_피그잼정제/_도구/gen_jogyeon.py
정제/_현황판/_피그잼정제/_도구/gen_rules.py
```

**🔴 왜 최우선인가:** 지금 파이프라인을 돌리면 **리포의 코드가 OneDrive의 데이터를 읽고 OneDrive에 쓴다.** 그러면 이후 모든 검증이 «옛 데이터를 보고 초록»을 낸다. 그게 오늘 반나절을 태운 사고와 같은 형태다.

**어떻게:** `정제/_현황판/경로.py` 한 파일에 `BASE`를 두고 9개가 그걸 부르게 한다.
**본보기:** `정제/_현황판/코퍼스.py`가 이미 `Path(__file__).resolve().parent` 방식이라 안 깨졌다.
⚠9개를 각각 고치지 마라 — 이 프로젝트가 열두 번 반복한 병이 **「한 군데만 고치기」**다.

### 2️⃣ 파이프라인 완주

```
cd 정제/_현황판
python 파이프라인.py --check     # 먼저 이것
python 파이프라인.py --all       # 초록이면 이것
```

⛔**멈추면 우회하지 마라. 멈춘 자리를 고쳐라.** 멈춘 것 자체가 정보다.
(오늘 이걸 어겼다 — 검사기가 가짜 적색을 냈는데 하류를 손으로 돌렸다.)

### 3️⃣ 앱 배선 — 두뇌가 아직 화면에 안 뜬다

리포에 **들어간 것**(전부 새 파일, 충돌 없음):

| 파일 | 정체 |
|---|---|
| `app/public/brain.json` | 두뇌 팩 3.3MB |
| `app/src/engine/brain.js` | 어댑터 — `loadBrain·keyToNode·relationsOf·jogyeonOf·readChart` |
| `app/src/components/BrainPanel.tsx` | 패널 — **⚠아무 데도 안 붙어 있다** |

**일부러 안 넣은 것 2개:**

- `app/src/engine/index.js` — 여기에 「두뇌도 같이 로드」를 넣어야 하는데, **리포판이 그새 달라져서**(리포 256줄 / 옛 사본 273줄) 덮으면 오늘 작업이 되돌아간다. **손으로 다시 붙여라.**
- `app/src/pages/Result.tsx` — **리포에 이 파일이 없다.** 현재 페이지는 `Analysis · Auth · Fun · InfoInput · Intro · Loading · Settings · Talk`다. 앱이 재편됐다.
  → `<BrainPanel brain={…} />`를 **어느 화면에 붙일지 다시 정해라**(`Analysis.tsx`가 유력하나 확인 필요).

**참고 자료:** 옛 `Result.tsx` 원본이 `C:\dev\saju\app\src\pages\Result.tsx`에 있다(임시 폴더, 아직 안 지웠다). 배선 끝나면 `C:\dev\saju`는 지워도 된다.

### 4️⃣ 실제로 뜨는지 확인

```
cd app
npm install
npm run build
```

### 5️⃣ 그 다음 밀린 것

- **반입 대기 약 240건** — 조견표 37 · 간선 27+79 · 판본병기 53+47
- **통근 조건 슬롯** — 관계 위원이 「세 대주제 낭설률을 한꺼번에 내리는 처방」으로 지목한 것
- **승인 대기 3건**(운영자 판단 필요) — 십이운성 `병·사`/`절·태·양` 가르기 · `수옥살`↔`겁살·재살` 고유성 충돌 · `궁합` 노드 신설

---

## 3. 🔴 공개 리포다 — 되돌리면 안 되는 것

운영자 260727: *"시크릿 안할거야"* → **비공개 전제로 통과시켰던 판정이 전부 무효다.**

| 뺀 것 | 왜 | 되살리는 법 |
|---|---|---|
| `정제/_현황판/_2차분류/` (262파일·42MB) | 저자 6인 블로그·강의 **원문 축자 발췌** | `python build_gather.py` (로컬에서만) |
| `figjam_board_raw.txt`의 「팀원 사주」 절 | 실명 + 개인 명식 20,320자 | **안 되살린다** |
| 이메일 311건 | 전사에 딸려온 상담 신청 주소 | **안 되살린다** |

**문은 생성 지점에 있다** — `정제/_현황판/코퍼스.py`의 `가리기()`를 `build_gather.py`가 부른다.
산출물에서만 지우면 **파이프라인 한 번에 되살아난다**(오늘 실증: 이메일 311건이 그랬다).

⚠**`.gitignore`는 줄 끝 주석을 지원하지 않는다.** 오늘 경로 뒤에 `# 772MB`를 붙였다가 그 줄이 통째로 안 먹어 **954MB가 그대로 추적됐다.** 주석은 반드시 제 줄에 쓴다.

⚠**「주민번호 3,143건」은 오탐이다** — 절기 epoch(`-2199522818312`)와 FigJam 좌표(`x="727.1234567890123"`)다. **지우면 절기표가 깨진다.**

**미결(운영자 판단 대기):** git 히스토리에는 「팀원 사주」가 그대로 있다. HEAD에서만 가렸다. 완전 제거는 히스토리 재작성 + force push라 PR 143개짜리 공개 리포에선 대가가 크다.

---

## 4. 🔴 이 리포엔 다른 손이 있다

오늘 한 시간 사이 **#141 → #142 → #143**이 따로 들어왔다. 지금 CLI 세션은 **권한이 다 열려 있다**(`bypassPermissions`).

- 푸시가 거절되면 **절대 `--force` 쓰지 마라.**
- `git fetch` 후 **무엇이 들어왔는지 파일 단위로** 확인한다.
- 겹치지 않으면 rebase, **겹치면 멈추고 보고**한다.
- 앞으로는 main 직행보다 **브랜치 + PR**이 안전하다.

---

## 5. 정제의 원칙 (이걸 어기면 산출물이 통째로 오염된다)

- **관계를 정의하고 «의미(결론)»를 정의하지 않는다.** `임수2 = 유학`을 저장하면 그건 정제가 아니라 해석이다.
- **결정론은 캐지 말고 적어 넣는다.** 목생화·십성 생성 규칙을 코퍼스에서 캐면 오전사·유파차로 오염된다.
- **조건이 없는 문장은 버린다.** 조건 없는 진술은 그 자체로 «의미 정의»다.
- **판본이 갈리면 지우지 말고 약하게 건다**(stance 병기).
- **노드는 고유해야 한다.** 같은 개념이 두 노드로 갈리면 그 아래 모든 데이터가 어긋난다.
- **어휘 규칙:** 데이터에 손대는 말은 «이사/승격/강등/병기»만 쓴다. **「뺀다」는 금지어**다.

**더 깊이:** `정제/문서/CLAUDE.md`(최상위 규범) · `정제/문서/★직관보드.md`(운영자 원문, **요약 금지 구역**) · `정제/문서/★이어받기.md`

⚠**리포 최상위 `CLAUDE.md`는 다른 규범집이다**(「기틀 조문」 — 성공/실패 이원제, 6단 보고, 10칸 계획 표, 평의회 8인). 앱·엔진 작업은 그쪽, 정제 작업은 `정제/문서/CLAUDE.md` 화법을 따르면 된다.

---

## 6. 이 프로젝트가 열두 번 반복한 병

**「한 군데만 고치기」** — 추출기를 고치고 적재기를 안 고친다. 판정 파일을 만들고 아무도 안 읽는다. 빌더를 넓히고 검증기는 안 넓힌다.

실물:
- 게이트를 만들어 놓고 **검사기가 그 게이트를 몰랐다**
- 관측창을 11.67%→100%로 넓혔는데(**옳은 결정**) 그 때문에 동반어 검사가 조용히 무력해졌다
- 검사기의 국소 창이 매치 구간을 포함해 **자기가 자기를 증명**했다(「소화기」 동반율 100%→12.5%)
- 정규식 패치가 `add()` 호출 경계를 넘어 **천간 오합 5쌍을 「육합」으로 라벨**했다 — **에러는 하나도 안 났다**

**있는 장치:**
- `_현황판/lint_markers.py` — 게이트 배선 정적 검사 · 일상어 별칭 발굴 · 부채 래칫
- `_현황판/data/검사_반례.jsonl` — **검사를 일부러 부러뜨려 우는지 본다**(돌연변이 시험).
  **「픽스처가 통과했다」는 「검사가 산다」의 증거가 아니다.**
- `_현황판/파이프라인.py` — 35단계 · 순서 강제 · 낡음 검사 · **생산-소비 대차대조표**
- `_현황판/작업내역.py` — 덮어쓰기 전 스냅샷. 되돌리기 = `python 작업내역.py 되돌리기 <번호>`

---

## 7. 수치 (2026-07-27 기준 — **인용할 땐 잰 시각을 같이 적어라**)

```
노드 262 · 정의 262/262 · 링크망 6,737 (L1 1,492 / L1½ 235 / L2 5,006 / L3 4)
근거사슬 대상 5,241 · 인과닫힘 149 · 낭설후보 1,893 (36%)
코퍼스 48,729문단 = 웹 21,026 + 전사 27,703
```

⚠**낭설률을 인용할 땐 분모를 밝혀라.** 오늘 「29%」가 실은 지도의 14%만 잰 값이었다.
⚠**전사는 지금도 늘고 있다**(맥북이 계속 뽑는 중) — 재적재하면 코퍼스가 커진다.

---

## 8. 막히면

| 증상 | 원인 / 답 |
|---|---|
| `401 OAuth access token has been revoked` | `/login` (사람이 직접 해야 함) |
| 푸시 거절 `[rejected]` | 다른 손이 밀었다. §4 절차대로. **`--force` 금지** |
| 파이프라인이 멈춤 | 우회하지 말고 멈춘 자리를 고쳐라 |
| 수치가 이상함 | 분모를 확인해라. 그리고 **지금 어느 폴더를 보고 있는지** 확인해라 |
| 「자료 없음」이 뜸 | 방법론 보드는 코퍼스 기본 집합에서 빠진다 — 보드까지 포함해 다시 재라 |
