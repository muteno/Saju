# -*- coding: utf-8 -*-
"""★「왜」 판정 어휘 — **여기 한 곳에만 적는다.**

═══ 왜 이 파일이 생겼나 (260728) ═══

`build_relation_table.py`(정본 표)는 L1 결정론 간선에 종류별로 제 이름
(공리/파생/인과/목차)을 붙이는데, **`build_brain.py`(앱 두뇌 팩)는 그 표를
안 갖고 있었다.** 그래서 같은 간선이 두 산출물에서 다르게 나왔다:

    관계도.csv   발현 157건 → 「인과」 · L1 1,551건 전부 판정 있음
    brain.json   같은 1,555건 → **전부 `왜 = null`**

앱은 `왜`가 없는 관계를 「이유 있는 것」 목록에서 빼기 때문에,
**「글자 → 겪는 일」을 말하는 유일한 층인 발현 간선 157건이 화면에서
통째로 사라져 있었다.** 앱 화면의 「이유 있는 것 N개」도 그만큼 적게 나온다.

이 프로젝트가 열세 번 반복한 「한 군데만 고치기」의 또 한 표본이다 —
추출기를 고치고 적재기를 안 고친다. 그래서 **표를 옮기지 않고 갈라냈다.**
다음에 판정 어휘가 늘면 이 파일만 고치면 둘 다 따라온다.

⚠새 소비자가 생기면 여기서 import 해라. 자기 사전을 따로 만들지 마라.
"""

# ── L1은 «검증 대상»이 아니라 검증의 **바탕**이다 ──────────────────
#   그냥 «미검증»으로 두면 1,551개가 마치 «못 믿을 것»처럼 보인다 — 정반대다.
L1KIND = {}

# 공리 — 더 물으면 명리 밖으로 나간다
for _k in ("생", "극", "소속", "지장간", "구성", "음양", "비화", "롤업", "육친",
           # «산출» = 신살 조견표(일간→지지). «입력에서 글자를 뽑는 표»라 공리에 가깝다.
           #   더 물으면 «왜 갑목의 천을귀인이 축미인가»가 되는데 그건 고전 표(甲戊庚牛羊)다.
           "산출",
           # «절차» = 강약 점수제(110점)·득령득지득세. 조견표와 같은 등급 —
           #   «원국 8글자를 넣으면 숫자·판정이 나온다»는 계산 규칙이라 더 물을 데가 없다.
           "절차"):
    L1KIND[_k] = "공리"

# 파생 — 공리로 환원된다(전수 확인됨)
for _k in ("합", "충", "형", "파", "해", "원진", "귀문", "제화", "천간합", "육합",
           "삼합", "방합", "반합", "합화", "천간충", "지지충", "삼형", "상형", "자형"):
    L1KIND[_k] = "파생"

# 인과 — «그래서 이렇게 된다»를 잇는 고리. **이 층이 이 프로젝트의 병목이다.**
for _k in ("귀결", "발현", "적용"):
    L1KIND[_k] = "인과"

# 목차 — 학습 순서·참조·대조. 이유가 아니다(사슬 경로에서 뺀다)
for _k in ("전제", "순서", "비교", "인접", "관계", "반증"):
    L1KIND[_k] = "목차"


VERDICT_NOTE = {
    "공리": "더 물으면 명리 밖으로 나간다 (생·극·소속·지장간·구성·음양…)",
    "파생": "합·충·형·파·해·원진 — 공리로 환원된다(전수 확인됨)",
    "인과": "«그래서 이렇게 된다»를 잇는 고리 (발현·귀결·적용)",
    "목차": "학습 순서·참조·대조. 이유가 아니다 — 사슬 경로에서 뺀다",
    "공리직결": "공리 하나로 바로 닿는다 — 이유가 자명",
    "사슬있음": "공리 + 인과 고리로 끝까지 닿는다",
    "인과닫힘": "힘이 오가고 «그래서 어떻게 되는지»까지 닿는다",
    "작용까지": "힘은 오가는데 «그래서»가 없다",
    "발현만": "결과는 붙었는데 힘이 오간 자리가 없다",
    "분류경로": "「A는 B에 속한다」 — 이유가 아니다",
    "구조만": "구조는 이어지는데 «그래서 어떻게 되나»가 없다",
    "낭설후보": "L1으로 경로가 없다 — 근거를 못 댄다",
    # ★260728 — V1 재심: 낭설 1,609의 47.5%가 인물·상담 접촉이었다. 명제가 아니라 메타.
    "비명제": "인물·상담화법 접촉 — 지식의 내용이 아니라 메타(누가 어떻게 말했나)라 경로가 원리상 없다",
    "미검증": "사슬 검증을 아직 안 돌린 것 (L2 중·약)",
}


def 판정(간선, 사슬표):
    """간선 하나의 «왜»를 낸다. 정본 표와 앱 두뇌 팩이 **같은 답**을 내게 하는 유일한 문.

    간선: {층, kind, a, b, …}   사슬표: {(a,b): {판정, 사슬}}
    """
    if 간선.get("층") == "L1결정론":
        return L1KIND.get(간선.get("kind"), "공리")
    a, b = 간선.get("a"), 간선.get("b")
    # ⚠양방향으로 찾는다 — 근거사슬은 한 쪽 순서로만 적히는데 링크망은 둘 다 온다.
    ch = 사슬표.get((a, b)) or 사슬표.get((b, a))
    return ch["판정"] if ch else "미검증"
