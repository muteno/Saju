# -*- coding: utf-8 -*-
"""
경로 정본 — 위치는 전부 여기서 받는다. 다른 파일에 경로 리터럴을 두지 않는다.

★왜 이 파일이 생겼나 (260727)
  OneDrive 절대경로(`C:\\Users\\Hwang\\OneDrive…`)가 9개 파일에 박혀 있었다.
  그 상태로 파이프라인을 돌리면 **리포의 코드가 OneDrive의 옛 데이터를 읽고 거기에 쓴다** —
  이후 모든 검증이 «옛 데이터를 보고 초록»을 낸다. 리포를 어디에 클론하든 돌게,
  모든 위치를 __file__ 기준 상대로 여기 한 곳에만 적는다.

  OneDrive ↔ 리포 대응(260727 실측):
    3. 사주\\2. 정제작업\\_현황판   ↔  정제/_현황판          (= 이 파일이 있는 곳)
    3. 사주\\4. 구 사주본_Saju-main ↔  리포 루트              (앱·엔진은 리포가 살아있는 정본)
    3. 사주\\7. 옵시디언 볼트       ↔  <리포 루트>/7. 옵시디언 볼트  (생성 산출물 · git 추적 안 함)

  ★원본 반입(운영자 260727 «아예 로컬에서의 작업은 없앨거야 · 비공개 불요, 정제본/원본 구분만»):
    원문 재료는 `정제/원본/`에 산다(P0_인벤토리·전사). 반입 = `원본_반입.py`(개인정보 가리기 내장).
    반입 전 레거시 로컬 배치도 계속 돌아가게, 신·구 중 **있는 쪽**을 쓴다.
    ⚠제3자 개인정보(상담 이메일·전화·팀원 사주)는 어느 리포에도 안 싣는다 — 반입기가 가린다.
"""
from pathlib import Path

현황판 = Path(__file__).resolve().parent          # 정제/_현황판
정제 = 현황판.parent                               # 정제/
뿌리 = 정제.parent                                 # 리포 루트 (muteno/Saju)

DATA = 현황판 / "data"
피그잼 = 현황판 / "_피그잼정제"

# ── 리포 안 정본
방법론 = 뿌리 / "dosa-app" / "methodology"         # figjam_board_full.md · figjam_board_raw.txt
엔진_VENDOR = 뿌리 / "app" / "src" / "engine" / "vendor"  # 계산 엔진(생성물 — 손대지 마라, D2-1)

# ── 생성 산출물(리포 밖으로 뽑는 것 — git 추적 안 함)
옵시디언_볼트 = 뿌리 / "7. 옵시디언 볼트"

# ── 원본(원문 재료) — 리포 반입 자리. 반입 전엔 레거시 로컬 위치로 폴백.
원본 = 정제 / "원본"
_P0_신 = 원본 / "P0_인벤토리"
_P0_구 = 정제 / "P0_인벤토리"                       # 레거시(반입 전 로컬 배치)
P0_인벤토리 = _P0_구 if (not _P0_신.exists() and _P0_구.exists()) else _P0_신
_전사_신 = 원본 / "전사"
_전사_구 = 뿌리 / "0. 전사프로그램" / "전사 내용"    # 레거시(맥북 생산 폴더)
전사_내용 = _전사_구 if (not _전사_신.exists() and _전사_구.exists()) else _전사_신
