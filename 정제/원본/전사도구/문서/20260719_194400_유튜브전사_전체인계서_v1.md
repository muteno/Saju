# 유튜브 채널 전사 — 전체 인계서 (어떤 세션·어떤 LLM이든 이것만 읽으면 이어받는다)

- **작성(KST):** 2026-07-19 19:44 · 작성 주체: Claude Code (맥북 M5 세션)
- **문서 목적:** 시행착오 전부를 실측으로 끝내놨다. 새로 오는 AI/사람은 아무것도 재실험하지 말고 이 문서의 결론을 그대로 쓰면 된다.
- **말투 규칙:** 이 프로젝트의 사용자는 비전문가다. 친근한 반말, 쉬운 비유로 보고할 것.

---

## 0. 지금 무슨 일이 벌어지고 있나

> **★ 2026-07-20 07:00 갱신 — 본 작업 완료됨.**
> 92/92 전사 성공(실패 0) · 루프 손상 26개 재전사 완료(전원 개선) · 인코딩 264개 통일 ·
> 부속물 3종 갱신 · 최종 압축본 `전사본_압축본/노뮤트_전사본_20260720.zip`.
> §6 완료 후 임무는 **전부 실행됐다** (dashboard.html 재생성만 다음 채널 과제로 이월).
> 현 상태 요약 정본 = `황세웅/saju_진행현황_이어하기.txt`.
> 남은 사용자 임무 = §5-4 쿠키 처리(critical). 남은 기술 과제 = §7 다음 채널 개선 목록.

- (이하 원문 보존 — 작성 시점 7/19 19:44의 기록)
- 유튜브 채널 **도화도레 사주(@dohwadore_saju, 영상 259개)**의 대본을 전부 **Whisper large-v3 정본**으로 통일하는 작업.
- 진행 확인: 브라우저에서 `http://localhost:8765` (전사 프로세스 알리미) 또는 폴더의 `_whisper_mac.log`.

## 1. 파일 지도 (전부 실존 경로)

| 자산 | 경로 |
|---|---|
| ★ **새 채널 전사 실행기 (더블클릭)** | `~/Desktop/유튜브채널전사.command` — 링크 넣으면 미리보기→3개 시험→전체 2스트림. 검증 스택 전부 내장 (원본: 스킬 폴더) |
| ★ 범용 엔진 (2026-07-20 신설) | `~/.claude/skills/whisper/whisper_channel_generic.py` — 2스트림·LPT·머리말재개·원자쓰기·BOM/CRLF·NFC·루프차단·저용량오디오·쿠키폴백·한국어원제 고정 |
| ★ 홍염 턴키 실행기 | `~/.claude/skills/whisper/run_hongyeom.sh` — 홍염 가입+새쿠키(~/.whisper_cookies.txt) 후 한 줄 실행 (53+루프멤버십3 = 56개, 약 13h) |
| **작업 폴더(결과물)** | `~/Library/CloudStorage/OneDrive-GS칼텍스예울마루/황세웅/dohwadore_saju_자막/` |
| 전사 엔진 | `~/.claude/skills/whisper/whisper_saju_mac.py` |
| 실행기(범용) | `~/.claude/skills/whisper/run_job.sh` |
| 진행 보드(웹) | `~/.claude/skills/whisper/board_server.py` + `board_config.json` |
| 파이썬 환경 | `~/.claude/skills/whisper/.venv/` (Python 3.12 + mlx-whisper + yt-dlp + 번들 ffmpeg) |
| §B 원본 엔진(범용, 수정금지) | `~/.claude/skills/whisper/whisper_channel_mac.py` |
| 라이브 로그 | 작업 폴더 `/_whisper_mac.log` |
| 재시도 대기열 | 작업 폴더 `/_retry_queue.txt` |
| 교체 전 백업(92개) | 작업 폴더 `/_versions/260719_1830_유튜브자막_교체전/` |
| 메타데이터·유튜브 자막 원본 | 작업 폴더 `/_raw/` (`<영상ID>.info.json`, `.ko.json3`) |
| 구형 로그(참고용) | `/_progress.log` = 7/17 윈도우의 "유튜브 자막 수집" 기록. 완료된 과거 단계다 |
| 인계 명세(구형) | `황세웅/saju_진행현황_이어하기.txt` — **7/19 저녁 기준이라 낡아간다. 이 문서가 최신** |

## 2. 폴더의 상태 — 259개의 정체 (전수 실측 완료, 재조사 불필요)

| 구분 | 개수 | 판정 방법 |
|---|---|---|
| Whisper 완료 (표준 파일명) | 107 | 파일 머리말에 `⚙️ 로컬 Whisper ASR` |
| Whisper 완료 (비표준 파일명 6개) | 6 | 유튜브 자막이 없어서 7/17에 먼저 ASR한 것들 |
| **지금 교체 중 (공개)** | **92** | 머리말이 `YouTube 자동생성`이고 공개 영상 |
| 홍염 멤버십 (불가) | 53 | **쿠키로 실측: 그 티어 미가입 → 오디오 다운로드 자체가 거부됨.** 유튜브 자막으로 유지 |
| 스텁 | 1 | 멤버십 선공개 뮤직비디오(9w8tu8NuVV4), 본문 56자. 전사 대상 아님 |

- 파일명 규칙: `YYYYMMDD_제목_영상ID.md` · 헤더 6줄(업로드/길이/조회수/URL/채널/자막) + 미검수 경고문 + `---` + 본문(세그먼트 줄바꿈)
- **핵심 설계: 완료 판정은 "파일이 있냐"가 아니라 "머리말에 Whisper라고 적혔냐"다.** 259개가 전부 이미 존재하기 때문. 이걸 모르고 §B 원본 엔진을 그대로 돌리면 완료본 113개를 재전사하고 다른 이름의 중복 파일을 만들어 **폴더를 망가뜨린다** (실제로 그럴 뻔했다).

## 3. 확정 실측치 (재측정 금지 — 이미 다 쟀다)

| 항목 | 값 | 어떻게 쟀나 |
|---|---|---|
| 맥 M5 · 1스트림 | 5.52배속 | 실제 대상 영상(14.1분) 전사 |
| **맥 M5 · 2스트림 (채택)** | **7.85배속** (전사만) · 다운로드 포함 실효 5~6 | 848초×2를 216초에 |
| 맥 M5 · 3스트림 | **실패** — 스왑 9.4GB 폭발, 1스트림보다 느림 | 10분 초과 미완주 |
| 윈도우 GTX 1080 | 3.36배속 | 7/18 실제 처리 107개 타임스탬프 역산 |
| large-v3-turbo | 30배속이지만 **기각** | 고유명사 파괴: 조국혁신당→초국혁신당, 범야권→법략권 |

**교훈 두 개 (다음에도 적용):**
1. 문서에 적힌 속도(복제팩의 "1080=4.8배속")는 실적(3.36)과 다르다. **비교는 반드시 실측으로.**
2. **짧은 영상(3분 미만)으로 속도를 재면 안 된다.** 모델 로딩 고정비 때문에 절반 값이 나온다(2.88 vs 5.52). 10분 이상 실제 대상 영상으로 잴 것.

**2스트림의 원리(요약):** 전사는 CPU 단계(오디오 준비)와 GPU 단계가 번갈아 돈다. 1개만 돌리면 GPU가 틈틈이 놀아서, 서로 다른 영상 2개를 동시에 돌리면 빈틈이 메워진다(세탁기 2대). 3개는 모델 3벌(약 20GB)이 24GB 통합메모리를 넘겨 디스크로 밀리며 전체가 느려진다. **윈도우 1080은 GPU 전용램이 8GB뿐이라 2벌(9~10GB)이 물리적으로 안 들어가 이 방법 자체가 불가.**

## 4. 실행 명령 (복사해서 쓰면 됨)

```bash
# 전체 실행 (2스트림 + 맥 잠들지 않게 + 끝나면 재시도 대기열 자동 처리)
~/.claude/skills/whisper/run_job.sh

# 특정 영상만 재전사 (실패 재시도 — 보드의 [재시도] 버튼과 동일)
~/.claude/skills/whisper/run_job.sh --only <영상ID[,영상ID...]>

# 검수용 미리보기 (실폴더 안 건드림)
~/.claude/skills/whisper/run_job.sh --limit 3 --preview /tmp/preview

# 진행 보드 서버 (죽었으면 이걸로 재기동)
nohup ~/.claude/skills/whisper/.venv/bin/python ~/.claude/skills/whisper/board_server.py >/dev/null 2>&1 &
# → 브라우저에서 http://localhost:8765
```

- **중단·재개:** 그냥 `run_job.sh`를 다시 돌리면 된다. 머리말 판정 덕에 끝난 건 건너뛰고 안 된 것만 한다. 실패분도 같은 원리로 자동 재처리된다.
- **반드시 `run_job.sh`(또는 run_saju.sh)로 실행할 것.** venv의 python을 직접 부르면 PATH에 번들 ffmpeg가 없어서 전사가 실패한다.
- 브라우저 보드의 [재시도]: 전사가 쉬는 중이면 즉시 실행, 작업 중이면 대기열(`_retry_queue.txt`)에 예약 → 전체 종료 후 자동 처리.

## 5. 함정 목록 (전부 실제로 밟았거나 실측로 확인한 것)

1. **뚜껑 닫으면 죽는다.** caffeinate로도 뚜껑 닫힘 강제 잠은 못 막는다. 전원 연결 + 뚜껑 열어두기.
2. **밤샘 중 무거운 앱(프리미어 등) 금지.** 모델 2벌이 메모리를 꽉 채워 스왑이 12~14GB다. 여유 없음.
3. **오디오 임시파일은 원드라이브 밖**(`~/.cache/whisper_saju_tmp`)에 두게 해놨다. 작업 폴더 안에 두면 수십 GB가 회사 클라우드로 동기화된다.
4. **🚨 `youtube_cookies.txt`(황세웅 폴더) — 평의회 판정 critical.** 이 파일은 유튜브만이 아니라 **구글 계정 전체를 여는 마스터 세션 쿠키**(SID·SAPISID·__Secure-PSID 계열)다. 회사 OneDrive에 두면 회사 관리자·동기화 대상 전원에게 노출 경로가 생긴다. 아래 한 줄로 로컬 전용 위치로 옮길 것(범용 엔진이 원래 그 위치도 자동 탐색한다):
   ```bash
   mv "$HOME/Library/CloudStorage/OneDrive-GS칼텍스예울마루/황세웅/youtube_cookies.txt" "$HOME/.whisper_cookies.txt"
   ```
   (윈도우에서 나중에 멤버십을 다시 시도할 땐 어차피 쿠키를 새로 뽑아야 하므로 옮겨도 잃는 것 없음)
   **이동 후 후속 2단계(평의회 B-critical):** ①OneDrive 웹에서 이 파일의 **버전 기록·휴지통까지 영구삭제** (로컬 이동만으론 클라우드 사본이 남는다) ②이미 회사 클라우드에 올라갔던 만큼 노출로 간주 — 구글 계정 [모든 기기에서 로그아웃]으로 세션 무효화 후 필요 시 쿠키 재추출(로컬 전용 경로로).
5. **맥판은 윈도우판과 디코딩 방식이 다르다.** mlx-whisper는 빔서치(beam 5)·VAD 미지원 → greedy로 돈다. 대신 fp16(윈도우는 int8 압축)이라 상쇄. 파일럿 품질 검증은 통과했으나 무음·음악 구간에서 같은 문장 반복(환각) 가능성이 이론상 있다 → §6의 품질 검사로 잡는다.
6. **홍염 53개에 시간 쓰지 마라.** 계정 권한 문제라 어떤 기계·설정으로도 불가. 그 티어를 결제한 계정의 쿠키가 생기면 진행현황 문서의 ID 목록으로 같은 방식 처리하면 된다.
7. 채널 신규 업로드는 7/19 19시 기준 0개 확인. 나중에 늘었으면 엔진의 대상 목록(진행현황 파일 기반)에 없으므로 별도 추가 필요.

## 6. ★ 완료 후 해야 할 일 (다음 세션의 첫 임무) ★

1. **결과 집계:** `_whisper_mac.log` 끝의 `=== DONE ok=N fail=M ===` 두 줄(스트림당 1줄) 확인.
2. **실패 재처리:** 실패가 있으면 `run_job.sh` 한 번 더 (실패분만 자동으로 다시 돈다).
3. **품질 자동 검사(환각 탐지) — 평의회 확정 스펙 + 실측 근거:** ⚠️ 이건 이론이 아니다 — 평의회가 기존 Whisper 완료본 123개를 실제 스캔해서 **9개(7%)에서 디코드 루프를 적발**했다 (윤석열 파일 'juris' 26연속, 결혼 브이로그 '일주' 34연속 — 윈도우 제작분·맥 제작분 모두에서 발생, fail 카운터는 0이라 조용히 저장됨). 따라서 **오늘 밤 92개만이 아니라 Whisper 헤더가 붙은 전체(~205개)를 스캔**할 것. 3지표 → 의심 상위 10~15개만 육안 확인:
   - ① **연속 반복줄** ≥ 4회 (`uniq -c`로 최대 연속 중복 세기) — 무음·음악 구간 디코드 루프의 전형 증상
   - ② **분당 글자수 이상**: 본문글자수 ÷ 영상분 이  100 미만(유실 의심) 또는 900 초과(루프 의심)
   - ③ **빈 본문 / 극단 압축비**(같은 n-gram 비중 과다)
   - 의심본은 `run_job.sh --only <ID,...>`로 재전사. **엔진에 루프 근본 차단 옵션(condition_on_previous_text=False)이 7/19 20:00 이후 반영돼 있어, 재전사 패스는 자동으로 이 옵션을 쓴다** (오늘 밤 본 실행분은 옵션 반영 전 코드로 돌았음 — 프로세스가 이미 떠 있던 상태라). 재전사해도 같으면 원본 오디오 특성이니 사용자에게 보고.
   - 주의: 현 엔진은 타임스탬프를 버리므로 "타임라인 공백" 검출은 불가 — 위 3지표가 유일한 자동 게이트다.
3-1. **완료 정합성:** `grep -c "=== DONE" _whisper_mac.log` 가 **정확히 2**인지 확인 (스트림당 1줄). 2 미만 = 한 스트림 중도 사망 → `run_job.sh` 1회 재실행이면 고아분만 다시 돈다.
4. **부속물 재생성:** `index.md` · `index_categorized.md` · `dashboard.html` · `categories.tsv`가 각 파일의 자막 출처를 "YouTube 자동생성"으로 표기하고 있다 → 92개가 Whisper로 바뀌었으니 낡았다. 헤더 6줄만 다시 읽어 표기를 갱신하면 된다(본문 재분석 불필요).
5. **진행현황 문서 갱신:** `saju_진행현황_이어하기.txt`를 새 상태(공개 완료 205/259, 남은 것 = 홍염 53 + 스텁 1)로 고치거나, 이 인계서 v2로 대체.
6. **인코딩 규격 마무리:** `~/.claude/skills/whisper/normalize_text.py` 실행 — 폴더 정규 규격은 **UTF-8+BOM+CRLF**(윈도우 구형 메모장 포함 모든 OS에서 한글 안 깨짐). 7/19 19:50에 기존 183개는 변환 완료, 오늘 밤 생성분(당시 대기 78개)이 남아 있으니 완료 후 한 번 더 돌리면 261개 전부 통일된다(멱등이라 몇 번 돌려도 안전). ※이후 이 폴더 파일을 코드로 읽을 땐 `encoding="utf-8-sig"` 사용(BOM 자동 처리), 파일명은 `unicodedata.normalize("NFC", ...)` — 엔진에는 둘 다 반영돼 있음.
7. **사용자 보고:** 성공/실패 개수, 총 소요, 품질 검사 결과 3줄 요약 + 백업 위치 안내.

## 7. 다음 채널부터 (반복 워크플로 설계 방향)

- 이번 자산 중 **재사용 가능:** venv 환경 통째, run_job.sh(2스트림+대기열), board_server.py+config(변수만 갈아끼우면 됨), LPT 균등분배·머리말 판정·원자적 쓰기 로직, 이 인계서의 실측치.
- **채널 고유라 갈아야 하는 것:** 대상 목록 소스(이번엔 진행현황 txt — 새 채널은 채널 URL에서 직접 열거), 파일명·헤더 양식(이번 건 기존 폴더 계승 — 새 채널은 자유), board_config.json 값들.
- 새 채널 절차: ①`yt-dlp --flat-playlist`로 영상 열거 → ②(선택) 유튜브 자막 선수집으로 즉시 쓸 수 있는 초벌 확보 → ③Whisper 교체를 2스트림으로 → ④보드 config만 새로 써서 관제. 사용자가 "웹앱에 붙일 생각"이라 했으므로 보드는 config 기반·PC 전용(127.0.0.1)을 유지한 채 확장하면 된다.
- **평의회가 확정한 다음 채널 개선 목록 (효과 순):**
  1. **프리페치**: 다음 영상 오디오를 전사 중에 미리 다운로드 (현 구조 최대 낭비 — 실효 5~6배속을 7.85에 근접시킴)
  2. **저용량 오디오**: `-f "bestaudio[abr<=70]/bestaudio/best"` — Whisper는 16kHz로 낮춰 듣기 때문에 고음질 다운로드가 낭비. 다운로드량 2~3배 절감 (코드 1줄)
  3. **채널 용어집 주입**: `initial_prompt`에 전문용어·출연진 이름을 넣으면 고유명사 정확도 상승 (사주 예: 도화살·홍염살·화개살·백호대살·십성·비견·겁재·대운·일주)
  4. **유튜브 예절 옵션**: `--sleep-requests 1.5` 추가 — 수백 개 연속 다운로드 시 봇 판정 예방. 429/봇확인 감지 시 분 단위 대기로 전환
  5. **실행 전 yt-dlp 갱신**: `pip install -U yt-dlp` 프리플라이트 (유튜브가 자주 바뀜)
  6. **샤드 매니페스트 파일**: 두 스트림이 각자 목록을 계산하는 대신 파일로 고정 (재개를 단일 샤드로 수동 재시작하면 목록이 어긋날 수 있는 이론적 구멍 봉쇄. 현행 규칙 = 재개는 반드시 전체 run_job.sh 재실행)
  7. **빈 전사 = 영구실패 마커**: 매 재실행마다 다운로드+전사를 반복하지 않게 스킵 목록 도입
  8. **산출물 로컬 우선**: 작업은 로컬 폴더에서 하고 완료 시에만 OneDrive로 반입 (동기화 부하·회사 테넌트 노출 최소화)
  9. **기각된 아이디어**: turbo 초벌 + large-v3 재검수 하이브리드 — large-v3가 어차피 전체를 다시 처리하므로 순이득 0 (평의회 계산으로 공식 기각)

## 8. 검증 이력 (신뢰 근거)

- 형식 검증: 파일럿 산출물 vs 기존 완료본 — 파일명 완전 일치·헤더 구조 일치·세그먼트 줄바꿈 일치 확인 (7/19 18:22).
- **오퍼스 10인 평의회 완료 (7/19 19:36~19:50, 10관점 32건 회수 → 본 문서에 반영됨).**
- 이 문서와 별개로 Claude 세션 메모리(`~/.claude/projects/-Users-hwang/memory/saju-whisper-mac-2stream.md`)에도 요약본이 있다(Claude 전용, 이 문서가 정본).

## 9. 평의회 결과 요약 (10관점 32건 → 3분류)

**A. 즉시 반영됨 (이 세션에서 처리 완료):**
- 보드가 스트림 1개만 죽으면 `⚠️ 스트림 일부 중단 1/2`로 경고하게 됨 (침묵 감속 방지)
- 보드 속도 표기를 "실효 속도(다운로드 포함)"로 명확화 (헤드라인 6배 vs 표 3.5배가 모순으로 보이던 문제)
- run_job.sh가 전체 실행 시작 시 로그를 초기화 (재실행 시 보드 완료예정 오염 방지)
- run_saju.sh의 `--single` 크래시 버그 → run_job.sh에서는 해당 모드 제거로 해소
- yt-dlp 2026.07.04 = 최신 stable 확인 (조치 불요)

**B. 완료 후 (§6에 통합됨):** 품질 3지표 스캔 · 부속물 4종 재생성(§6-4: index가 자막 출처를 파일명 접미사로 판정하므로 반드시 **파일 내용의 Whisper 마커**로 판정을 바꿔 재생성할 것) · 진행현황 재발행 · 빈 전사 영구실패 마커

**C. 다음 채널 (§7에 통합됨):** 프리페치 · 저용량 오디오 · 용어집 · sleep-requests · 매니페스트 · 로컬 우선 · turbo 하이브리드 기각

**기각/무해 판정:** OneDrive 일시정지 불필요(텍스트 소용량) · 발열 스로틀링 징후 없음(계속 관찰) · 2스트림 샤드 경합 없음(시작 동시 관측이라 동일 분배 보장, 단 재개는 전체 재실행으로만) · turbo 하이브리드 공식 기각(순이득 0) · "멤버십 콘텐츠가 회사 클라우드에 동기화" 주장은 grep 실측 0건으로 부분 기각(동기화되는 건 공개 대본·메타·자격증명)

**평의회 최종 평결(원문 요지):** "현 실행은 건강하며 그대로 끝까지 둬도 된다. 33건 중 이번 잡에 실시간 위협은 둘뿐 — ①회사 OneDrive의 구글 마스터 쿠키(엔진은 쿠키를 안 쓰므로 지금 옮겨도 작업 안 죽음) ②뚜껑 닫힘(caffeinate가 못 막음). 품질 리스크는 실재하나(7% 디코드 루프 실측) 교체전 백업이 있어 롤백이 안전하고, 3지표 기계 스캔으로 상위 10~15개만 걸러내면 된다."

**차기 과제 추가분(C):** 보드 연도 버그(로그에 연도가 없어 '2026-' 하드코딩 — 해 넘기면 ETA 붕괴) · 타임스탬프 사이드카(.srt) 보존으로 세그먼트 유실 검출 · P1/P2 종료코드 개별 검사 · 러너의 FAIL stderr 로그 보존(현재 삼켜짐)
