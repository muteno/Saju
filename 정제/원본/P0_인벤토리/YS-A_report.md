# YS-A 배치 리포트 — 이달의 운세(일간별) 글모음.txt 1~10335줄

- **배치ID** YS-A · **ID prefix** `HM-MONTH` · **author** 현묘 · **license** 전건 `none`(저작권 문구 없음)
- **규격** P0 스펙 v1 + v1.1(운세확장) 부록 적용. 충돌 시 부록 우선.
- **마지막으로 처리한 줄번호: 10335** (배정 범위 전량 완료, 미처리 없음)

## 1. 통계

| 항목 | 값 |
|---|---|
| posts | **102** (P000 머리말 1 + 글 **101**편) |
| paras | **1,243** (글당 평균 12.3개, 문단 길이 평균 8.3줄·중앙값 7줄) |
| 커버 줄 | **1 ~ 10335 · 미커버 0줄** (posts/paras 모두 빈틈·중복 없이 타일링) |
| quote | **1,081개 전수 축자 일치**(불일치 0) · 120자 초과 0 · 말줄임표 추가 0 |
| gist | 최장 31자 (40자 초과 0) |
| key_natal / key_luck | **101/101 전건 추출**(결측 0, P000 제외) |

**kind 분포** — 운세 722 · 이론 204 · 개운 125 · 잡동 103 · 공지 86 · 화법 3
**subj 분포** — S10 950 · S08 360 · S01 306 · S05 193 · S09 125 · S03 103 · S02 101 · S11 3
**slot 분포** — 총평 266 · 조언 125 · 관계 120 · 재물 120 · 직업 120 · 간지의미 103 · 마무리 96
**flags** — 결정론 265 · 별점 66 · 수치 13 · 게이트 4

**격자 완비 확인**: 10일간 × 10개월(을사년 무인월~정해월) + 병화일간 무자월 1편 = **101편**, 빠진 칸 없음.
key_luck별 각 10편(무자월만 1편), key_natal별 각 10편(병화일간만 11편).

**검수 결과** — 프로젝트 검수기 `p0_verify.py YS-A` 5게이트 전부 통과:
G1 ID중복 0 / G2 미커버 구간 `[[10336, 20731]]`(= YS-B 배정 범위와 정확히 일치) / G3 타일링 불량 0 / G4 quote 1081/1081 / G5 kind·subj 불량 0.

## 2. 🚨 다음 배치가 반드시 알아야 할 것 — 줄번호 체계 불일치

**이 파일 1023줄 안에 U+2028(LINE SEPARATOR)이 1개 박혀 있다.** 그 결과 도구마다 줄 수가 다르다.

| 도구 | 줄 수 | 1023줄 취급 |
|---|---|---|
| **Read · ripgrep(Grep) · PowerShell Get-Content** | 20,730 | 한 줄로 봄 |
| **Python `splitlines()` = 검수기 `p0_verify.py`** | 20,731 | 두 줄로 쪼갬 |

- 1022줄까지는 두 체계가 같고, **1023줄부터 Read 기준 N = Python 기준 N+1**로 어긋난다.
- 분담표(YS-A `1~10335` / YS-B `10336~20731`, 총 20731)는 **Python 기준**이다. Python 기준으로 10335줄이 병화일간 글의 마지막 줄, 10336줄이 다음 글 제목 줄이라 경계가 정확히 맞아떨어진다.
- 따라서 **본 배치 산출물의 줄번호는 Python(검수기) 기준으로 맞췄다.** Read 기준으로 보면 1024줄 이후가 1씩 작다.
- **YS-B 담당자에게**: Read 도구 줄번호를 그대로 쓰면 1023줄 이후 전 구간이 -1로 밀려 G2/G3에서 통째로 어긋난다. 파일을 `splitlines()`(또는 U+2028 포함 분리)로 읽어라.
- 다른 파일에도 같은 문자가 있을 수 있으니 라운드3 잔여 배치는 착수 전 1회 점검 권장(`txt.count(" ")`).

## 3. 특이 발견

1. **2025년 8월(P061)에 글 포맷이 통째로 바뀐다.** 2~7월(60편)은 `신강/신약 × 재물·직장·연애·개운` 11섹션 구조, 8~12월(41편)은 `목·화·토·금·수 과다 유형` 7섹션 구조다. 같은 파일 안에 **원국 분류축이 두 개**(강약 축 480문단 / 오행과다 축 205문단) 존재 → 앱 월운 카드도 두 축을 모두 받아야 하고, P2 역매핑 때 축을 섞으면 안 된다.
2. **십신 배정 문단은 66개뿐**(전용 섹션 61 + 인트로 5). 오행과다형으로 바뀐 뒤 `(일간 × 월간지 → 십신)` 명시 서술이 사실상 사라졌다. 결정론 격자로 쓸 수 있는 건 **2~7월 60편 + P077**이며, `flags:별점`이 붙은 66개가 그 전량이다.
3. **이 글들은 현묘 본인이 쓰지 않았다.** 크레딧을 실측하니 **파이브시즌스(5SEASONS) 상담사 12명 분업 집필**(채운·이화·산호·삼소희·소은담·주홍·가온·단아·늦봄·비밀의 숲·리보). `sub_author`에 전부 담았다. → **교차 확증 독립성 재계산 필요**: 저자 클러스터 "현묘"로 묶어 계상하면 안 되고, 일간별로 필자가 고정(예: 갑목=채운, 기토=소은담)이라 **같은 일간 글끼리는 사실상 1인 반복 서술**이다.
4. `sub_author` 미검출 5건 = P000 + **을목일간 4편(8~11월)**. 원문 크레딧이 "파이브시즌스 상담사님과 함께"로 개인명을 안 밝힌다(추출 실패가 아니라 원문에 없음).
5. **표기 흔들림**: 같은 사람이 `비밀의 숲`(6편) / `비밀의숲`(5편) 두 표기. P1 정규화 대상.
6. **`결정론` 265건의 정체**: ①인트로의 절기 기간("을사년 무인월은 2월 3일부터 3월 4일까지이며 절기로 입춘부터 경칩 이전까지") — 만세력 L1으로 기계 검증 가능한 월 경계표가 **101개** ②십신 배정문 ③천간 속성표(오행·음양·계절). L1 엔진 회귀 테스트 재료로 바로 쓸 수 있다.
7. **원문 목차의 오탈자 3종** — P055 목차는 "십신으로 보는 **무토월** 무토일간"(본문은 계미월), P077은 "**2** 십신으로…"(마침표 누락), P018·P055는 목차 '나가며'인데 본문은 '총론'. **목차-본문 문자열 대조만으로 소제목을 잡으면 섹션이 통째로 빠진다.** 본 배치는 목차 대조 + 표준어구 사전 + 번호 연속성(1,2,3…) 3중으로 잡아 101편 전부 복구했다.
8. **개운법 소목록이 본문에 박혀 있다**(P088 이후 일부): 오행 섹션 안에 "이달의 개운법!" + 번호 항목 3개가 3~4줄로 들어간다. 3줄 미만 파편 규칙에 따라 부모 운세 문단에 포함시켰다. P2에서 `조언` 슬롯을 캘 때 **`slot:총평` 문단 내부도 훑어야** 놓치지 않는다.
9. **원문 절단 의심 1건** — P078(9월 경금일간)은 마지막 줄이 "좋은 일은 하는 방법은 다음과 같습니다."로 끝나고 정작 목록이 없다. 스크랩 누락으로 보인다.
10. **1~2줄 문단 78개**는 대부분 글 끝 크레딧·상담 유도(`kind:공지` 86개)다. "3줄 미만은 이웃에 붙인다"(§3-3)보다 "성질이 다른 블록은 떼어 각자 kind를 준다"(§3-4)를 우선 적용했다 — 광고성 블록을 운세 본문에서 분리해야 P1이 걸러낼 수 있기 때문.

## 4. 작업 방식 메모 (재현·감사용)

- 이 파일은 (일간 × 월운) **격자**이고 글 구조가 규칙적이라, 전 줄을 눈으로 읽는 대신 **구조 스캐너 + 결정론 생성기**로 처리했다.
  - 소제목 탐지 = 글별 목차 집합 대조 ∪ 표준어구 사전, + 번호 연속성 검사(본문에 박힌 번호형 개운법 항목이 소제목으로 오인되는 것을 차단).
  - **quote는 모델이 옮겨 적지 않는다.** 스크립트가 원문 줄 문자열을 그대로 복사하고, 120자를 넘으면 **문장 종결부호 위치에서만** 자른다(가운데 삭제·말줄임표 추가·따옴표 치환 없음). 따옴표 짝이 깨지는 절단은 후보에서 제외. → CJK 호환한자 정규화 사고(부록 §1-1) 구조적으로 불가.
  - quote 선정 우선순위: 섹션별 선호 패턴(십신 섹션=십성 용어, 성향 섹션=물상/특징, 인트로=절기 기간·간지 구성) → 완결 문장 → 수사의문문·"살펴보겠습니다"류 전환문은 후순위.
- 저장은 15편 단위 7파트, **파트 경계 연속성**(다음 파트 첫 글 시작 == 직전 파트 마지막 글 끝+1) 자동 검사 통과. 글 유실 0.
- 산출물: `YS-A_posts_1~7.jsonl`, `YS-A_paras_1~7.jsonl`, `YS-A_report.md`.

## 5. 남긴 판단 (메인 세션 확인 요청)

- `개운`(125) = "종합 정리 및 개운법" 섹션. 요약+처방 혼합이지만 처방 비중이 커 `개운`으로 잡고 `slot:조언`을 부여했다(부록 슬롯 정의 "조언 = 개운·처방·주의"에 근거).
- 십신 섹션은 십성 정의(이론)와 이달 예보(운세)가 한 문단에 붙어 있다. 부록 §2-3 "그 달의 예보 본문 = 운세"를 따라 `운세`로 잡고, 이론 채굴 신호는 `flags:별점`으로 남겼다.
- `별점`은 **십신 배정 문단 66개에만** 부여했다(라운드1의 배치 간 별점 편차 1~207 문제를 피하려고 기준을 하나로 고정). 배치 내 상대 지표가 아니라 "결정론 격자" 표시로 읽어달라.
