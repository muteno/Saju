# 임시 인수인계 — 읽고 삭제

작성: 2026-09-22. 대상: `muteno/Saju`의 다음 작업 세션.

**사용자 지시:** “다음 세션에 시킬까? 인수인계서 임시 로 읽고 삭제하라고 남겨줘”

이 문서를 읽고 작업을 인계한 즉시 **`HANDOFF_TEMP.md`를 삭제하고 `AGENTS.md`의 `TEMP_HANDOFF_START`~`TEMP_HANDOFF_END` 안내 블록도 제거**한다. 다음 작업 커밋에 두 삭제를 포함한다. 사용자 원문·아젠다·상시 현황 문서는 삭제하지 않는다. 임시 문서를 다른 영구 인수인계 파일로 복제하지 않는다.

## 1. 바로 이어서 할 일과 중단 지점

사용자는 Figma 자료 연결 뒤 “적용 고고”, “ㄱㄱ”로 **실제 앱의 일주·기본 성향 풀이에 적용**하도록 요청했다. 자료·엔진·화면·상담 경로를 조사한 뒤 사용자 요청으로 다음 세션에 넘겼다.

- **새 기본 풀이 엔진·앱 코드는 아직 구현하지 않았다.** 이번 적용 작업의 코드·데이터 생성 및 수정은 0개다. 아래 설계는 구현 제안이지 완성된 기능이 아니다.
- `scripts/build_basic_reading_data.py`, `dosa-app/kb/basic_reading.json`은 **예정 파일이며 아직 없다**. 선별 완료된 성향 문장 목록도 없다.
- 다음 세션은 문서 추가에서 멈추지 말고 출처가 붙은 기본 풀이를 엔진→앱→상담에 연결하고 검증·PR·병합까지 진행한다. 최신 사용자 지시가 바뀌면 그 지시를 우선한다.

## 2. 사용자 의도·수학·뉴런식 지식망의 영구 기록

`AGENTS.md`의 기존 필독 순서를 따른다. 다음 내용은 **이미 깃에 보존되어 있으며 이 임시 문서를 삭제해도 남는다**.

| 정본 | 보존 내용 |
|---|---|
| `docs/knowledge-model/USER_INTENT_LOG.md` | 뉴런식·옵시디언 구상, 실제 노트 앱이 아니라 연결망 형태라는 정정, 키워드 조합별 확률, 0~1의 연속성, 대운·추가 변수·축적 자료, 예시의 모순 가능성까지 사용자 원문 그대로 |
| `docs/knowledge-model/PROJECT_AGENDA.md` | 기본 개념→조건부 관계망→학습·평가·LLM 설명의 장기 목표. 특정 단어쌍의 고정 가중치로 축소하지 않는 원칙 |
| `docs/knowledge-model/MATHEMATICAL_DESIGN.md` | 문맥·다중 상호작용 로지스틱, 요인 그래프, PSL/HL-MRF, GATv2 후보와 수학적 의미·한계·비교 순서 |
| `docs/knowledge-model/학습과평가설계.md` | 자료·저자 중복, 관측 결과·라벨, 학습/검증 분리와 평가·보정 설계 |
| `docs/knowledge-model/conditional_model.py` | 연속 입력·다중 조합을 받는 첫 실행 시제품. 기본은 미학습이며 합성 설명 모드와 구별 |
| `docs/knowledge-model/CURRENT_STATE.md`·`BASIC_READING_PLAN.md` | 실제 완료 범위, 미완료 사항, 먼저 사용 가능한 일주·기본 성향을 만드는 계획 |

사용자는 마지막으로 수학적 알고리즘·뉴런·옵시디언 내용의 보존 여부를 재확인하고, 빠진 내용이 있으면 새로 깃에 넣지 말고 별도로 달라고 했다. 위 내용은 기존 PR #172에 이미 포함되어 있다. 이번에는 새 모델 결정을 추가하지 않는다. 실제 누락을 나중에 발견하면 해당 부분은 사용자에게 별도로 제시한다.

핵심 방향:

- Figma의 기초 개념 체계를 기준으로 하고 블로그에서 정의·조건·예외를 대조한다. 기존 출처를 소급 교체하지 않는다.
- 목표는 주변 키워드·여러 변수·대운·시점에 따라 관계가 달라지는 지식망이다. 옵시디언은 형태 비유다.
- 단일 키워드로 성격을 단정하지 않는다. 사용자 예시는 정답·규칙·학습 라벨로 쓰지 않는다.
- 계산 사실, 문헌의 경향 설명, 미확인 조건, 검증된 예측 확률을 구별한다. 근거 없는 숫자를 붙이지 않고 미학습 확률은 `null`로 둔다.
- 첫 목표는 **사용 가능한 일주·기본 성향 풀이**다. 자료량이나 코드 검사 통과를 알고리즘·예측 정확도 완성으로 보고하지 않는다.
- 대운·세운 조건이 아직 기본 성향 계산에 반영되지 않았다면 그 범위를 명시한다. 장기 목표에서 시간 문맥을 빼라는 뜻은 아니다.

## 3. 이미 완료된 저장소 상태

작성 직전 확인한 main: `f4072874859db940757fa34c2aaa61e4dea11aa1`.

| 병합 | 완료 범위 |
|---|---|
| PR #171 · `15ef883` | 기존 대화형 앱·프로필·상담 요청 흐름 정리 |
| PR #172 · `f6808c6` | 사용자 의도·아젠다·기본 개념·수학 설계·연구 시제품 보존 |
| PR #173 · `f407287` | 현재 Figma 기초·심화 자료 직접 읽기와 개념·도식 참조 연결 |

Figma 보드: <https://www.figma.com/board/SEEnsqUToHwgVY5QZnYLeU>. 기초·심화에서 **18개 개념·50개 노드 인용, 3,619노드·154연결선·10표·943셀 구조**를 확인했다. 앱 적용을 위해 보드를 찾는 작업부터 반복할 필요는 없다.

- `docs/knowledge-model/data/basic_concepts.json`: 블로그 근거를 가진 18개 개념 정의.
- `docs/knowledge-model/data/figma_live_bindings.json`: 보드 노드 ID·인용·확인 수준. 정의 3, 설명 13, 제목 2개. 제목만 확인한 음양·인성도 블로그 정의는 있다.
- `docs/knowledge-model/data/figma_live_structure.json`: 도식 구조. 연결선을 성향·인과·확률 규칙으로 자동 승격하지 않는다.
- `docs/knowledge-model/FIGMA_LIVE_REVIEW.md`: 조회·검증 범위와 한계.
- `docs/knowledge-model/knowledge_graph.json`: 58노드·132개 의미 관계·11개 다중 입력 관계. 보드 연결선과는 별개다.

원본 ZIP과 Figma 전체 XML의 일시 작업 경로는 다음 세션에 사라질 수 있다. 저장소의 검수된 발췌·메타데이터에서 시작하고 실제 원문이 필요할 때만 확보한다. 개인 기록 영역이나 원문 전체를 새 공개 파일로 복제하지 않는다.

## 4. 조사한 적용 경로와 문제

| 위치 | 확인 사항 / 다음 변경 |
|---|---|
| `dosa-app/engine/src/report.js` | `buildReport(chart,keyset,kb)`가 실제 근거 리포트 정본. 기본 풀이 섹션과 시간 미상 옵션을 추가하는 방안이 적합하다. |
| 같은 파일의 `sipsinDistribution`·`elementDistribution` | 시주를 항상 포함한다. 미상 시 임시 시간의 십신·오행이 새 기본 풀이에 들어가지 않도록 계산 단계부터 제외한다. |
| `app/src/engine/index.js`·`index.d.ts` | `buildReading(input, unseYearName)` 어댑터와 타입 선언. **index.ts가 아니다.** 기존 호출 호환성을 유지하며 미상 옵션을 연결한다. |
| `app/src/data/saju.ts`·`useReport.ts` | 두 곳 모두 `buildReading`을 호출하므로 동일한 기본 풀이·미상 조건을 받아야 한다. |
| `saju.ts`의 일주 카드·대화 | 기존 성격 요약에 첫 출처를 대표 부착한다. 새 문장마다 실제 해당 출처를 연결한다. “일주 중심의 풀이는 그대로 정확하니” 문구도 수정한다. |
| `app/src/data/analysisGroups.ts` | 새 카드 ID를 적절한 그룹에 등록한다. `CardBlock`을 문장별로 쓰면 기존 UI·출처 표시를 재사용할 수 있다. |
| `app/src/data/dosaTopics.ts` | 일주 문헌 핵심을 `tone:'calc'`로 취급하는 부분이 있다. 계산 사실과 문헌 후보를 구별한다. |
| `app/src/data/dosaClient.ts` → `functions/api/dosa.ts` | 전송은 주로 `text, grounds`만 남긴다. 문헌 성격·미상·시간 조건 미적용 정보를 상담 요청과 프롬프트까지 보존한다. |
| `scripts/build_kb.mjs` | 본문은 기본 8문단만 번들에 포함한다. 뒤쪽 인용을 앞 8문단에 억지 매핑하지 말고 검수된 경량 기본 풀이 데이터를 별도 포함한다. |
| `scripts/sync_engine.mjs` | 별도 엔진 파일을 신설하면 `FILES`에 추가한다. vendor 사본을 직접 편집하지 않는다. |

시간 미상은 현재 임시 정오를 사용한다. 시주를 가리는 것만으로 전체 불확실성이 해결되지 않으며, 날짜 경계·절입 근처의 일주·연월주도 확정적으로 설명하면 안 된다. 기존 강약 110점의 배점을 새 확률로 재해석하거나 이번 범위에서 임의 재비례하지 않는다.

## 5. 다음 세션에서 만들 데이터의 제안

기존 `dosa-app/kb/distilled/ilju/*.json`은 60일주·성격 요약 402문장·직접 인용 166개를 포함한다. 원문은 생성물 `dosa-app/kb/unit_bodies.json`의 `title`, `meta`, `paras`로 확인한다. **요약 402문장의 의미가 전부 검증된 상태는 아니다.**

직접 인용이나 연결된 원문 문단에서 일주마다 짧은 일반 성향 발췌 1~2개를 검수한다. 건강·성별·연애·사건이나 극단적 우열 단정을 일반 성향으로 자동 채택하지 않는다. 무리하게 60일주를 채우는 대신 근거 부족은 `insufficient_evidence`로 남긴다.

제안 계약: `ilju[일주]`에 `key`, `title`, `status`, `probability:null`, `claims[]`를 둔다. 각 claim은 `text`, `scope:'day_pillar_reference'`, `use:'reference_not_personal_fact'`, `probability:null`, 개별 `source`를 가진다. `source`에는 `unit`, `doc`, `title`, `paragraph_start/end`, `char_start/end`, `quote`, `quote_sha256`, `paragraph_sha256`, `unit_sha256`, `meta`, 확인된 `url`을 둔다.

문단은 원문 유닛 `paras` 기준 1부터, 문자 위치는 Unicode 문자열 기준 0부터·끝 제외다. 확인되지 않은 URL은 `null`로 둔다. 예정 빌더 `--check`는 유닛 존재, 인용 일치·위치·해시, 재생성 결과 일치를 검사한다. 스키마와 파일명은 구현 제안이므로 개선 가능하다.

Figma의 개념 정의·노드 참조는 성향 발췌와 구분해 연결한다. 화면은 계산된 일주·일간 등 기본 구조와 해당 개념 설명, 문헌 경향·출처, 아직 적용하지 않은 조건을 읽을 수 있게 한다. 숫자 확률을 보여주는 단계로 건너뛰지 않는다.

## 6. 구현·검증·병합 순서

1. 최신 main과 작업 상태를 확인하고, 이 문서를 읽고 인계한 뒤 임시 파일·안내 블록을 삭제한다.
2. 출처가 연결된 기본 풀이 데이터를 생성·검수하고, Figma 개념과 블로그 설명을 필요한 범위로 KB 빌더에 연결한다.
3. 엔진 정본에서 기본 풀이를 만들고 앱 어댑터·화면·상담에 같은 근거와 미상 상태를 전달한다.
4. 기존 카드·토큰·컴포넌트를 재사용한다. UI 변경 전후의 모바일 화면을 캡처한다.
5. 실제 리포트→주제→상담 요청 회귀를 검사한다. 제안 fixture: 1990-01-01 12:00 F의 십신 합계는 시간 기지 7자리, 시주 제외 5자리이며 오행은 6자리다. 2024-02-04 입춘 17:00/17:30, 야자시 경계도 확인한다. **이 새 동작의 테스트는 아직 작성·실행하지 않았다.**
6. 인용 원문 연결, 근거 부족, 시주 제외, 경계 불확실성, 확률 `null`, 상담에서 문헌 후보가 계산 사실로 바뀌지 않는지를 확인한다.
7. 필수 게이트 `npm run verify`를 통과시키고 현황·이력을 갱신한다. PR 변경 범위·원격 체크 확인 후 병합한다.

이전 세션에서 동작한 명령(환경 경로는 새 세션에서 확인):

```bash
SMOKE_CHROMIUM=/tmp/chromium npm run verify
python3 -m unittest discover -s docs/knowledge-model -p 'test_*.py'
```

PR #173 당시 verify 8개 게이트와 연구 테스트 37개가 통과했다. 기존 코드 계약 검사이며 앞으로 만들 기본 풀이의 검증 결과가 아니다. 최종 보고는 `CLAUDE.md`에 따라 결과 / 변경 / 병합 및 다음 작업의 3줄로 한다.

## 7. 같은 작업 공간을 재사용할 때

이전 로컬 HEAD는 원격 main보다 뒤에 있고, 이미 병합된 PR #171~173 파일들이 로컬 변경으로 표시될 수 있다. `git status`만 보고 미완료 코드로 오해하거나 전체 `git add`, 강제 reset으로 처리하지 않는다. 최신 main의 깨끗한 체크아웃 또는 변경 파일별 원격 대조를 사용한다. 이번 적용 작업은 조사까지만 했으며 신규 런타임 파일은 없다.

이 임시 문서 자체가 main에 들어간 뒤에는 위 기준 SHA보다 main이 앞서는 것이 정상이다. 최신 상태 위에 이어간다.
