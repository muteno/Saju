# STATUS — 세션 인수인계 (어느 세션·어느 모델이든 이 파일부터)

## 2026-09-22 최신 인계 — 검토 사례·라벨과 같은 자료의 모델 비교

- 사용자 원문 25에 따라 `docs/knowledge-model/case_review.py`, `compare_models.py`를 추가했다. 원국을 다시 계산해 라벨·근거·시점과 모델 입력을 연결하고, 동일 사례·특징·설정에서 개별 항/조합 항 모델을 비교한다. 기존 원국 어댑터는 표준 입력도 받으며 파일 입력은 유지한다.
- 계산 예제 6개와 제안 목표 1개·조합 항 6개를 검토 입력으로 동봉했다. 전부 빈 라벨이며 학습 적격 사례는 0개다. `case_review.py --compare`는 blocked와 모델 null을 반환한다. 미상·이견·합성·계산 예제·자동 조건값을 실제 전문가 라벨로 전환하지 않는다.
- 출처·인물·파생 자료·입력 근거 해시·복제 요청을 간접 연결까지 묶어 학습/평가 중복을 막는다. 명시된 입력 가용 시점과 라벨 검토일을 확인하고, 선택적인 forward 분할에서는 학습 기준일도 검사한다. 실제 자격·출처·과거 접근 가능성은 코드가 인증하지 않는다.
- Python 전체 93개(신규 17개), 원국·시간 Node 9개 통과. 평가 라벨 변경이 학습 계수를 바꾸지 않고, 이미 조합한 값을 기준 모델에 넣지 못하며 이름만 다른 의존 출처를 독립 집단으로 세지 않는지 확인했다. `SMOKE_CHROMIUM=/tmp/chromium npm run verify` 전체 8개 게이트 통과(exit 0): 기존 엔진 19건, 앱/API 13건, 증류 79파일·223인용, 시험은행 1건, 빌드·토큰·실제 브라우저 스모크 포함이며 스킵은 없다.
- 다음은 원문에서 입력·해석·예외가 확인되는 실제 사례의 독립 검토와 목표·척도 확정이다. 실제 학습·확률 보정·해석 정확도 검증은 미완료다. 상세 = `docs/knowledge-model/CASE_REVIEW.md`.

## 2026-09-22 최신 인계 — 원국·시간 관찰값과 검토 조건 연결

- 사용자 원문 24에 따라 이관 다음 단계를 진행했다. `docs/knowledge-model/chart_context.mjs` → `context_query.py`가 기존 L1 원국·세운·월운, 명시된 기간의 대운을 조건 판정에 연결한다. `data/context_reviews.json`의 8개 조건식은 기존 후보 8건·DOCX 4개의 앞뒤 문맥을 복원한 구현자 검토다.
- 표면과 지장간의 부재, 을목과 신유월/을유일 범위, 삼합 중심자, 인해 문맥의 천간 화 조건을 구별한다. 조건 충족만 판정하며 개인 성향·길흉·합화·강약을 추론하지 않는다. 조건식의 출처·후보·인용·해시를 CLI 실행 전에 대조한다.
- 시각 미상은 가능한 분 단위 원국의 공통 관찰값과 범위로 남긴다. 야자시·절기 경계·시간대 전환을 처리하며, 주어진 대운 기간의 끝 시각에는 다음 기간을 선택한다. 빈 기간은 미상이다. 대운 나이만으로 정확한 시작일을 만들지 않는다.
- 무가중 기호 비율은 측정값이며 확률·강약 점수가 아니다. 새 CLI에 학습 모델은 기본 제공하지 않는다. `--model` 사용 시 특징 정책·어댑터·계산 파일·검토 조건의 해시가 일치해야 한다. 미학습/합성 모델과 자동 학습 라벨 생성을 차단한다.
- Python 전체 76개(신규 14개), 새 Node 회귀 9개 통과. `SMOKE_CHROMIUM=/tmp/chromium npm run verify` 전체 8개 게이트 통과(exit 0): 엔진 19건, 증류 79파일·223인용, 시험은행 1건, 앱/API 13건, 빌드·토큰·실제 브라우저 스모크 포함이며 스킵은 없다. UI·기존 L1 정본·vendor·확률 계수는 변경하지 않았다.
- 다음은 남은 조건의 전문가 의미·예외 검수와 독립 사례·목표 정의, 강약·통근 측정 정책 및 같은 자료에서 조합 모델 비교다. 실제 해석 정확도·학습·확률 보정·앱 추론 연결은 미완료다. 상세 = `docs/knowledge-model/CONTEXT_BRIDGE.md`.

## 2026-09-22 최신 인계 — 기존 조건 자료를 현재 검토 경로로 연결

- 사용자 원문 22~23에 따라 기존 정제 지도와 현재 조건부 모델을 비교했다. 기존 자료는 재사용 가능하므로 원본을 보존한다. 고정 점수·동시출현·미확인 관계는 현재 모델의 확률 규칙으로 가져오지 않는다.
- `docs/knowledge-model/legacy_import.py`가 조건·분기 후보 767건을 재현한다. 455건은 보유 DOCX에서 구절 발견, 5건은 미발견, 307건은 이번 입력에 없는 원문 대기다. 기존 개념 51개에 대응한 후보 570건을 현재 CLI 조회에 연결했다. 이 중 구절까지 찾은 항목은 337건이다.
- 원문 위치·해시·부재 조건·두 분기·기존 문단 ID를 유지한다. 문단 ID 동일성과 의미 검수는 미확인이며, 모든 후보는 추론 비활성·확률 null이다. 기존의 7,295개 관계나 점수를 가져온 것이 아니다.
- Python 회귀 62개(신규 11개) 통과. `verify`에 이관 스냅샷 재생성 검사와 Python 회귀를 추가했다. 전체 8개 게이트 통과(exit 0): 엔진 19건, 증류 79파일·223인용, 시험은행 1건, 앱/API 13건, 빌드·토큰·실제 브라우저 스모크 포함. 스모크를 생략하지 않았다.
- 다음은 원문 연결·개념 대응이 있는 337건부터 조건의 의미·예외·맥락을 검수하고 원국/시간 특징 및 라벨을 설계하는 것. 독립 사례 학습, 확률 보정, 앱 추론 연결은 미완료다. 상세 = `docs/knowledge-model/LEGACY_MIGRATION.md`.

> 갱신: 2026-09-22 (알고리즘 계산·조건 조회 검증 및 최신 인계 반영). 설계 원칙은 `dosa-app/README.md`(절대 원칙 4 + L4 서술 표준).
> **이 문서는 구현 현황·인계 기록이다. 사용자 의도의 원문과 현재 아젠다는 `docs/knowledge-model/USER_INTENT_LOG.md`·`PROJECT_AGENDA.md`에 누적 보존한다. 작업할 때 함께 읽고 갱신할 것.**

## 2026-09-22 현재 인계 — 알고리즘 계산·조건 조회 오류 수정

- 현재 목표는 사용자 원문 20~21의 **사주 해석 알고리즘 정확도 향상**이다. 수학적 관계 모델·조합·대운과 시점·학습·평가를 포함한다. 조건부 로지스틱은 첫 비교 모델이며 요인 그래프·PSL·GATv2는 후보로 남는다.
- `conditional_model.py`: 미학습 계수의 확률 출력을 차단하고, 계산·학습 중 비정상 수치를 거절한다. 큰 오답의 평가 손실을 잘못 줄이던 확률 잘라내기를 없애고 로짓에서 안정적으로 계산한다.
- `knowledge_query.py`: 조회 근거와 직접 연결된 조건 후보·원문 이견·동반 인용을 함께 반환한다. `application_status=not_evaluated`이며 근거가 같다는 이유로 해당 원국의 적용 규칙으로 확정하지 않는다. 현재 연결 범위는 조건 3/5, 이슈 4/6이다.
- 검증: 지식·조건 모델 unittest **51개 통과**. `scripts/verify.sh` **8개 게이트 전체 통과(exit 0)**: 만세력 19건, 증류 79파일·인용 223건, 시험은행 1건, 앱/API 13건, 앱 빌드, 디자인 토큰, 실제 Edge 브라우저 스모크 포함. 브라우저 검사는 생략하지 않았다. Windows에서 동일 검사를 실행하도록 파일 URL·정적 서버 디렉터리 처리 3곳을 수정했다.
- 정확도 한계: 위 결과는 코드 오류·출처 연결·앱 회귀 검사다. 실제 사례 정답·학습 계수·확률 보정·후보 모델 비교와 새 기본 풀이의 앱 연결은 미완료다. 화면 디자인이나 사주 규칙을 새로 만들지 않았다.
- PR #174의 원문 16~19와 조사 결과를 최신 main에서 가져와 보존했다. 읽은 임시 인수인계서와 AGENTS의 한시 안내만 제거한다. 원문·아젠다·상시 현황은 유지한다. 다음 작업은 원국·시간 입력과 검토 사례를 연결하고 같은 평가 자료에서 오류를 비교하는 것이다.

## 2026-09-22 이전 인계 — 아젠다 보존과 기본 풀이 우선

- **최신 요청(원문 16~19):** 기본 풀이 적용을 조사한 뒤 사용자가 다음 세션 인계를 요청했다. 새 적용 코드·데이터 생성은 0개다. 루트 AGENTS의 임시 인수인계 안내를 읽고 인계한 뒤 문서와 임시 안내를 삭제한다. 다음 작업은 문장별 출처, 시간 미상 처리, 기본 풀이의 앱·상담 연결이다. 수학·뉴런식 지식망·옵시디언 비유는 기존 의도 원문·아젠다·수학 설계에 이미 보존되어 있다.
- **확인한 기준 main:** PR #173 `f407287`까지 병합. PR #172는 의도·연구 자료, PR #173은 Figma 직접 연결이며 기본 풀이 적용은 아직 포함되지 않았다.

- 매 세션 [사용자 원문](../docs/knowledge-model/USER_INTENT_LOG.md) → [아젠다](../docs/knowledge-model/PROJECT_AGENDA.md) → [현재 범위](../docs/knowledge-model/CURRENT_STATE.md) → [기본 풀이 계획](../docs/knowledge-model/BASIC_READING_PLAN.md)을 읽는다. `AGENTS.md`·`CLAUDE.md`·루트 README도 이 경로를 안내한다.
- 사용자 목표 = 기본 개념을 추려, 동반 키워드·여러 변수·대운과 시점에 따라 관계의 정도와 해석 확률이 달라지는 지식망을 구축하고 수집 자료로 보강하는 것. 옵시디언은 형태 비유이며 사용자 예시는 정답·규칙으로 확정하지 않는다. 앞선 말과 수정·모순 가능성도 원문으로 남긴다.
- 사용자 원문 14 당시: Figma 보드를 더 원천적인 기본 개념 기준으로 추가. 기존 `dosa-app/methodology/figjam_board_full.md`의 기초·심화 부분을 `docs/knowledge-model/FIGMA_FOUNDATION.md`와 참조 데이터로 연결한다. 최신 Figma 재수집·스냅샷 동일성은 미확인. 블로그는 정의·조건·예외를 대조·보강하고 기존 출처를 보존한다.
- 사용자 원문 15: 현재 Figma 직접 읽기 성공. `docs/knowledge-model/FIGMA_LIVE_REVIEW.md`에 18개 개념·50개 실제 노드 인용과 기초·심화 구조 연결 결과를 기록한다. 과거 스냅샷과 블로그 근거는 보존하며 자동 확률·성향 규칙으로 승격하지 않는다.
- 첫 사용 가능한 단계 = **출처가 붙은 일주 설명과 기본 성향 풀이**. 추가 스크래핑 자료를 계속 참고하되, 자료량이나 코드 시제품만으로 알고리즘 완성을 주장하지 않는다.
- 보존된 파생 작업본 = `docs/knowledge-model/`: 기본 개념 18개, 노드 58개, 관계 132개, 다중 입력 관계 11개, 문맥 조건부 모델·수학 후보·평가 설계. Python 검증 24개 통과는 코드 계약 검증이며 해석 정확도 검증이 아니다.
- **지식 추출·연구 시제품은 착수했으며, 앱의 새 해석 알고리즘과 실제 학습 확률·대운 변수 연결은 미완료**다. 기존 앱의 일주 정제본·기초 출력은 재사용 후보로 검토한다. 아래의 기존 강약 점수·키 조회·운세 정책은 과거 실행 현황으로 보존하며 새 모델의 학습 정답으로 자동 승격하지 않는다.
- 이전 대화형 앱 정리 PR #171은 main `15ef883`에 병합된 것을 확인했다. 운영 배포 상태는 이번 문서 편입으로 확인하지 않았다.

## 2026-09-22 대화형 앱 기반 정리 당시 인계 (보존 기록)


- 사용자 목표 = **사주 알고리즘을 다시 만들기 전에 캐릭터 대화 흐름과 UI/UX를 정돈한 출발점 마련**. `Saju_handoff_20260922.zip`의 검토 중 패치를 이어받았다. **알고리즘 재구축은 아직 착수하지 않았다.** 아래 과거 기록의 엔진 완료 표시는 기존 구현 이력이며 정확도 재검증 완료를 뜻하지 않는다.
- 프로필 손상 데이터 필터링·저장 실패 시 세션 유지·같은 ID 편집, 공유 대상의 성별/시간/도시/보정 구분을 반영했다. 시간 미상 상담 요약은 실제 엔진 열 이름인 `시주`를 기준으로 임시 시주와 강약 판단을 제외한다.
- 상담 API 호출을 `data/dosaClient.ts`로 분리했다. 한글 조합 중 Enter 억제, 중복 전송 방지, 요청 취소·시간 제한, 재시도, 대화 변경 후 늦은 응답 차단, 모바일 조작 영역을 정리했다. 기존 디자인 토큰과 캐릭터 대화 구조를 계승한다.
- 검증: 기존 엔진 **19건**, 앱/API 회귀 **13건**, 빌드·토큰·KB 적재·실브라우저 스모크 통과. 실제 엔진 리포트 회귀 검사가 수정 전 시간 미상 시주 노출을 재현하고 수정 후 통과했다. 누락된 시험은행 원문을 원격 blob과 동일 SHA로 복원한 뒤 리플레이도 통과했다. **최종 `SMOKE_CHROMIUM=/tmp/chromium npm run verify` 8개 게이트 전체 통과(exit 0, 브라우저 스킵 없음)**. 상담 브라우저 회귀 **10/10건 통과**: 3종 뷰포트·입력 초점, 초기 화자, IME·중복 전송·재시도, 화자/프로필 변경 및 화면 이탈 취소, KB 오류·복구. 전후 캡처는 상세 보고서에 연결했다.
- `npm run test:app`을 추가하고 `verify`의 **7/8 필수 게이트**로 연결했다. 브라우저 스모크는 8/8이다. 테스트는 AI 응답을 모킹한다. 실제 AI 자격·모델 가용성·운영 환경 응답 품질, 전체 출생일/절기/시각 경계의 계산 정확도는 이번 검증으로 확인되지 않았다.
- **병합·배포 완료 미확인.** 상세 변경·검증 범위·전후 화면 = [`docs/reports/20260922_conversation_foundation.md`](../docs/reports/20260922_conversation_foundation.md). 다음 큰 작업은 알고리즘 정답 데이터와 경계 조건을 정의한 뒤 계산 계층을 재구축하는 것이다.

## 🔒 세션 수칙 (누락·드리프트 0 — 최우선)

퀄리티는 세션 실력이 아니라 **통과해야 하는 검사기**가 보장한다. 그래서:

1. **시작**: `AGENTS.md`의 원문·아젠다·기본 풀이 계획 읽기 → 이 STATUS 읽기 → `git fetch` → `origin/main`이 앞섰으면 **즉시 병합**(장수 분기 금지 — 2026-07-17 앱#39와 지식#40이 갈라진 사고가 그 증거). 그다음 `npm run verify`로 파생물 재생성 + 상태 확인.
2. **단일 진실 = main.** 작업 브랜치는 짧게 쓰고 빨리 머지. 다른 세션이 main을 전진시켰으면 내 브랜치에 먼저 병합하고 이어간다.
3. **기계산출물 손편집 금지**: 색인(`kb/*_index.json`)·`kb.json`·`unit_bodies.json`·`app/src/engine/vendor/*`는 전부 생성물. 값 바꾸려면 **생성 스크립트를 고쳐 재실행**한다.
   - 엔진 정본 = `dosa-app/engine/src/`. 앱 사본은 `npm run sync:engine`으로만 갱신.
4. **커밋 전 `npm run verify` 통과 필수** (8게이트: 기틀·파생물→디자인토큰→vendor드리프트→엔진테스트→증류검증+리플레이→앱빌드→앱/API회귀→브라우저 스모크). 하나라도 빨강이면 커밋 금지.
5. **작업 후 이 STATUS 갱신** (진행표·작업큐). 다음 세션에게 남기는 쪽지다.

## 명령어 (전부 저장소 루트에서)

```bash
npm run verify        # 만능 품질 게이트 (커밋 전 필수)
npm run test:app      # 프로필·공유·시간 미상·IME·상담 API 회귀 검사
CHROMIUM_PATH=/tmp/chromium npm run test:chat # 빌드 후 실제 브라우저 상담 상호작용 회귀(설치된 Chromium 경로 사용)
npm run build         # prebuild(파생물+sync+kb번들) → app tsc+vite → dist/  (Cloudflare가 이걸 씀)
npm run sync:engine   # dosa-app/engine/src → app/src/engine/vendor 재생성
npm run build:kb      # kb 번들(app/public/kb-<hash>.json + vendor/kb_ref.json) 재생성
# 스킬: /saju <생년월일시> (근거 리포트+풀이) · /feed <유튜브URL> (지식 주입)
```

## 기존 구현 이력 (당시 verify 통과 — 최신 검증 범위는 위 인계 참조)

| 층 | 상태 |
|---|---|
| L1 만세력 엔진 (`dosa-app/engine/src`) | ✅ 원국·십신·지장간·십이운성·신살·합충·공망·대운·**구조판정(judge)**·**운대입(unse)** — 테스트 19건, 포스텔러 픽스처 일치 |
| L2 색인 | ✅ unit_index(598키) + interaction_index(103키/13,641문단) + impression_index(46키/2,969문단) |
| L2 증류 | ✅ **일주 60/60** + **image 19/19**(2026-07-17 천간 6키 완성, 이미지층 종결) — 인용 223건 축자 검증 |
| L3 리포트 (`report.js`) | ✅ 구조판정→일주(증류)→십신→합충→신살→대운→세운, 출처 병기 |
| **앱 (`app/`, React+Vite)** | ✅ **명식당 셸 파도(260721, Q.23) 완료**: 운영자 목업 v2(design_handoff) = 화면 정본 — 9화면(로그인/가입/찾기/로딩/오늘/내 원국/사주분석/사주 재미/내 설정) + 상주 크롬(드로어·프로필 팝오버·하단 글래스 알약 네비) 재구현, v3 기본캐 플레이트(`assets/dosa-plate.jpg`) 3개소 배선, 라이트 단일(다크 폐지), 데이터 = 엔진 실계산 계승 · 총운 점수/월운 차트 = 엔진 미지원 비노출 · 미구현 = '준비 중' 정직 표기. ✅ **상용화 파도(260718, Q.08~09) 완료**: 고정 목업 전면 제거 — ①프로필 저장소(localStorage 다중, `data/profiles.ts`) ②입력 검증(조용한 샘플 폴백 제거·시간모름·도시→경도·음력='준비 중' 정직 비활성) ③결과 = URL 딥링크 + 7섹션 리포트 + 원국표 7행(지장간·12신살) + 대운 레일 ④홈 = 온보딩 분기 + 오늘 일진 실계산 + 운세 점수(`todayFortune`+근거줄) ⑤PWA·OG. **파도 2(Q.09, PR #62) = UI 15인 병렬 실측 수선**: 발췌 정제기(목차·인사말·남의 사건 발췌 차단 — `saju.ts cleanParas/pickExcerpt`)·공유 루프 봉합(수신자 배너+전환 CTA)·다크 스테이지 스크림(`--stage-scrim`)·오행 라벨 이중 팔레트(`--oh-label-*`, 기존 값 재사용)·접근성(줌 복원·aria·44px·inputMode 버그)·대화 스킵 버그 수선. 전/후 상세 = `docs/보고_20260718_상용화파도_전후.md` |
| L4 도사 대화(LLM) | ✅ **골격 가동**: 주제 5종 결정론 조회(`data/dosaTopics.ts`) → 타이프라이터 대화(`DosaChat.tsx`, 플레이그라운드 정본 계승) + `functions/api/dosa.ts` Claude 프록시. **키 미설정 시 L3 폴백 완결 동작** — Cloudflare env `ANTHROPIC_API_KEY` 등록 즉시 LLM 서술 켜짐 |
| 트레이닝/승계 | ✅ `/saju`·`/feed v2`(블라인드 시험 루프 — 루트 CLAUDE.md **제1법칙**) 스킬 + `probe_coverage.py` + STATUS + `npm run verify` 게이트 |

## 다음 작업 큐 (우선순위)

**2026-09-22 현재 최우선:** 사용자 원문 20~25의 알고리즘 정확도 목적에 따라 `docs/knowledge-model/CASE_REVIEW.md`의 경로에 실제 검토 사례를 연결하고 목표·척도·출처/인물·라벨·예외를 확정한다. 같은 평가 자료에서 조합 유무 모델을 비교하며 강약 측정 정책·학습·보정과 기본 풀이 앱 적용은 후속 검증으로 진행한다. 아래는 보존한 기존 작업 큐다.

1. ~~앱 KB 번들 경량화~~ **완료(← Q.05, 260717)** — JS 번들 0.57MB, kb 독립 캐시, loadKb() 게이트.
2. **배치 전 레일 3종**(평의회 260717 제안): ⓐ`replay_exams.py` **완료(← Q.04)** ⓑL3 리포트 골든 스냅샷 3건(조립 이음새 계약 테스트) ⓒ증류 단일출처 플래그 린트(validate_distilled 확장).
3. **도화도르 채널 배치 트레이닝** — **코퍼스 도착(260718, ← Q.09)**: 노뮤트 전사본 zip(영상 대본 259편 · Whisper 83/자동 175/스텁 1 · 운영자 분류 tsv). 통합 전략 정본 = `docs/20260718_165850_노뮤트전사본_통합전략_v1.md`(3분할 게이트: 지금 = 신규 Whisper 59 · 대기 = 자동 175 재전사 후 · 동결 = 기보유 61[교체 24 조건부]). **파도 1 = 신규 Whisper C07 이론 12 + C04 재물 8 = 20편, 사용자 GO 대기**(GO 1회 = 파도 1개 · zip은 레포 커밋 금지[블라인드 격리]라 GO 시 재업로드 필요 가능). ※도화도레 = 도화로운 동일 저자 확인(O10) — 소스 라벨 병합. **정제 파일럿 3편 완료(Q.10)** — 유닛 40·교정 64쌍·시트 스키마 v1.1(훅/적중검증형/사다리+신뢰 2층, 전략 문서 §9).
4. **상호작용 상위 30키 + 통변방법론 28편 증류** → frame/·chain/ 키가 근거 유닛을 갖게 됨(현재 구조 후보 = 문헌 근거 0 — 260718 실측).
5. **직업→십신 역매핑 사전** (금융=편재, 학문=인성 …) — L4 후속 질문 처리용.
6. ~~앱 출생지→경도 전달~~ **완료(← Q.08, 260718)** — 도시 26개 테이블 + 프로필/딥링크에 경도 배선(진태양시 캡션 표기).
7. **일진 다이어리 UI** — 엔진 `diaryDayInfo` 완성 + 홈 오늘운세에 배선됨. 기록 UI는 사용자 제공 예정.
7-1. ~~정곡 오프닝~~ **1고리 완료(← Q.10, 260718)** — `data/jeonggok.ts`(f6accc4 프로토 포팅: 탐지 9축·임팩트 가중 0.35/0.40/0.25·원국×대운 시기 크로스조인) → DosaChat 오프닝(단정→[맞아/아니야]→的中 크리티컬/리커버리·근거=엔진 판정 명시). **후속**: 고리 확장(depth 4 = 플레이그라운드 기본값)·호감 게이지·명식수 노드.
8. ~~L4 도사 대화(API)~~ **골격 완료(← Q.08)** — 자유 질문 입력·실패 재시도·요청 취소는 2026-09-22 기반 정리에서 모킹 브라우저 검증 완료. 남은 것: 스트리밍, 대화 이력, 실제 AI 환경 확인.
9. 귀문 인상론 보강(유일 커버리지 공백) · 대운수 반올림 유파 옵션.
10. **음력 입력 지원** — 엔진에 변환 없음(260718 단정 실측, UI는 '준비 중' 비활성). KASI 음양력 테이블(1900~2100) 벤더링 → 변환 함수 → 픽스처 검증. 상용 필수 축.
11. **시간 모름 엔진 3주 모드** — 현재 = 정오 대입 + UI 잠금(강약·신살·합충·대운 비노출). 엔진이 시주 없이 강약 재비례(90/110)까지 지원하려면 **결정 로그 110점제 확장 = 사용자 승인 필요**.
12. **발췌 품질** — 신살/십신 발췌가 글 헤딩·인사 문단부터 시작하는 경우 존재(260718 스크린샷 실측). 유닛 본문 시작점 스킵을 세운 외 문서 유형으로 확장.

13. ~~에니어그램 교차 렌즈~~ **완료(← Q.12, 260719)** — `app/public/enneagram/`(툴킷, Q.11 이식) + `data/enneaLens.ts`(십신→후보 유형 결정론 가중표) + 리포트 말미 카드·테스트 링크. **평의회 260719(페이블 8인·5분 캡) = PASS 6+조건부 1·차단급 0, 교정 6건 반영.** 후속: ⓐ가중표 코퍼스 근거 보강 — **필수 승격**(위원2: 원칙1 '코퍼스 출처' 충족 조건 · 다음 증류 파도 병합) ⓑ~~테스트 결과 역수입~~ **완료(← Q.13, 260719)** — localStorage 직독 보조지표 블록(정확 일치·fail-soft). **⚠위계 원칙(운영자 260719 확정): 메인 = 사주 원국 해석 · 에니어그램 = 보조지표(곁 신호) — '가설 vs 실측' 판정 프레임 금지**(정곡/L4에 유형 축을 얹을 때도 이 위계 유지) ⓒ툴킷 소스 접촉 파도(ⓑ와 동시): #test 해시 딥링크(위원7 — 현재 버튼 착지 = 홈+1탭) + R9 새니타이즈 선행.

14. **브랜드 반영 파도(명식당·연리, ← Q.16)** — **화면 셸·인앱 문구는 Q.23 목업 파도로 선반영 완료**(로그인 로고·드로어 "명식당/주인 연리"·로딩 "연리가 만세력을…"·홈 "연리에게 물어볼까요"·태그라인). 잔여 = `app/index.html`(title·og:site_name·og:title·description)·manifest·인라인 로더 문구·OG 이미지·공유 수신자 배너·DosaChat 첫 인사. **선행 조건 2**: ①연리↔아이샤·남신 기본캐 관계 확정(결정 로그 260721) ②채택 카피 확정. 카피팩 정본 = 원장 [A. 16] 채팅 보고.

15. **UIUX 보완 4파도(← Q.30, 260725)** — 청사진 정본 = `docs/20260725_172039_UIUX보완청사진_v1.md`(폰 열람본 = 라이브 `/reports/20260725_172039_uiux-blueprint_v1/`). 전 11화면 실렌더 픽셀 실측으로 결함 17건 확정. **파도 0(가독, GO 즉시 착수 가능)** = W0-1 스테이지 스크림 라이트 복원(⚠**회귀**: 260718 다크 전용 도입 → 260721 다크 폐지로 `--stage-scrim` 영구 transparent → /result 캐릭터 플레이트 위 텍스트 65개 무보호) · W0-2 알약 네비·상단 유틸 유리 불투명도(현 `.28` = 뒤 본문 투과 3건, 방식론 §3-c) · W0-3 홈 히어로 이름 2줄 시 43px 침범(`Home.tsx` 247–253). **파도 1(흐름)** = W1-1 `/result`를 `MyeongShell` 편입(현재 셸 밖 = 하단 네비 0 = 막다른 길) · W1-4 헤더 3요소 단일 레이어(스크롤 시 세그먼트만 소멸·중앙Y 2.5px) — W1-2 정보구조(탭4↔드로어5)·W1-3 분석↔결과 층위·W1-5 캐릭터 정체성(이름3·얼굴2)은 **운영자 결정 D1~D3 선행**(D1 = 위 브랜드 항목 14의 미확정 1건과 동일 축). **파도 2(문법)** = 문자도형·이모지→SVG · '준비 중' 단일 부품 · 폼 문법 통일 · 형제 중앙 1px 스윕 · 최장명 오버플로 방어 · 스크롤 경계 어포던스 · solar 의미축 분리(※[4-1] 소급 캠페인 금지 = 게이트 등재축만 강제·나머지는 touch-then-align). **파도 3(루프)** = 일진 다이어리 기록 UI(엔진 `diaryDayInfo` 배선 완료 = 큐 7과 동일 축) · `/fun` 8칸 중 7칸 준비중 재편. **동승 필수 = 기계 게이트**(값 수정만으론 재발 — A1이 축 변경 회귀였음): G1 렌더 기하(스크림 알파>0·유리 뒤 투과 0·형제 중앙Y≤1px·대표데이터 오버플로 0·클립 아래 잘린 컨트롤 0) · G2 글리프 0 · G3 캐릭터 참조 `chefs.ts` 단일 경유 — `scripts/smoke.mjs`가 이미 실브라우저를 띄우므로 증분 확장.

16. ~~UIUX 재구성 파도 0·1(레퍼런스 기반)~~ **완료(← Q.31, 260725)** — 정본 = `docs/20260725_181500_UIUX재구성_레퍼런스와전후_v1.md` · 전/후 영수증 = 라이브 `/reports/20260725_181500_uiux-reconstruct_v1/`. **레퍼런스 = `forceteller-ref/` 자립 스냅샷 실렌더**(결과 13섹션·단색배경·행라벨 표문법) + 소비자앱 5종 IA. **화면 정본 갱신점(다음 세션 필독)**: ⓐ`/result` = 이제 **`MyeongShell` 안**(active='analysis' · `gate={false}` = 공유 딥링크 수신자 비게이트) — 셸 밖 아님 ⓑ`CharacterStage` = **담긴 스테이지 공용 부품**(height prop · 기본 380 · `/result`는 300) — 전면배경(inset0) 금지가 이 부품의 존재 이유(라이트 `--stage-scrim`은 여전히 transparent = 다크 유물, 스테이지를 담아서 우회했으니 스크림 값에 의존하지 말 것) ⓒ**하단 탭 = 5개**(오늘·원국·분석·재미·설정) = 드로어와 1:1 · `HomeSegment` **삭제됨**(홈 세그먼트 문법 폐지) ⓓ상주 크롬 유리 = `chromeGlass` 단일상수(`.92` + `saturate(0)`) — 새 크롬 표면은 이 상수를 쓴다 ⓔ화자 이름 = `chefs.ts`의 **`HOST_NAME`('연리')** 단일 출처(`aisha.png`·'아이샤' 참조 0 · ⚠연리↔셰프단 위계는 Q.16 미확정 유지) ⓕ문자 도형 대체 SVG = `Pict.share/mail/pencil/check/chevronLeft/Down/Right` ⓖ'준비 중' = `Fun.tsx`의 **`SoonBadge` + opacity .58 + aria-disabled** 한 쌍이 단일 표준. **잔여(큐 15의 파도 2·3 축)** = 입력 문자 체크박스(`□`·`✓`)·로그인↔가입 폼 문법 2종·`的中?` 라벨(플레이그라운드 정본이라 운영자 확인)·입력 3단 위저드(레퍼런스 공통인데 없음)·**G1~G3 기계 게이트 미구축**. ⚠**청사진 수치 정정**: 알약투과·입력잘림 판정식은 x축 미고려 **과탐**(G1은 픽셀 대비 샘플링으로 설계) · 설정 '중심Y 9px'은 2줄 스택 오측정 = 결함 아님 · 대운 마스크는 14px로 좁았던 것(부재 아님).

17. **인트로 재조판 파도(← Q.37, 260726)** — 정본 = 원장 [A. 37] · 플레이그라운드 = `docs/reports/260726_인트로배치_v1.html`(폰 열람 사본 `app/public/reports/260726_인트로배치_v1.html`). **화면 정본 갱신점(다음 세션 필독)**: ⓐ`/result` 섹션 순서 = **오늘 운세 → 사주 원국 → CTA 2**(전엔 원국이 위였다 — 뒤집으려면 운영자 승인) ⓑ**우상단 프로필 아바타·팝오버 = 제거됨**(`MyeongShell`에 이제 상단 유틸이 햄버거 하나뿐) — 계정 축 진입점 = **`Settings.tsx` 한 곳**(프로필 전환·구매 내역·로그아웃·수정 전부 거기 실재). 새 계정 UI를 상단에 다시 만들지 말 것 ⓒ`SajuTable`에 **`fluid`** prop 신설 = 카드 껍데기 없이 부모 폭 채움(열 `flex:1` · 타일 56 = `compact` 값 계승) · `OhaengStrip`에 **`bare`** prop 신설 = 같은 축. 원국 카드는 이 둘을 한 `.glass`에 담는다 ⓓ오늘 카드 = 점수 **좌·`--c-primary`** + 라벨 좌/우 + 문안은 엔진 `REL_PHRASE`의 「사건 — 처방」 절 경계 줄바꿈(`' — '` split — 엔진 문안 형식이 바뀌면 여기도 봐야 한다) + 근거 = 칩 라벨 + 관계 한 건씩 줄 ⓔ날짜 머리 한자 = `todayIljin().dayHanja`(엔진이 원래 주던 값) ⓕ진태양시 캡션 = **산출값만**(오행 아래 우측) · 보정량·출생지·야자시는 **원국 카드 머리 줄**로 이관. **실측 기준선(회귀 감시용)**: 두 카드 x=20·w=350 · 원국표 열 타일 중심X 0px · 오행 중앙칸 195.02 vs 카드 195. **잔여** = 실기기 확인 미실시 · 플레이그라운드 축 A~E에서 운영자가 다른 안을 고르면 그 축만 교체. ⚠**병렬 충돌 이력**: 같은 시각 PR #118(계정 기억·원탭 재로그인·스트릭, ← Q.36)이 `Intro.tsx`를 함께 고쳐 머지 충돌 — 병합 시 #118의 **연속 방문 배지**(날짜 머리 오른쪽 · 2일차부터)와 **`hasBanner` 이중 여백 방지**를 새 구조에 이식했다. 날짜 머리는 배지가 붙어도 중앙축을 잃지 않게 `SectionTitle` 자체 정렬이 아니라 **행 전체 `justifyContent:center`**로 잡았다.

18. **디자인 SSOT 확립 + 타 레포 차용(← Q.38, 260726)** — 정본 = **`docs/디자인토큰_제1핵심명령.md`**(【바인딩】 「디자인 확립본 = 없음」의 빈칸을 메운 문서). **다음 세션 필독**: ⓐ **디자인 값은 이제 계승/갱신 2택이다** — SSOT = `app/src/index.css :root`(36토큰) · 예외 SSOT = `theme.ts`의 오행 의미색 15 + `minFont/minTap`. 새 hex·px를 쓰려면 문서 §A-③(두 선택지 제시 후 승인) ⓑ **게이트가 문다** = `npm run verify` **2/7 = `scripts/check_tokens.mjs`**(T1 토큰락 · T2 거울 무손실 · T3 raw 총량 baseline hex 37/rgba 48). 토큰을 늘렸으면 승인 후 **`npm run lock:tokens`** — 안 하면 커밋 못 한다 ⓒ **MUI palette에 `var(--x)` 문자열을 넣지 마라** — MUI가 alpha/darken 계산을 못 해 **error #9로 앱이 통째로 죽는다**(260726 실측). `theme.ts`의 `cssToken()`이 `:root` 실값을 읽는 방식이 정답 ⓓ `OhaengStrip` → 인트로는 **`OhaengSegBar`**(비율 세그먼트 바 · 0개는 빗금 잔류)로 교체. 구 부품은 5축 판정이 다 필요한 자리용으로 보존 ⓔ **차용 대기분**(조사 완료·미적용) = orb/dots 로더 · 스켈레톤 스윕 · 토스트 · 모션 커브 3종 + 스태거(95ms/24ms) · press 5토큰 사다리 · reduced-motion 전역(현재 **0건**) — 대부분 **토큰 신설 승인 대기**. 조사 원문 = 원장 [A. 38] ⓕ **폰 목업 규격이 틀렸다**(postspark 26기기 스키마 대조) — QA 렌더·플레이그라운드가 390×844(iPhone 13/14)에 radius 34를 쓰는데 iPhone 16 Pro는 **402×874 · radius 68**. 목업을 다시 쓸 일이 있으면 이 값으로. ⚠**[A.37] 오진 정정**: 루트 `dist`가 안 갱신되던 원인은 SIGPIPE가 아니라 **`cd app` 후 cwd가 남아 앱 빌드만 돈 것**. 빌드는 반드시 레포 루트에서.

19. **모션 어휘 + 커밋 훅(← Q.39, 260726)** — **다음 세션 필독**: ⓐ **커밋 훅이 산다** — `.githooks/pre-commit`이 `npm run verify`를 돌린다. **clone하면 `git config core.hooksPath .githooks`를 1회 해야 한다**(안 하면 조용히 미실행 = C4의 그 함정). 확인 = `git config core.hooksPath`. 우회는 `--no-verify`이고 우회했으면 보고에 적는다 ⓑ **`prefers-reduced-motion` 대응이 생겼다**(그전 0건) — 새 애니를 넣을 때 개별 가드를 달 필요 없다(전역 블록이 강등). 단 **JS 타이머는 CSS가 못 잡으므로** `useReducedMotion()`(`components/Motion.tsx`)을 직접 소비해야 한다 ⓒ 모션 부품 = `DigitRoll`(자릿수 롤) · `WordReveal`+`wordOffsets`(단어 리빌) · `.msd-popin`(되살린 스태거 유틸). 새 등장 연출이 필요하면 여기서 계승 ⓓ **DosaChat 본문엔 등장 기법을 얹지 마라** — 타이프라이터가 이미 그 역할이다(중복).

20. **인트로 조판 2차 + 분석 주제 재편 + 상단 크롬 철거(← Q.40, 260726)** — **다음 세션 필독**: ⓐ **상주 크롬 = 하단 알약 네비 하나뿐이다** — 좌상단 햄버거·드로어 **제거**(운영자: "아래에 메뉴들이 있어서"). 드로어 5행이 하단 탭과 1:1 중복이었다. 그 여파로 **전 화면 상단 여백을 크롬 회피분만큼 되돌렸다**(6.5~7 → 1.5 · 인트로 5.5 → 0.5 · 배너 6.5 → 1). 상단에 새 유틸을 만들려면 이 여백도 같이 되돌려야 한다. ⓑ **카드 반경 = 14px 단일**(18 전량 수렴 · 운영자 "모서리 둥글기를 줄여") — 새 카드는 14를 쓴다. ⓒ `OhaengSegBar`의 0칸 = **오행 실색 빗금 + 「화 0」 라벨**(전엔 회색 빗금 + "0"만) · 판정 줄(「수 3개·과다」·「화 없음」)은 바에서 떼어 **`OhaengVerdicts`**로 분리 → 원국 카드 **12신살 아래**로 이관. ⓓ 인트로 근거 = **기본 접힘**(칩 클릭 토글) · 날짜 머리 「오늘」 = **알약 배지**. ⓔ 원국 카드 머리 = **일주 `GapjaSticker` + 이름(강조색) + 생년월일시(2강조색 `--c-heading`) + 시주 한자 + `|` + 출생지**. 괄호 감싸기 폐지. ⓕ **`/analysis` = 주제 아코디언 6종**(성향과 기질·관계와 인연·일과 재능·조심할 점·올해의 흐름·그 밖의 근거 — `data/analysisGroups.ts` 매핑 + `components/TopicAccordion.tsx`). 카드 축이 술사 도구축(일주·십신·합충)에서 독자 관심축으로 바뀌었다 — **원문·출처는 재배치만**(1자 무변경) · 미지 카드 id는 '그 밖의 근거'로 폴백해 소실 0 · **'가치관' 그룹은 없다**(매핑될 근거 유닛이 0이라 빈 그룹 금지 원칙 적용 — 큐 4의 상호작용 증류가 붙으면 신설 가능). ⓖ T3 baseline **rgba 48 → 42 인하**(드로어 제거분). ⓗ **60갑자 스티커 레일 = `.github/scripts/gen_gapja_stickers.py` 작성 완료·미실행**(시트 4장 × 15칸[3동물×5오행] → 알파 트림 후 512px 정사각 중앙 배치 → `app/public/assets/gapja/`). 실행 = Actions `gen-gapja-stickers` 수동 dispatch(시크릿 `OPENAI_API_KEY_NOMUTE` · 워크플로가 결과를 main에 직접 커밋). **운영자 승인 260726 = "시트 4장"(약 $1)**. 멱등이라 재실행해도 과금이 안 는다. 에셋이 0장이어도 앱은 `GapjaSticker` 폴백(오행색 원 + 이모지)으로 정상 동작한다 — 즉 **부품·데이터·파일명 계약·프롬프트 플레이그라운드는 전부 서 있고 빠진 건 PNG 60장뿐** → **✅260726 실행 완료**(감사 실측: `app/public/assets/gapja/` 60장 실존). ⓘ **⚠ 캐릭터 체제가 성별 분기로 확장됐다(운영자 260726 구술 + 레퍼런스 4컷 첨부)** — 레일 = `.github/scripts/gen_chefs_gendered_v1.py`(**작성만 · 생성 금지 상태**: 운영자 "안뽑았으면 일단 뽑지말구"). ⓐ **남성 사용자 상대 = `noona`** 백발 기센 언니(옆으로 땋은 은백발·앰버 눈·붉은 눈꼬리·레드 립·검정 민소매 하이넥·딥그린 단색 배경) ⓑ **여성 사용자 상대 = `doryeong`** 갓 쓴 흑발 도령(검정 갓·갓끈 구슬·형광 앰버 눈·창백한 피부에 보랏빛 실금 문양·검정 도포·밤빛 푸른 실내 보케) — 운영자 **"메인이 1번"** = 앉은 자세 대표 컷. ⓒ 그림 축 = **v3~v5 남신의 밤 수채·회화를 명시적으로 폐기**하고 비주얼노벨 스탠딩 CG(또렷한 라인 + 평평한 셀 채색)로. ⓓ **레퍼런스에서 뺀 것 = 담배·맨가슴 노출**(스토어 심사·연령 축 · 운영자가 그대로 가자면 되살린다). ⓔ **미해결 = 연령 축**: 먼저 말한 "도사 **할머니**"와 첨부(20~30대 백발)가 어긋난다 — **확정 전 생성 금지**. 앱 배선(`chefs.ts` `ACTIVE_CHEF_ID`·플레이트 사본)은 컷을 보고 고른 뒤 별도 커밋.

21. **연식당 세계관 배선(← Q.41, 260726)** — **다음 세션 필독**: ⓐ **상호가 바뀌었다 = 명식당 → 연식당(緣食堂)**(운영자 구술). 앱 문자열 10곳 치환 완료 · `app/public/reports/` 아래 과거 보고서 박제본은 **의도적으로 안 고쳤다**(그때의 기록이라서). ⓑ **무드 정본이 생겼다**: "너무 모던하게보다는 적당히 모던 + 글래스모피즘 + 일본식 벚꽃·미연시 캐릭터 풍경 + 사쿠라 느낌 목재 건물의 고풍과 품위". 순수 모던 SaaS 톤 제안은 이 축에서 기각된다. ⓒ **신규 부품 `ShopStage`** = 간판「緣食堂」 → 글래스 판 → 키잉된 인물 3층. 캐릭터 아트 0장이라 인물은 폴백 실루엣이고, 플레이트가 들어오면 `<img>`가 그대로 채운다. ⓓ **도사 교체 연출** — 성별 분기 배정(`chefForGender`: 남성 사용자=noona · 여성=doryeong)에 더해, 정곡을 **빗맞히면 맞은편 도사가 촤르륵 밀고 들어와 판을 받아 간다**(`BARGE_LINE` · `msd-barge-r/l`). 사과·수습이 아니라 **화자 교체**가 우리 리커버리 문법이다(운영자 구술 정본). ⓔ 정곡 선택지 = 답변 유도 → **속마음**("음… 그랬던 거 같아요 (어떻게 알았지)"). ⓕ 기능 라벨 금지 축 — "○○와 상담하기" 같은 메뉴 문구는 몰입을 깬다 → "연식당, 자리에 앉다". ⓖ **접근성 3건 배선**: `:focus-visible` 전역 링(실측 0건이었다) · 하단 네비 비활성 탭 `inkFaint`(2.5:1 미달)→`inkSub` · `prefers-reduced-transparency` 강등. 전부 `var()` 계승이라 T3 무증가. ⓗ **UI 웹조사 2건 완료**(페이블=캐릭터·연출 축 / 오퍼스=구조·성능·접근성 축) — 즉시분은 위 ⓖ로 반영했고 **승인 대기분**이 남았다: 미연시 백로그·오토 모드 · 포커스 링 토큰화 · `--dur*`/`--r-*` 토큰 신설(raw 30곳 회수) · `--blur-m 26px` 예산 축소 · 대비 미달 6색(`--c-ink-faint` 2.48:1 · `--c-solar` 2.98:1 · 오행 라벨 토/금/화) · View Transitions. ⚠ `theme.ts`의 `tokens.radius`(sm8/md12/lg18)가 실제 값(12/14/18)과 어긋난 **두 번째 손실 거울**이고 게이트 T2는 색만 본다. ⓘ **레퍼런스 첨부 시트 레일 = 대기**: `gen_chef_faces_v1.py`(표정 15×4=60 · 장면 16/20) — **`app/public/reports/chef-refs/`가 있어야 돈다.** → **✅260726 도착**(감사 실측: 7장 실존). ⓙ 캐릭터 4인 등재 · **✅260726 이름 확정**(홍화동녀·단리아·알카사르·설향 동자) — 다만 **메신저 전환(Q.46) 이후 화면엔 이름표를 안 띄운다**(운영자 "이름은 제외" · 낭독용 sr-only 접두만 남김).

22. **상담 = 메신저 · 서체 Paperlogy · 유리 실동작(← Q.45-b~46-d, 260726)** — **다음 세션 필독**: ⓐ **`/talk`이 대사창을 버리고 메신저 말풍선 로그가 됐다**(운영자 "메세지 하나당 하나씩 · 이름은 제외 · 그 다음에 내가 대답"). 한 통 = **최대 2문장**(문단으로만 자르면 통짜가 된다) · 내가 고른 답은 **내 말풍선**으로 로그에 남는다 · 화자 이름표 없음(낭독용 sr-only 접두 「도사: 」/「나: 」만) · 로그는 **하단 앵커** · 자동 스크롤은 **바닥 근처일 때만**(안 그러면 되읽기가 불가능하다). ⓑ **명식 = 좌상단 표식**(`MiniChart` · 간지 8자만 · 실측 101px = 프레임 26% · `aria-hidden` — 운영자 "사용자는 사실 볼 필요 없게"). ⓒ **앱 서체 = Paperlogy**(원본 ttf = `assets/fonts/paperlogy/` · 배포는 woff2 **실사용 5웨이트만**). 토큰 `--pretendard` → **`--font-sans`**. ⚠**간지 한자 14자가 이 서체엔 없다** — 뒤 스택 폴백이 명식을 살리므로 **폴백 체인을 지우지 마라**. ⓓ ⚠⚠ **유리가 앱 전역에서 죽어 있었다**(260726 검토 적발): `index.css`가 무접두 `backdrop-filter`를 **먼저**, `-webkit-`을 나중에 써서 미니파이어가 중복으로 보고 **뒤엣것만** 남겼고, 크롬은 `-webkit-`을 안 받는다 → computed `none`. **접두 먼저·표준 나중**이 규칙이고, `verify.sh` 6.5단계가 산출물 CSS에서 이걸 지킨다. **말풍선엔 blur를 걸지 마라** — 대화 길이에 비례해 유리 층이 늘어난다(표면값만 계승 · blur 상한 = 미니명식·네비 2장). ⓔ **표정 컷 배선은 서 있고 자산만 잠겨 있다**: `FACE`는 **v2 실물 01~06**이다(앞서 폐기 v1 번호 16·17·29·48을 써서 전량 404였다) · `HAS_FACES`는 **빈 집합** — 유일한 v2 시트가 알파 없이 밤 배경째라 `bare` 무대에 올리면 파란 사각형이다. **키잉된 컷이 오면 id 한 줄**이면 살아난다. ⓕ **최상위 정리 89 → 파일 7개**: 정제 전 원본 docx 76장이 `원본/`으로 갔고 **`refine-tools/refine.py`의 입력 글롭도 같이 옮겼다**(루트를 글롭하고 있었다 = 파일명은 파이프라인 계약이라 무접촉). ⚠**디렉터리 21개는 아직 그대로** — 운영자 요구 "각각 적당한 폴더로"는 **부분 이행**이다. ⓖ 브랜드 잔재 치환(`index.html`·manifest = 아이샤 → 연식당) · 「○○와 상담하기」 2곳 → 「연식당, 자리에 앉다」(21-ⓕ 금지 축 이행). ⓗ **인물 = 배경 그 자체**(Q.47-b 재확정): `/talk`은 도사 풀 컷(`chef-<id>-full.webp` = 운영자 원본 `chef-refs/*.jfif` 재인코딩)이 프레임 전체를 덮고, 그 위 중하단에 대화가 **위→아래**로 쌓인다. **키잉 안 한다**(운영자 판단 — 이 그림들은 조명·배경이 인물과 한 덩어리라 떼면 테두리가 남는다). 인물을 세우는 층(`ShopStage`·`PixelDosa`·`faceFor`)은 이 화면에서 빠졌다 — **배선은 `chefs.ts`·`ShopStage`에 그대로 살아 있다**. 교체(barge) = `onChef` 콜백으로 배경 교체. ⓘ **입력창**(예타 `.yeta-in` 문법 계승) — Enter 전송·Shift+Enter 줄바꿈 · 서버(`functions/api/dosa.ts`)가 화이트리스트 주제 **또는** 자유 질문(`question` 300자)을 받는다. ⓙ **⚠ 두 번 데인 함정 2개**: (1) **MUI `sx`의 숫자 `1`은 `px`가 아니라 `100%`다** — sr-only를 `width: 1`로 적으면 화면을 통째로 덮는다(실측 390×762). (2) **접두 CSS는 접두 먼저·표준 나중** — 순서를 뒤집으면 미니파이어가 표준 선언을 지운다. ⓚ **L3 폴백은 정제된 줄만 말한다** — `DosaLine.raw`(발췌 원문)는 말풍선에서 빠진다. 안 그러면 도사가 문서 제목·유튜브 채널 인사를 읽는다.

23. **운영자 결정 대기(260727 시점 · 검토 4인 감사 결과)** — ⓐ**「한수 진행하셈」(굴절 유리 SVG) 미이행** — 말풍선에서 blur를 걷어낸 결정과 충돌해 임의 착수 안 함 ⓑ**알카사르 풀 컷 노출 수위** — 가슴 열린 원본이라 20-ⓓ의 스토어 심사 축이 다시 걸린다(더 노출이 큰 `-alt`는 안 썼다) ⓒ**`/enneagram/`이 방문자 localStorage에 실명 명단을 심는다**(`enneagram_people_v1` · 병렬 세션 산출물) ⓓ**도트 캐릭터 `PixelDosa` = 도달 불가 코드**(8비트 축 존폐) ⓔ**`StageBackdrop`은 `/talk` 한 화면뿐** — 운영자 "전역"의 범위 확인 ⓕ**하단 네비 탭 40px < 44 하한**(기존 축) ⓖ**로그인·회원가입 빈 폼 통과**(계정 축 미구현) ⓗ**최상위 디렉터리 21개는 아직 그대로** — "각각 적당한 폴더로"는 파일만 이행 ⓘ**`유튜브_자막_모음/` 308파일이 영상 제목을 평문 파일명으로 추적 중**(블라인드 격리 구멍) ⓙ**표정 컷 v2가 알파 없이 배경째** → `HAS_FACES` 빈 집합으로 잠금(키잉된 컷 = id 한 줄로 부활).

22. **상담 탭 = 미연시 무대(← Q.41, 260726 · 드래프트 PR — 운영자 지시로 미머지)** — **화면 정본 갱신점(다음 세션 필독)**: ⓐ`/talk` = 무대 문법 단독: 중앙 표제 한 줄(「당신의 사주팔자를 들여다봅니다」 — 제목+부제 2단 폐지) → **도트 캐릭터**(`components/PixelDosa.tsx` — 16×19 도트 맵·색 전량 토큰 참조·**width는 16의 배수만**[도트당 정수 px, crispEdges 우글거림 방지]) → **원국 상시 펼침**(`DosaChat` 내 `VnChart`: 년월일시+간지 8타일·십성 행 비노출) → 글라스 선택지(분류 키워드 mini 폐지·**blur 비장착** = 유리 표면 7장 방지) → 그라데이션 대사 구역. 하단 「사주 분석 풀이 보기」 CTA 제거 ⓑ**대화 시기 표기 = 연도**(`jeonggok.ts` EVENT = `birthYear+age`년 · LLM 프롬프트 동일 지시) — 나이(N세) 표기 금지(운영자 260726) ⓒ정곡 오프닝 = 단정+`ask` 훅(「…그것 때문에 왔지?」) · choose 인사 3형(초면/정곡 경유/재방문) ⓓ**포커스 부상** = `jeonggok.focus`+`TOPIC_FOCUS` → 관련 기둥 translateY·scale(이웃 겹침 zIndex 필수) ⓔ**덜컹 = WAAPI `rumble`** — key 리마운트 금지(근거 details 접힘 소실)·WAAPI는 reduced-motion CSS 밖이라 JS 가드 필수 ⓕ화자명 잔재 소탕: `functions/api/dosa.ts` '아이샤'→'연리'. ⚠대기 = 말투 분열(LLM 존댓말 vs L3 반말 도사체) · 도트캐↔셰프 플레이트 관계(캐릭터 확정 시).

23. **OAuth 계정 로테이션 레일(← Q.42, 260726 · PR #127 드래프트 동승)** — nomute-editor 차용: `shared/claude_transient.sh`·`claude_py.py`(런타임 폴오버 SSOT — 쿼터 시 ALT→ALT2→ALT3 전환·5xx 재시도·preflight) + `shared/account_failover.py`(sticky 승격 — 체인 = **MUTENO→NOMUTEFB→EMS1130G→MUTENONA→EMS1130M→EMS1130N** 6계정 순환) + `account-selftest.yml`. **사용법·완성형 env 블록 = `docs/oauth_계정로테이션_차용.md`**(Claude 호출 워크플로 만들 때 §3 블록 4개 복사 = 즉시 로테이션). 사본 갱신 규약 = 정본(nomute-editor) diff 후 재복사(손편집 금지). ⚠운영자 액션 = `GH_VARS_TOKEN` PAT(Variables: Read and write) 등록 → account-selftest 런 「🎉 PAT 실측 통과」 확인(그전까지 승격만 no-op·폴오버는 동작).

24. **상담 LLM = OAuth 체인 + 모델 전환 + 선반응(← Q.43, 260726)** — **다음 세션 필독**: ⓐ`functions/api/dosa.ts` = OAuth 6계정 로테이션(`Bearer` + `oauth-2025-04-20` 베타 · 401/403/429/5xx·한도 문구 = 다음 계정) → `ANTHROPIC_API_KEY` → L3 폴백. **CF Pages env 등록 = 운영자 액션**(`CLAUDE_CODE_OAUTH_TOKEN_<계정명>` 6개 — `docs/oauth_계정로테이션_차용.md` §3-e) ⓑ모델 화이트리스트 = `sonnet`(소넷5·effort low·기본)/`opus-fast`(오퍼스5+`speed:"fast"`+`fast-mode-2026-02-01`) — ⚠**빠름 모드는 Opus 전용**(소넷5엔 없음 = 표준이 곧 빠른 축, claude-api 정본 실측) · 선택 = `data/prefs.ts`(로컬 단일 출처) + 설정 화면 상담 섹션 ⓒ**선반응 프리페치** = DosaChat `llmCache`(선택지 노출 시 미본 주제 5건 250ms 시차 발사 · 캐시 키 = 모델:주제 · 적중 시 LLM 대사 즉시 시작) ⓓ시스템 프롬프트 `cache_control` 배선(프리픽스 캐시 — ⚠소넷5 최소 1024토큰이라 짧으면 무음 미캐시).

25. **전역 배경 + 유리 스택(← Q.45, 260726)** — **화면 정본 갱신점(다음 세션 필독)**: ⓐ**배경은 부품 안에 가두지 마라** — `components/StageBackdrop.tsx`가 프레임 전체(9:16)를 덮는다. 스크롤 컨테이너 **밖**에 둬야 무대처럼 고정된다(안에 두면 대사를 따라 흐른다) ⓑ`ShopStage`는 `bare` prop = 무대 카드(배경·간판·유리 판) 소등 · 인물만. 전역 배경이 있는 화면은 **무조건 bare**(안 그러면 사진 위에 사진 = 카드 나열 = 운영자가 지목한 "투박함") ⓒ**인물 = 흐름 밖 절대 레이어**(268px, `mask-image` 하단 페이드) — 흐름에 두면 세로 예산에 눌려 176px가 한계였다 ⓓ**유리 스택은 바닥 붙박이**(원국→선택지→대사, 전부 `flex:0 0 auto`) — 대사창이 프레임 밖으로 잘리던 것 해소 ⓔ**대사 본문 `maxHeight:120` + 자동 스크롤** = 미연시 텍스트 상자 관례. 상자가 자라면 무대를 밀어 인물 얼굴을 덮는다(실측) ⓕ 레퍼런스 정본 = 원장 [A. 45] 채팅 보고(2026 UIUX 20건) — 우리 `.glass`는 이미 권장 수치와 합치(blur 26<30 · 1px 보더 · inset 림라이트).

26. **성향 3층 인계(← Q.44, 260726)** — 운영자가 다른 레포에서 가져온 페르소나·애니어그램 자산을 **이 레포에 심었다**(휘발 방지). ⓐ 원천 2문서 = `docs/inherited/YETA_페르소나x애니어그램_인사이트_v1.md`(YETA 캐릭터챗 설계 인사이트 — 4모델 정합·상처엔진·역행 비대칭·호칭 라더·리얼리즘 가드) + `docs/inherited/애니어그램_발췌지식집_v1.md`(강의 50편 정제 교재 전수 — 3분법·하모닉·통합분열·날개·`TYPES[]` 12필드). **둘 다 발췌본이지 정본이 아니다.** ⓑ **인계서 = `dosa-app/인계서_성향3층_v1.md`** — 증류 섹션이 바로 읽도록 옆에 뒀다. 골자: 기존 4모델(MBTI·애니어그램·Big5·애착)에 **다섯 번째 렌즈 = 사주 명식(혼의 기본값)**을 붙인다. 앞의 넷은 자기보고·작가 배정인데 **명식만 외부 입력에서 결정론으로 나온다** — 그래서 겹치는 게 아니라 바닥에 깔린다. ⓒ **다리는 이미 있다** = `app/src/data/enneaLens.ts`(십신→애니어그램 후보 결정론 가중표). 증류가 이어받을 지점 = 이 가중표의 **코퍼스 근거 보강**(큐 13-ⓐ와 동일 축). ⓓ **증류에 요청한 것** = 유닛당 성향 필드 6종(`desire`·`fear`·`dislikes`·언어장·위로처방·역행). 우선순위 = **일주 60(이미 증류 완료 → 필드만 얹으면 됨 = 최저비용 최대효과)** → 십신 10 → 상호작용 30키(큐 4). ⓔ **⛔ 금기(원천에서 가장 비싸게 검증된 것)**: **유형은 백스테이지, 무대엔 행동·말투만** — YETA 카드 12장에 MBTI·애니어그램 표기가 0이었다("난 INFP라서…" 하는 순간 인물이 죽는다). **⚠ 우리는 지금 위반 위험**: `chefs.ts`의 concept·PERSONA에 유형 표기가 그대로 있고 LLM 프롬프트로 합성된다 → 다음 파도에서 행동·말투로 번역하고 유형은 주석으로 내릴 것. 그 밖에 감정 결정론 금지(판정=LLM·환산만 코드) · 역행 비대칭(밝은 캐릭터일수록 낙차가 크다) · 호칭 라더("너"는 중립이 아니라 1단 냉각) · 비검증 모델 과신 금지. ⓕ **결정 대기 4건** = 유형 표기 제거 범위 · 말투 few-shot 허용 여부("예시는 쌓지마"가 세계관 예시만인지) · 성향 축 증류 착수 시점 · 캐릭터 4인 명식(엔진값 채택 vs 생년월일 역산).

27. **상담 손질 5건(← Q.51, 260727 · PR #138 머지)** — **다음 세션 필독**: ⓐ **시그니처 색 축이 생겼다** = `Chef.sig`(`chefs.ts`) — 값은 **오행 라벨 토큰 계승**(알카사르 수·단리아 토·홍화동녀 화·설향 동자 목 · 신규 색 0). 말풍선은 **한 구절만** 그 색으로 칠하고(결정론 우선순위 「」→연도→사주용어→첫 문장 · `withEmphasis`), **지문과 말줄기 끝말은 전경째** 칠한다(`Bubble`의 `lead`). ⚠ 폴백(첫 문장)은 **뒤에 문장이 더 있을 때만** — 안 그러면 말풍선 넷이 전부 색으로 덮여 강조가 배경이 된다(260727 실측). ⓑ **로그 스크롤 = 말풍선이 늘면 무조건 끝으로**(운영자 "항상 포커싱은 마지막으로"). 종전 「바닥 60px 안」 단일 조건은 긴 말풍선 한 통에 임계를 넘겨 그 뒤로 영영 안 따라갔다. 타이핑 중 축(되읽기 보호)은 그대로 살아 있다. ⓒ 스크롤바 = `YG.scroll`(잉크 12%) · 트랙·화살표 없음 · **접두 먼저·표준 나중**. ⚠ 헤드리스 크로뮴은 오버레이 스크롤바라 **막대 픽셀을 캡처 못 한다** — 이 축은 computed 실측으로 판정한다. ⓓ **헤더 = [뒤로][프사][이름][부제][교체][복주머니]** — 교체 버튼(`Pict.swap`)은 4인 순환(`nextChef`)이고 누르면 흔들림 + 배경 인물 교체 + 난입 대사(빗맞힘 교체와 같은 문법). 배경은 스크롤 컨테이너 **밖**이라 `Talk`이 따로 흔든다. 연타하면 지문이 겹쳐 쌓여서 **직전 줄이 같은 지문이면 건너뛴다**. ⓔ `Pict.pouch` 재작도 = **몸통 채움**(선만으로는 19px에서 정체 불명). ⓕ ⚠ **`.msd-shim`은 `background-clip: text`다** — 같은 요소에 배경·색을 주면 그 배경까지 글자 모양으로 잘려 안 그려진다. 유리는 **바깥 겹**에 둔다. ⓖ ⚠ **교체 버튼으로 홍화동녀가 무대에 오르면 담배 컷이 그대로 노출된다**(20-ⓓ 스토어 심사 축 · 23-ⓑ와 동일 축) — 운영자 판단 필요.

30. **스크롤 어포던스(← Q.54, 260727)** — ⓐ 스크롤바를 **되읽는 동안에만** 켠다: `reading` state(바닥에서 24px 밖) → 막대 색이 `YG.scrollOn`(**흰 14% = 무채색**, 운영자 "테마색에 영향 안 받고")로 켜지고, 붙어 있으면 `transparent`. ⚠ **폭은 늘 잡아 둔다**(`scrollbar-width: thin` 고정) — 보일 때만 자리를 만들면 그 순간 글자가 옆으로 밀린다. ⓑ ⚠ **「최신으로」 아래 화살표는 되돌렸다**(운영자 260727 "한 수 긍정적으로 반응해서 하라고 지시한 거 다 되돌려" — C1의 「한 수는 제안까지」 축). 다시 만들지 말 것. 같은 지시로 **복채 주머니 임시 반응도 철회** = 눌러도 아무 일 없는 '준비 중'(opacity .58 + aria-disabled)이 정본이다 — 운영자가 먼저 말한 "둘 다 누르면 비활성화가 일단 맞음"으로 복귀. 교체 버튼은 직접 지시분이라 그대로 동작한다.

29. **스크롤 정책 최종(← Q.53, 260727)** — ⓐ **로그 스크롤바 = 숨김**(운영자 "스크롤 내 생각엔 없어도 될 거 같음"). 스크롤 자체는 살아 있다. ⓑ **화면은 늘 마지막 말에 붙는다 — 예외는 손 하나**: `stick` ref가 「바닥에서 24px 안인가」만 보고, 손으로 올리면 떨어져 그동안 안 끌어내리며, 다시 바닥까지 내리면 도로 붙는다. ⚠ 앞서 두 번 어긋난 자리 — 「바닥 60px 안」 조건 하나로 두면 **긴 말풍선 한 통에 임계를 넘겨** 영영 안 따라가고, 말풍선 **수**만 보면 한 통이 타이핑되며 길어지는 동안 뒤처진다. 실측 4케이스(쌓기·손으로 올림·올린 채 새 말·도로 내림) 전부 통과. ⓒ 플레이그라운드 짧은 주소 = **`/reports/chat-playground.html`**(한글 파일명 사본과 동일 내용 — 한글 URL은 손으로 열기 어렵다).

28. **대화창 플레이그라운드 + 내 말풍선 유리화(← Q.52, 260727)** — ⓐ **플레이그라운드 정본 = `docs/reports/260727_대화창_플레이그라운드.html`**(폰 열람 사본 `app/public/reports/` · 라이브 `/reports/260727_대화창_플레이그라운드.html`). 5축 × 10안 = 표면·형태·강조·끝말 액션·등장 + 화자 4인 전환(시그니처 색·배경 컷이 같이 갈림). 값은 전부 앱 토큰 계승이고 **지금 앱 값에 '고정' 배지**가 붙어 있다 — 운영자가 고른 안만 코드로 옮기면 된다. ⓑ **내 말풍선 = 코발트 판 폐지, 도사와 같은 검정 유리**(운영자 260727 "나의 말은 검정색 불투명도 낮춘 거로 글래스모피즘으로 항상 · 지금 앱 강조색은 투박해서 흐름을 깬다"). 한 단 진한 알파(`YG.meBg` = 잉크 32%)로만 층을 가르고, **화자 구분은 자리와 꼬리**가 한다. 새 색 0. ⚠ 이 화면에서 `--c-primary`를 다시 꺼내 쓰지 말 것 — 무대 위에서 그 색만 앱 부품처럼 튄다.

31. **원본 반입 = 로컬 작업 소거 완료(← Q.54-b, 260727 밤 · PR #149 드래프트)** — **다음 세션 필독**: ⓐ `정제/원본/`(+`정제/P2_유닛`)에 재료 **7,166파일**이 들어왔다(반입기 가리기 = 메일 169·전화 5 마스킹 · 이진 26 건너뜀 · 대용량 생략 0 · 반입물 167MB). 검산 = 디스크 7,166 = git 7,166(조용히 먹힌 파일 0) · 반입물 이메일·인증 토큰·`_2차분류`·`figjam_board_raw` 전부 **0건**. **이제 정제 파이프라인은 어느 세션에서든 돈다** — 로컬 전용 단계가 없다. ⓑ 🔴 **운영자가 올린 분할 ZIP 53조각(각 19.5MB·총 1.02GB)에 암호가 없었다** — 인계문이 전제한 `7z -v90m -mhe=on -p암호`/`사주원본.7z.001`이 아니었고, 그 안의 **유튜브 로그인 쿠키 351KB**(Netscape Cookie 형식)·이메일 5,733건·저자 축자 발췌 278건·팀원 사주 실명 절이 **공개 리포에 그대로 올라갔다**. 커밋 `d9519a6`에서 HEAD 제거했으나 **blob은 히스토리에 잔존**(아래 사용자 액션 2건). ⓒ ⚠ **`/feed` 블라인드 격리에 구멍이 뚫렸다** — 답안지(영상 전사)가 `정제/원본/`에 실재하므로 학생이 grep으로 줍는다. `SKILL.md`에 「학생은 `정제/원본`·`정제/원본_압축` 접근 금지」가 필요한데 **SKILL.md는 평의회 대상**이라 세션이 임의로 못 고친다(23-ⓘ와 같은 축). ⓓ **리눅스 이전 부작용 1건 수선**: `정제/_현황판/build_neuron_map.py:314` `_lines_of()`가 NFD로 정규화한 파일명으로 `Path.exists()`를 부르다 **ENAMETOOLONG(errno 36)**을 그대로 맞고 파이프라인 ③단계를 죽였다(한글 NFD = 바이트 2배[125→263B] → 리눅스 상한 255B 초과 · 파이썬 3.11은 ENOENT·ELOOP만 삼킨다) → `try/except OSError`. **윈도우 NTFS에선 안 나던 종류다 — 경로·파일명 코드는 리눅스에서 한 번 더 밟아 볼 것.** ⓔ `파이프라인.py --check` = ①②단계가 「입력없음」에서 벗어남 · ③단계는 통과 · **`--all` 전 단계 완주는 미확인**(다음 세션 숙제). 인계문 정본 = `정제/★지금이어받기.md`.

## 상용화 로드맵 (사용자 결정 필요 — 세션이 임의 진행 금지)

| 축 | 내용 | 선행 조건 |
|---|---|---|
| LLM 서술 켜기 | Cloudflare Pages env `ANTHROPIC_API_KEY`(+선택 `DOSA_MODEL`) 등록 — 즉시 가동 | 사용자 등록 1회 |
| 계정·동기화 | 프로필이 기기 로컬뿐(계정 축 '없음') → Cloudflare KV/D1 + 로그인 | 방식 결정(이메일/소셜) |
| 결제·프리미엄 | 무료 = 리포트+대화 N회 / 유료 = 심층 리포트·무제한 대화 등 상품 설계 | 상품·가격·PG 결정 |
| 법무 | 이용약관 15개조·개인정보 처리방침 13조+부칙 v1 = **앱 내 열람 배선 완료**(← Q.58 · 260804 · `/terms`·`/privacy` · 정본 `app/src/data/legal.ts` · 운세 재미·참고 고지 = 약관 제5조 · YETA 260709 승인 골격 계승) — 계정 서버·분석·결제 도입 시 같은 작업에서 개정 필수 | 사업자 정보 · 운영자 법무 최종 검토 |
| 분석 | 방문·전환 지표(Cloudflare Web Analytics = 무료·쿠키리스 후보) | 도구 승인 |
| 브랜드 | 파비콘 = 템플릿 잔재(보라 번개) 계승 중 — 정식 아이콘/캐릭터 일러스트 | 사용자 제공([선택] 항목) |
| 콘텐츠·마이 탭 | BillyNav의 콘텐츠/마이 탭 = 미배선 | 기획 결정 |

## 사용자 액션 아이템 (세션이 대신 못 하는 것 — 잊지 말 것)

- [ ] **기존 Cloudflare "saju" 프로젝트 삭제/연결해제.** 웹 빌드 없던 시절 대시보드 설정 잔재라 커밋마다 빨간 X를 냄. 정상 배포처는 **"saju02"**(2026-07-17 배포 성공: claude-organize-extracted-fi.saju02-7n8.pages.dev). saju 지우면 노이즈 사라짐.
- [ ] **Cloudflare Pages(saju02) env에 `CLAUDE_CODE_OAUTH_TOKEN_<계정명>` 6개 등록**(MUTENO·NOMUTEFB·EMS1130G·MUTENONA·EMS1130M·EMS1130N) — **1순위 자격이 이걸로 바뀌었다**(← Q.43 · `functions/api/dosa.ts` CHAIN). 레거시 `ANTHROPIC_API_KEY`는 2순위 폴백으로만 남는다. ⚠**등록 전까지 라이브 상담은 100% L3 폴백**이라 연도 화법 교정·용어 선두 금지·페르소나 말투가 **전부 안 켜진다**(260726 감사 실측).
- [ ] **`GH_VARS_TOKEN` PAT(Variables: Read and write) 등록** → `account-selftest` 수동 런으로 sticky 승격 실측(← Q.42).
- [ ] **표정 시트 v2 생성 승인**(noona·baekui·dongja · 유료 · Actions dispatch) — 지금 v2 시트는 doryeong 1인분뿐이고 **그마저 알파가 없어**(밤 배경째) 무대에 못 올린다. 배선은 다 서 있고 **키잉된 컷만** 빠졌다(← Q.46-d).
- [ ] **캐릭터 위계 확정**(연리 ↔ 셰프 4인 = Q.16 미확정) — 이게 안 풀려 화면 문구가 이름을 못 쓴다.
- [ ] **`정제/원본_압축` blob 히스토리 완전 제거 여부(force push) 결정 — D2**(← Q.54-b) — 무암호 분할 ZIP 1.02GB가 `d9519a6` **이전 히스토리에 남아 있다**(HEAD에선 이미 빠졌다). 히스토리 재작성 + force push는 비가역이라 세션이 못 정한다.
- [ ] **유튜브 쿠키 무효화(전 사용 재발급)**(← Q.54-b) — 위 압축본에 실려 공개된 로그인 쿠키 351KB(Netscape Cookie 형식). **히스토리 제거보다 이게 급하다** — 지금 무효화하면 blob이 남아도 쓸모없는 값이 된다.
- [ ] (선택) 도사 캐릭터 컨셉·일러스트, 일진 다이어리 UI/UX 제공 → L5 연결. 정식 앱 아이콘 결정(현재 = 템플릿 파비콘 계승). **남신 도사(아이샤 짝) 시각 자산 1차 완성(← Q.17~19, 260721)**: 확정본 = **v3 밤 수채 표정 5세트** `app/public/reports/dosa-male-v3/`(라이브 `/reports/dosa-male-v3/` · 기준 얼굴 = v2 C안 face_c_night · v1 정통 5컷 = 기각 보존 · 재생성 레일 = Actions `gen-dosa-images` 수동 dispatch, 스크립트 v1~v3 = .github/scripts/) — **다음 = 셰프단 체제(260721 캐릭터 체제 결정 로그) 전제의 라인업 설계 + 역할·이름 확정**(v3 남신 = 기본캐·역할 미정 · 연리 관계 = Q.16 미확정 1건과 동조 · 프롬프트 정본 = `docs/20260721_141213_남신도사_페르소나_GPT이미지프롬프트_v1.md`[← Q.14]).

## 결정 로그 (왜 — 뒤집으려면 사용자 승인)

- **260726 자산 3축(운영자 확정) — "보존/삭제"가 아니라 "다시 만들 수 있나 · 얼마에"로 가른다.** 「이건 어디 넣지」가 안 생기는 유일한 축이다.
  | 축 | 뜻 | 처리 | 해당(실측) |
  |---|---|---|---|
  | **A. 다시 못 만듦** | 잃으면 끝 | 무조건 커밋 | 증류 결과·시험은행(`kb/distilled`·`kb/exams`) · 규범 문서(CLAUDE.md·STATUS·README·docs) · 앱/엔진 코드 · 운영자가 직접 준 것 |
  | **B. 재생성되나 비쌈** | 시간·돈·재수집 | 보존하되 **레포 밖 가능** | 유튜브 전사본 · 스크래핑 원본(docx 155·수집 json 54) · 캐릭터 이미지(≈$3.3) · KB 색인 3종 |
  | **C. 공짜 재생성** | 스크립트가 만듦 | **레포에 둘 이유 없음** | `unit_bodies.json` · `dist/` · `kb-*.json` · `vendor/kb_ref.json` · `youtube-tools/raw/` |
  - **C 정리는 이미 완료 상태**(위 5종 전부 `.gitignore` 등재 확인, 260726 실측). 남은 추적분 2건은 **뺄 수 없는 이유가 있어 유지**: ⓐ`vendor/*` = 빼면 `app/` 단독 빌드가 tsc 실패(바인딩이 명시한 경로) + verify 게이트2(드리프트 검사)의 검사 대상 소멸 ⓑ색인 3종(`unit_index`·`interaction_index`·`impression_index`) = **prebuild 체인에 생성 단계 없음**(`map_units.py`·`scan_interactions.py`·`scan_impressions.py` 별도 실행) → 빼면 fresh clone에서 verify 불가. **= C가 아니라 B다.**
  - **B의 유일한 예외 = 답안지는 레포 밖**(블라인드 격리). 근거 = `/feed` SKILL.md §23. **실측 증명(260726)**: zip은 파일명을 압축 없이 저장해 **261개 영상 제목이 평문 바이트로 노출** → `grep`/`strings` 한 번에 답안지 목록이 나온다(본문은 압축돼 안 샘). 조치 = `노뮤트_전사본_*.zip`·`_압축본_백업/`·`*_자막/` **추적 해제 + gitignore**(삭제 아님 — 디스크·히스토리 유지). **복구** = `git show 0e266c1:'노뮤트_전사본_20260718.zip' > 노뮤트_전사본_20260718.zip`. 주입은 경로 인자로 무관하게 돈다(`ingest_transcript.py <경로>`).
  - ⚠ **"저품질 구 전사본 삭제"는 조건 미충족 = 보류**: 자동자막 175편(멤버십 71 포함, 쿠키 런으로 회수)의 **재전사본이 아직 도착하지 않았다.** 지금 지우면 대체물 없이 소실 — 재전사본 도착 후 교체 삭제.
  - ⚠ **레포는 public**(260726 확인). 위 조치는 격리(시험 신뢰도) 축이고, 공개 노출 자체는 운영자가 "그럴 필요 없다"고 판단한 별개 축.

- **⚠260722 배포 구조(운영자 강제푸시 허용, ← Q.27) — 다음 세션 필독**: 프로덕션 = **Cloudflare Pages `saju02`**(도메인 `saju.soong.kr` = CNAME→saju02-7n8.pages.dev · `soong.kr` 루트는 별개 프로젝트 yeta). **프로덕션 브랜치 = `app-deploy`(main 아님!)** → `main` 머지분은 **Preview로만** 배포됨(main 브랜치 별칭 = `main.saju02-7n8.pages.dev` = 상시 최신 검수용). `app-deploy`엔 main에 없는 **이메일+비번 계정 시스템(Pages Functions + D1)** 커밋 5개 = 분기 → **함부로 강제푸시 금지**(파괴). 260722 운영자 허가로 main(18c8a14)→app-deploy 강제 동기화 = 명식당 프로덕션 라이브 확정. 구 app-deploy(계정 시스템) = **`backup/app-deploy-accounts-260722` 브랜치 보존**(복구 가능). **✅260726 해소(감사 실측)**: 라이브 `saju.soong.kr` 번들에 연식당·홍화동녀·알카사르가 실재 = **프로덕션 브랜치는 이미 `main`**이고 `origin/app-deploy`는 main보다 59커밋 뒤(마지막 = #102)로 **레거시**다. CLAUDE.md 【바인딩】(프로덕션 분기 = main)이 정본 — 이 문단의 옛 '미해결'은 그대로 두면 다음 세션을 오도한다.

- **260721 명식당 셸(운영자 목업 핸드오프, ← Q.23)**: 앱 화면 정본 = **운영자 제작 목업 v2**(design_handoff_명식당_앱.zip — 글래스 2단 blur 26/11 · 컨트롤 규격 h52/r14/칩 h36/서클 44 · 도형 4종 · 코발트 CTA 화면당 1개). ①라이트 단일 테마(다크 모드 폐지 — mode.tsx 강제 light, 토글 UI 제거·다크 CSS는 휴면 보존) ②로그인 = 기기 로컬 입장 플래그(`msd-entered-v1` — 계정 서버는 로드맵, 프로필 보유 재방문자·공유 딥링크는 비게이트) ③기본캐 플레이트 = v3 cut4_consult 최적화 사본 `app/public/assets/chef-default.jpg`(정본 원본 = `/reports/dosa-male-v3/` · **셰프단 골격(← Q.24) = `app/src/data/chefs.ts` 레지스트리** — 플레이트 규약 `chef-<id>.jpg`·전환 = `ACTIVE_CHEF_ID` 한 줄·표정 컷 경로 등재, 셰프 추가 = 파일+한 줄) ④총운 점수·월운 차트 = 엔진 미지원이라 비노출(고정 목업 금지 원칙 우선 — 엔진 지원 시 개방) ⑤미구현 기능 = '준비 중' 정직 표기(거짓 액티브 금지). 홈 원국표는 '내 원국' 화면으로 이동(SajuTable compact — 전체 7행은 리포트 유지).

- **260721 캐릭터 체제(운영자 구술, ← Q.20)**: 명식당 상담 캐릭터 = **셰프단 멀티 페르소나** — 구성 = 수셰프·셰프들, **셰프마다 컨셉·상담 스타일이 전부 다름**(운영자: "다 셰프들이 다른 컨셉이 되서 상담하는 스타일루"). 밤 수채 남신(v3, `/reports/dosa-male-v3/`) = **기본캐**(최종 역할 미정 — "쟤가 뭐가 될진 모르겠는데 지금은 기본캐"). 다크모드 전용 도사 제안(A.19 한 수) = 보류. 연리(주인장)·아이샤와 셰프단의 배치 = 운영자 확정 대기(아래 브랜드 확정 항목의 미확정 1건과 동일 축). 신규 캐릭터 생성 레일 = Actions `gen-dosa-images`(스크립트만 교체).

- **260721 브랜드 확정(운영자, ← Q.16)**: 서비스명 = **명식당**(命式堂/命食堂 이중 독해 — '명식을 차려주는 집') · 주인장 도사 = **연리**(緣理 '인연의 이치' + 連理枝 연상 · 동음 주의 = 年利). 오퍼스 5인 평의회 75안 중 선정(Q.15). **미확정 1건**: 연리 ↔ 기존 아이샤·남신 3안(무운·천겸·서월)의 관계(대체/병존/총칭) — 반영 파도 전 운영자 확인 필수. **카피 레지스터 2층 원칙**: 식당 은유(기본상·정식·겸상·곁들이·단골 장부)는 상품명·도사 대사·마케팅 층까지만 — 리포트 본문 섹션·근거줄은 전문 용어 유지(근거 병기 신뢰 축 보호). **대표 태그라인 확정(운영자 추인 14:46): 「당신의 운명을 위해, 오늘도 한 상.」** — 컨셉 = 매일 들러 일일 사주(일진) 한 상 받고 미래를 계획하는 집 → **일진 다이어리(작업큐 7) = 브랜드 핵심 루프 승격**.

- **260719 위계 원칙(운영자 확정)**: 메인 = 사주 원국 해석 · 에니어그램 = 보조지표(곁 신호) — 판정 프레임 금지. 정곡·L4에 유형 축을 얹을 때도 유지(뒤집으려면 운영자 승인).

- 신강신약 = 보드 110점제(천간각10, 지지 년15·월30·일15·시10; 30/45/60). 12신살=월일시지는 년지 삼합·년지는 일지 기준. 도화/역마/화개 기본=broad(왕지/생지/고지 글자).
- 일진 = (JDN+49)%60. 시주경계=진태양시(경도×4분). 자시 기본=정자시(23시 익일).
- 검색(RAG) 금지 — 키 결정론 조회만. 증류 인용=축자, 이견=관점차이 병기.
- 지식 주입 품질 게이트 = **블라인드 시험 루프**(/feed v2, 루트 CLAUDE.md 제1법칙): 영상=답안지, 질문 역추출→주입 전 1차 시험→격차 원인(부재/미발견/관점차이) 유추→정제 반영→새 블라인드 학생 재시험, 3라운드 실패 시 사용자 승격. 키 스캐너 판정은 전처리로 강등, **0단계 수집기는 판단 금지**(`fetch_transcripts.py` — 음성 전량 수집, NO_SUBS/INCOMPLETE/THROTTLED 구분·페이싱 내장) — 2026-07-17 사용자 설계.
- 유튜브 주입 정본 경로 = `ingest_transcript.py`(stage→fulltext+yt_units 멱등 등록) — `check_new_video --save` 주입 금지(score<2 무음 거부+색인 고아, 평의회 260717 발견①·박제 시트 실증) — 2026-07-17.
- 앱 엔진은 **벤더링**(정본 복사) — 손편집 금지, sync 스크립트로만. kb.json은 build:kb 생성물(gitignore).
- (260718 Q.08) **오늘의 운세 점수 = 엔진 일진 관계(합충형파해원진·공망)의 결정론 정책 매핑**(rubric = `app/src/engine/index.js` REL_SCORE, base 75·clamp 45~95) — 문헌 주장이 아니라 엔진 산출 요약 지표, UI는 근거 관계 병기 필수. 십신 유래 문안(SIPSIN_THEME/REL_PHRASE)도 정책 문안.
- (260718 Q.08) **시간 모름 = 정오(12:00) 대입**(일주 오판 불가 실증 — 보정 포함 경계까지 최소 10.5h) + 시주 의존 산출물(강약·십신분포·신살·합충·대운) UI 비노출·시주 열 '?' 표시. 절입일 출생 + 시간모름은 연·월주 확정 불가 한계 존재.
- (260718 Q.08) **대운 표기 연도 = 출생연도 + age**(만나이 관례) · 결과 딥링크 = URL 쿼리(y/mo/d/t/g/city/hu/sc/lz/n) · 세운 KB 번들 = 올해·내년 유닛만 본문부 56문단(과년도 8문단).
- (260718 Q.09) **노뮤트 전사본 3분할 게이트**(평의회 페이블 11+검증 2+오퍼스 10·F2 감사 반영 수치): 지금 주입 = **신규 Whisper 59편만**(오전사 정규화 매핑 전단 필수 — KB 키 사전 기반 자모 유사도 자동 채굴, '재고귀인→제고기인' 실측) · 대기 = 자동자막 175(재전사 재전달 시 — '십성→씹성' 축자 오염 차단) · 동결 = 기보유 61(교체는 zip=Whisper 24편 한정, 구본 스냅샷 병존+인용 단위 재바인딩+게이트4 통과 조건부) · 제외 = 오랑 판타지 6(사주 무관)·스텁 1. **답안지(zip·categories.tsv 원본) 레포 커밋 금지 = 블라인드 격리**(제목=답 유출 — 전략 문서에도 제목 축자·vid→주제 페어 미기재), 커밋은 제목 제거 파도계획 TSV만. `--channel`은 기존 등재명 "도화도르- 사주팔자 쉽게 풀어주는 남자" 강제(타 표기 = folk_etc 강등). **도화도레 = 도화로운(dohwaroun.com) 동일 저자(김정훈)** — KB 소스 라벨 = 도화로운 클러스터 병합(이중 카운트 금지).

## 새 세션 부팅 (다른 계정/모델 동일)

```bash
npm run verify   # 이 한 줄이 파생물 재생성 + 8게이트 전부 (통과하면 정상)
# 사주: /saju 1993-11-30 08:00 M 순천   |   지식주입: /feed <유튜브URL>
```

## 260718 밤 — 에니어그램 툴킷 편입 (외부 이식 · 운영자 지시 Q.11)
- `app/public/enneagram/` = 자기완결 오프라인 단일 HTML 웹앱(유튜브 강의 50편 증류 · 테스트/개인·그룹 분석/강의/교재 7뷰 · Vite public 통과 = 빌드 무접촉 → 라이브 `/enneagram/`). 원본 스냅샷 = yeulmaru-promo playground(유지) · 노뮤트 사본 = 제거(사용처 0 실측). 후속 계획 정본 = yeulmaru-promo `docs/reports/20260718_172604_애니어그램_노뮤트이식_추후계획_v2.md`(수용처만 사주로 치환).
- ⚠ 인계 주의: `enneagram_analyzer.html` 이름 렌더 4개소 무이스케이프(현재 자기-XSS 하급) — 서버 동기화·공유 링크 붙이기 전 **새니타이즈 필수**(v2 보고 R9) · 소스 수정 시 `python build/build_merge.py` 재빌드 필수(all.html 직접 편집 금지) · 사주 CLAUDE.md 구조 서술 등재는 차기 기틀 커밋 동승.
