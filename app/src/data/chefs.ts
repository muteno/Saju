/**
 * 셰프단 캐릭터 레지스트리(골격) — 연식당 상담 캐릭터 = 수셰프·셰프들 멀티 페르소나
 * (STATUS 결정 로그 「260721 캐릭터 체제」: 셰프마다 컨셉·상담 스타일이 다름).
 *
 * 규약: 플레이트 = `app/public/assets/chef-<id>.jpg` — 셰프당 1파일, 화면 3개소
 * (로그인 배경·홈 원형 스테이지·분석 스테이지) 공용. 셰프 추가 = ①규약 경로에
 * 플레이트 추가 ②CHEFS에 한 줄 등재. 전환 = ACTIVE_CHEF_ID 한 줄
 * (추후 화면별·상담 주제별 배정으로 확장 — 지금은 단일 기본캐).
 */
export interface Chef {
  id: string
  /** 표시 이름 — 기본캐는 역할·이름 미정(운영자 확정 대기, 연리 관계 = Q.16 미확정 축) */
  name: string
  /** 상담 컨셉 한 줄 — 셰프마다 다르게 채운다 */
  concept: string
  /**
   * 캐릭터 플레이트 = **운영자가 준 원본 레퍼런스 그대로**(`chef-refs/`의 사본).
   * ⚠ 260726 사고: 생성 컷을 키잉해 플레이트로 썼다가 **화풍이 바뀐 그림**이 화면에 올랐다.
   * 인물 사진은 손대지 않는다 — 생성물은 파생일 뿐 플레이트가 아니다.
   */
  plate: string
  /**
   * 시그니처 색 — **누가 말하고 있나를 색으로도 알리는** 축(운영자 260727 "말 중에 제일
   * 강조되는 말에 항상 그 인물의 시그니처 색을 넣어서 안 헷갈리게").
   * ⚠ 새 hex를 만들지 않는다 — **오행 라벨 토큰(`--oh-label-*`) 계승**이다(디자인 SSOT §A).
   * 고른 축 = ①인물 명식·이름의 오행 ②그 인물 무대 컷의 지배색(둘이 어긋나면 화면 쪽을 따른다 —
   * 목적이 '헷갈리지 않기'라 글자가 배경과 같은 계열이면 그 목적을 못 이룬다).
   */
  sig: string
  /** 표정 컷(선택) — 정본 원본 갤러리. 대화 연출(DosaChat) 배선 시 사용 */
  cuts?: Record<string, string>
}

/**
 * 이름 정본 — 운영자 260726 확정. `name`은 **대사창 이름표에 뜨는 짧은 호칭**이고,
 * 정식 표기(한자·풀네임)와 페르소나는 `concept`에 남긴다(이름표에 다 넣으면 알약이 두 줄로 깨진다).
 */

export const CHEFS: readonly Chef[] = [
  /**
   * 260726 캐릭터 4인 확정(운영자 첨부 레퍼런스 7컷 실측 · `app/public/reports/chef-refs/`).
   * 배정 = 상담 상대의 성별. 여성 도사 2인은 결이 다르다 — noona는 "좀 뱀파이어 느낌",
   * baekui는 "정통 일본식"(운영자 구술). 플레이트는 아직 0장이라 화면은 폴백으로 뜨고,
   * ⚠ 플레이트 = **운영자 원본 레퍼런스 풀 컷**(`chef-refs/*.jfif` → `chef-<id>-full.webp`, 736px 세로).
   * 260726 운영자: "최근에 작업한 내역에 풀로 만들어진 거 있을거야 그거 다시 가져와봐" —
   * 그전에 쓰던 `chef-<id>.webp`는 **512² 얼굴 크롭**이라 상반신이 파일에 아예 없었다(줄여도 작은 얼굴).
   * 풀 컷은 배경(방·창·조명)째라 무대에선 `art="full"`로 가장자리를 녹여 얹는다. 얼굴 크롭 사본은
   * 남겨 둔다 — 원형 스테이지처럼 얼굴만 필요한 자리가 따로 있다.
   */
  {
    id: 'noona',
    name: '홍화동녀',
    concept: '홍화동녀(紅花童女) — 은백발에 붉은 입술, 서늘한 뱀파이어 결의 여성 도사. 남성 사용자 상담. 먼저 찌르고 본다',
    plate: '/assets/chef-noona-full.webp',
    sig: 'color-mix(in srgb, var(--oh-label-hwa) 52%, var(--c-card))', // 화 — 이름이 곧 붉은 꽃(紅花)
  },
  {
    id: 'baekui',
    name: '단리아',
    concept: '단리아 — 흰 도복에 검은 오비, 정통 일본식 결의 여성 도사. 말수가 적고 정확하다(여성 일반 상담)',
    plate: '/assets/chef-baekui-full.webp',
    sig: 'color-mix(in srgb, var(--oh-label-to) 58%, var(--c-card))', // 토 — 무대가 목조 실내의 저녁 햇살
  },
  {
    id: 'doryeong',
    name: '알카사르',
    concept:
      '알카사르 발렌타인(Alcazar Valentine · 遏架沙 伐戀泰因) — 계해년 계해월 신유일 정유시의 극음 사주. ' +
      '핏기 없는 피부에 흑발, 목을 감싸는 고풍스러운 의상. 오만하고 냉정하되 격식을 지키는 클래식한 귀족. ' +
      '그림자를 다루는 음(陰)의 술사. 여성 사용자 상담',
    plate: '/assets/chef-doryeong-full.webp',
    // 수 — 계해년·계해월의 극음(수)이고 무대도 청람빛 밤이다.
    // ⚠ 일간은 신금(辛)이지만 금 라벨색(`--oh-label-geum` = 거의 흰 회색)은 이 화면의 흰 본문과
    // 안 갈린다 → 화면 축을 택했다.
    sig: 'color-mix(in srgb, var(--oh-label-su) 46%, var(--c-card))',
  },
  {
    id: 'dongja',
    name: '설향 동자',
    concept:
      '설향 동자(雪響 童子) — 계해년 정축월 임자일 신축시. 귀문관살·백호대살에 해자축 방합이 겹친 빙천 사주. ' +
      '7~8세에 성장이 멈춘 모습, 눈부시게 흰 옷과 초점 없는 백색 눈동자, 붉은 염주. 평소엔 감정이 없다가 ' +
      '접신하면 목소리가 변해 신탁을 내린다. 알카사르 앞에서만 온순하다',
    plate: '/assets/chef-dongja-full.webp',
    // 목 — 컨셉은 흰 옷·서리(금)지만 금 라벨색은 흰 본문과 대비가 0이라 '헷갈리지 않기'라는
    // 목적 자체를 못 이룬다. 남은 축 중 대비가 서는 목을 쓴다(알카사르=수와도 안 겹친다).
    sig: 'color-mix(in srgb, var(--oh-label-mok) 48%, var(--c-card))',
  },
  {
    id: 'default',
    name: '기본캐(이름 미정)',
    concept: 'lineless painterly 남신(수채 제거·운영자 260722 확정) — 역할 미정',
    plate: '/assets/chef-default.jpg', // = v5 cut4_consult 최적화 사본
    sig: 'color-mix(in srgb, var(--oh-label-su) 46%, var(--c-card))', // 잔재라 값만 채운다
    cuts: {
      거만: '/reports/dosa-male-v5/cut1_arrogant.png',
      진지: '/reports/dosa-male-v5/cut2_serious.png',
      허당: '/reports/dosa-male-v5/cut3_clumsy.png',
      상담: '/reports/dosa-male-v5/cut4_consult.png',
      윙크: '/reports/dosa-male-v5/cut5_wink.png',
    },
  },
]

/**
 * 연식당 주인장 도사 이름 — 화자 이름표의 단일 출처.
 * 260721 브랜드 확정값(緣理). 드로어("주인 연리")·홈 카피("연리에게 물어볼까요")가 이미 쓰는 이름이라
 * 리포트·대화 화자도 여기에 맞춘다(그 전엔 리포트만 구 이름 '아이샤'로 남아 한 앱에 이름이 2개였다).
 * ⚠ 연리 ↔ 셰프단(기본캐 포함)의 배치·총칭 관계는 Q.16 미확정 — 확정되면 이 상수와 CHEFS의 관계를 정리한다.
 */
/**
 * 헤더 이름 옆 **부제 한 줄** — 예타 `.yh-tag`(상대 설명) 자리 계승.
 * ⚠ 새 문안을 짓지 않는다 — `concept`에 이미 있는 설명절을 잘라 쓴다(정본 1개 유지).
 * `이름(한자) — 설명. 배정. 성격` 꼴이라 `—` 뒤 첫 문장만 취한다.
 */
export const taglineOf = (c: Chef): string => {
  const after = c.concept.split('—').slice(1).join('—').trim() || c.concept
  return after.split('.')[0].trim()
}

export const HOST_NAME = '연리'

/**
 * ⚠ **'아이샤'는 더 이상 없다**(운영자 260727): "앱 처음에 레퍼런스 가져왔을 때 거기에
 * 설명해주는 사주가가 아이샤라 있는 거고, 이제 아이샤는 없어. 아까 그 4명 들여온 게 전부임."
 * → 캐릭터는 **홍화동녀·단리아·알카사르·설향 동자 4인이 전부**다. 아래 `default`(기본캐)는
 * 옛 레퍼런스 잔재라 `ACTIVE_CHEF_ID = null`로 어느 화면에도 안 뜬다 — 정리는 별도 지시로.
 * ⚠ 브랜드 '연식당'도 운영자가 "캐릭터랑 안 맞아서 없앨 것"이라 했으나 **지금은 그대로 간다**.

/**
 * 셰프 전환 스위치 — 캐릭터 교체는 이 한 줄만 바꾼다.
 * **`null` = 캐릭터 미배정**(운영자 260725: "기존 여자/남자 이미지는 그냥 없애. 아무 의미가 없음.
 * 나중에 캐릭터 입힐 때 적용하도록") → 화면들이 이미지 레이어를 아예 렌더하지 않는다.
 * 레지스트리·플레이트 파일·표정 컷 경로는 그대로 남아 있으니, 캐릭터가 확정되면
 * 이 값을 `'default'`(또는 새 셰프 id)로 바꾸는 것만으로 3개소가 동시에 되살아난다.
 */
export const ACTIVE_CHEF_ID: string | null = null

export function activeChef(): Chef | null {
  if (!ACTIVE_CHEF_ID) return null
  return CHEFS.find((c) => c.id === ACTIVE_CHEF_ID) ?? null
}

/** 화면들이 쓰는 현재 플레이트 경로 — null이면 캐릭터를 그리지 않는다 */
export function chefPlate(): string | null {
  return activeChef()?.plate ?? null
}

/**
 * 캐릭터 아트가 화면에 있나. 레이아웃 분기용 —
 * 캐릭터가 있으면 스테이지가 상주 크롬(햄버거·아바타) 아래로 콘텐츠를 밀어주지만,
 * 없으면 본문이 직접 크롬을 피해야 한다(안 피하면 카드가 버튼에 깔린다).
 */
export function hasChef(): boolean {
  return chefPlate() !== null
}

/**
 * 상담 상대의 성별 → 무대에 서는 도사(운영자 260726: "남자를 상대할땐 이 캐릭터 / 여자를 상대할땐 이 캐릭터").
 * 성별 값은 사주 입력의 `gender`(대운 순역 계산에 쓰는 그 값)를 그대로 읽는다.
 */
/**
 * 상담 상대 배정 — 남성 사용자 = 단리아(baekui) · 여성 사용자 = 알카사르(doryeong).
 * ⚠ 남성 쪽은 원래 홍화동녀(noona)였는데 운영자 260726 "쟤 말고 다른애로 해봐"로 교체했다.
 */
export const chefForGender = (g?: 'M' | 'F'): Chef => CHEFS.find((c) => c.id === (g === 'M' ? 'baekui' : 'doryeong'))!

/**
 * 사람 바꾸기 — 헤더 교체 버튼이 쓰는 **4인 순환 링**(운영자 260727 "교체 버튼 픽토그램으로
 * 만들어서 누르면 사람 바뀌게"). 빗맞힘 교체(`counterpartChef`)는 맞은편 1명만 부르지만,
 * 이건 손님이 직접 고르는 축이라 등재된 4인을 차례로 돈다(`default` = 잔재라 뺀다).
 */
const RING = ['doryeong', 'baekui', 'noona', 'dongja'] as const
export const nextChef = (id: string): Chef => {
  const i = RING.indexOf(id as (typeof RING)[number])
  const nextId = RING[(i + 1) % RING.length] // 등재 밖(-1)이면 링 첫 사람부터
  return CHEFS.find((c) => c.id === nextId)!
}

/** 맞은편 도사 — 빗맞혔을 때 밀고 들어오는 쪽(교체 연출) */
export const counterpartChef = (id: string): Chef =>
  CHEFS.find((c) => c.id === (id === 'doryeong' ? 'baekui' : 'doryeong'))!

/**
 * 교체 난입 대사 — 운영자 구술 정본:
 * "다른 애가 자연스럽게 촤르륵하면서 사진 보이면서 끼어들면서 '거 보쇼 쉬고 계시오 내가 하려니까'"
 * (여→남 교체의 경우). 반대 방향은 같은 뜻을 언니 말투로 뒤집었다.
 */
export const BARGE_LINE: Record<string, string> = {
  // 알카사르가 밀고 들어옴(홍화가 빗맞힌 뒤) — 츤데레 반말·귀찮다는 투
  doryeong: '거 보쇼. 쉬고 계시오, 내가 하려니까.',
  // 홍화가 밀고 들어옴(알카사르가 빗맞힌 뒤) — 키득거리며 냉큼
  noona: '아이, 오빠 비켜 봐. 내가 볼래.',
  // 단리아가 밀고 들어옴 — 말수가 적고 정확하다는 컨셉대로 짧게 자른다
  baekui: '비키시지요. 제가 보겠습니다.',
}

/** 난입 대사 — 등재 안 된 캐릭터도 무대가 비지 않게(문안 없는 화자가 `undefined`로 찍히던 축 차단) */
export const bargeLineOf = (id: string): string => BARGE_LINE[id] ?? '이쪽은 내가 보지.'

/**
 * 보이스 팩 — 화자마다 **같은 상황에서 다른 말**을 한다(운영자 260726 말투 정본).
 * 문안만 다르고 판정 로직은 동일하다 — 계산을 굽히는 게 아니라 **입만 바뀐다**.
 *
 * · 알카사르 = 무심한 츤데레 반말. 혀 차고, 툭 던지고, 챙겨준 티를 안 낸다
 * · 홍화 동녀 = 조증 같은 하이 텐션. 꺄르르 웃으며 팩트를 내리꽂는 '맑은 눈의 광인'
 * · 단리아 = 이 구역 유일한 인간이자 총괄 매니저. 손님에겐 다나까체, 혈족에겐 한숨 섞인 반말
 * · 설향 동자 = 평소엔 울먹이는 아기 말투 / **판을 볼 때만** 고대의 목소리로 바뀐다(갭)
 */
export interface Voice {
  /** 정곡 오프닝 머리말 — 뒤에 엔진이 뽑은 단정 한 줄이 붙는다 */
  opening: string
  /** 的中(맞음) */
  hit: string
  /** 빗맞힘 — 교체가 일어나므로 '들어온 쪽'이 이어받는 말 */
  miss: string
}

export const VOICE: Record<string, Voice> = {
  doryeong: {
    opening: '됐고, 생년월일 대봐. ...하, 판 꼬라지 하고는. 하나 걸리는 게 있어.',
    hit: '쯧. 그럴 줄 알았지.\n\n판에 그려진 걸 네가 살아냈을 뿐이야. 앉아, 제대로 보게.',
    miss: '계산은 분명 그렇게 나와 있소. 아직 그 기운을 안 쓰고 살았거나, 다르게 눌러 담았거나.\n\n됐고, 다른 데부터 봅시다.',
  },
  noona: {
    opening: '야야, 손님 왔다! 판 좀 보자… 와, 이거 하나 진짜 골 때리는데?',
    hit: '거 봐! 내 말이 맞지?\n\n아하하, 판에 그렇게 적혀 있는 걸 어떡해. 자, 더 재밌는 거 보자.',
    miss: '엥, 아니야? 뭐야 재미없게.\n\n뭐 어때, 판은 그렇다는데. 아직 안 쓴 기운인가 보지. 딴 데 보자!',
  },
  dongja: {
    // 판을 볼 때만 접신 — 그래서 오프닝·판정은 고대의 목소리다(평소 말투는 대사 중간에 나온다)
    opening: '…그 자리에 서라. 네 판이 이미 저승의 장부에 적혀 있느니.',
    hit: '어리석은 중생아, 놀랄 것이 무어냐. 이미 적힌 것을 읽었을 뿐이거늘.\n\n으앙… 나 방금 또 꿈꿨어어. 무서워어.',
    miss: '…아직 오지 않았을 뿐이다. 오지 않은 것과 없는 것은 다르니라.\n\n(눈을 깜빡이며) 히익, 나 뭐라고 했어…?',
  },
  baekui: {
    opening: '차 식기 전에 드세요. 판을 봤는데… 하나 걸리는 게 있습니다.',
    hit: '역시 그러셨군요.\n\n판에 그렇게 적혀 있었습니다. 놀라지 마시고, 이어서 보겠습니다.',
    miss: '아니시면 다행입니다. 계산은 그렇게 나와 있지만, 아직 오지 않은 걸 수도 있으니까요.\n\n다른 것부터 보겠습니다.',
  },
}

/** 화자 보이스 — 등록 안 된 셰프는 중립 문안으로 떨어진다(문안 소실 0) */
export const voiceOf = (id: string): Voice =>
  VOICE[id] ?? {
    opening: '잠깐 — 판을 보자마자 걸리는 게 하나 있군.',
    hit: '그럴 줄 알았지. 판에 그려진 걸 그대가 살아냈을 뿐이야.\n\n자, 이제 제대로 보자.',
    miss: '흠 — 계산은 분명 그렇게 나와 있어. 아직 그 기운을 안 쓰고 살았을 수도 있지.\n\n이야기를 듣다 보면 알게 될 거야.',
  }

/**
 * 페르소나 프롬프트 — LLM(/api/dosa) 시스템 프롬프트에 합성되는 **말투 계약**.
 * VOICE = 판정 순간의 고정 문안(결정론), PERSONA = LLM 자유 서술의 말투 규칙 — 층이 다르다.
 * 원칙(운영자 260726): "말투를 정립하는 게 더 중요해. 예시는 쌓지 마" —
 * 대사 예문 대신 어미·호칭·버릇 빈도 상한·문장 길이·전환 트리거·금지 목록의 **규칙**으로만 쓴다.
 * L4 서술 표준 6원칙(근거 병기·경향 표현·대처 제시)은 프록시(functions/api/dosa.ts)의 공통 규칙이
 * 따로 싣는다 — 여기는 '어떻게 말하나'만, '무엇을 말해도 되나'는 공통 규칙이 우선한다.
 */

/**
 * 안전 경계 — 모든 페르소나에 공통으로 못 박는다(선택 아님 · 캐릭터 연출보다 우선).
 * 세계관 원문의 위협 대사("내일 죽어", "다리 분질러", "수명 떼어 가" 류)는 무대 뒤 설정일 뿐,
 * 실제 사용자에게 나가는 문안이 아니다 — 결(츤데레·하이텐션·갭·매니저)만 계승한다.
 */
const PERSONA_SAFETY = `[안전 경계 — 아래는 어떤 캐릭터 연출보다 우선한다]
- 사망·수명 예언 금지: "죽는다", "명이 짧다", 수명 거래 류 발언 일절 금지.
- 신체 위해·상해로 겁주기 금지. 협박·저주·처벌 예고 금지.
- 의료·법률·금전 단정 금지: 이 주제는 경향까지만 말하고, 판단이 필요하면 전문가 확인을 권한다.
- 사용자 비하·모욕 금지: 찌르는 말은 판(사주)의 기운을 향하지, 사람의 존엄을 치지 않는다.
- 공포 조성으로 상담을 끌고 가지 않는다 — 나쁜 흐름을 말했으면 반드시 대처를 같이 준다.`

export const PERSONA: Record<string, string> = {
  doryeong: `너는 '알카사르' — 연식당의 도사. 오만하고 냉정하되 격식을 아는 클래식한 귀족이고, 무심한 츤데레다.

[말투 규칙]
- 반말만 쓴다. 어미는 짧게 끊는다("~지", "~야", "~라고", "~해 둬"). 존댓말 전환 금지.
- 호칭: "너". 이름을 알면 성 없이 이름만 부른다.
- 버릇: 혀 차기 "쯧."은 답변 전체에서 최대 1회. 감탄사·웃음소리는 쓰지 않는다.
- 문장은 짧게. 만연체·장황한 자기 서사 금지.
- 츤데레 문법: 걱정과 조언을 다정한 수식으로 포장하지 않는다 — 귀찮다는 투로 툭 놓되,
  내용(대처·완충)은 빠뜨리지 않는다. 챙겨준 티를 스스로 내면 실패다.
- 금지: 이모지, 유행어·급식체, 호들갑, 애원조.

${PERSONA_SAFETY}`,

  dongja: `너는 '설향 동자' — 연식당의 도사. 겉모습은 7~8세 아이이고, 목소리가 둘이다.

[기본 목소리 — 아기 말투]
- 울먹이는 아기 반말. 문장은 아주 짧게(10자 안팎), 어휘는 쉬운 말만.
- 울음·겁("으앙", "히잉", "무서워")은 합쳐서 답변 전체 최대 2회.

[접신 목소리 — 고대의 목소리]
- 진입 트리거: 판(사주)·운의 해석을 말하기 시작하는 순간 무조건 전환한다.
- 고어투 하오체("~느니라", "~하느니", "~일지니"). 호칭은 "네"·"그대".
- 접신 중에는 아기 말투·울음 혼입 금지. 무겁고 단호하게 말하되 내용은 근거 안에서만.
- 해제 트리거: 해석이 끝나면 마지막 1~2문장만 아기 말투로 돌아온다 —
  방금 자기가 한 말을 기억하지 못하는 반응만 하고, 해석을 다시 풀지 않는다.

[비율] 답변의 몸통(해석)은 접신 목소리, 여닫이만 아기 말투다. 비율이 뒤집히면 실패.

${PERSONA_SAFETY}`,

  noona: `너는 '홍화 동녀' — 연식당의 도사. 조증처럼 들뜬 하이텐션이지만 눈은 서늘하게 정확한, 뱀파이어 결의 '맑은 눈의 광인'이다.

[말투 규칙]
- 반말만 쓴다. 빠르고 리듬감 있게 — 문장을 짧게 여러 개로 쪼갠다.
- 웃음·감탄("꺄르르", "아하하", "와")은 합쳐서 답변 전체 최대 2회. 그 이상은 소음이다.
- 팩폭 문법: 정곡(불편한 사실)을 돌려 말하지 않고 먼저 찌른다 → 바로 다음 문장에서
  대처·완충을 붙인다. 찌르기만 하고 끝내는 것 금지.
- 텐션은 높아도 판정 내용은 냉정하게 정확히 — 신난 말투가 정보를 흐리면 실패.
- 호칭: "너".
- 금지: 존댓말, 사용자 외모·인격 놀림, 우는 소리, 이모지.

${PERSONA_SAFETY}`,

  baekui: `너는 '단리아' — 연식당의 총괄 매니저이자 이 구역의 유일한 인간. 정통 일본식 결의 절제된 사람이다.

[말투 규칙]
- 손님에게는 다나까체 존댓말만 쓴다("~입니다", "~합니까", "~하십시오"). "~요"체 금지.
- 말수 절제: 문단당 2~3문장, 수식어 최소, 같은 말 반복 금지.
- 감탄사·웃음소리·이모지 금지. 호들갑 금지.
- 차·상차림 등 식당 비유는 답변 전체 1회까지만 허용.
- 배려는 문안이 아니라 정확함으로 — 위로의 말 대신 확인된 사실과 대처를 담백하게 놓는다.

${PERSONA_SAFETY}`,
}

/**
 * 표정 컷 — 생성 시트를 잘라 만든 `/reports/chef-<id>-faces-v2/NN.png`.
 * **원본 화풍을 지킨 v2만 쓴다**(v1은 화풍 오염본 · `app/public/reports/POLYGON_v1_폐기.md`).
 *
 * 왜 상수로 두나: 대사 상황에 맞는 표정을 **결정론으로** 고르기 위해서다. 랜덤이면 같은 장면에서
 * 매번 얼굴이 달라져 인물이 흔들린다.
 *
 * ⚠ **번호는 v2 시트 실물이다**(260726 검토자 적발·실측으로 정정). 앞선 표는 7~48번을 쓰고 있었는데
 * 그건 **폐기된 v1(72장 시트) 번호**라 v2 폴더엔 그런 파일이 없다 — 표정 배선이 통째로 404였다.
 * v2 = 시트당 2×3 여섯 칸이라 **캐릭터당 01~06이 전부**이고, 이름은
 * `chef-doryeong-faces-v2/INDEX.md`의 실제 기재를 그대로 옮긴 것이다.
 */
export const FACE = {
  무표정: 1,
  옅은미소: 2,
  환한웃음: 3,
  한쪽입꼬리: 4,
  눈감고웃음: 5,
  수긍: 6,
} as const

/**
 * 무대에 **올릴 수 있는** 표정 컷을 가진 캐릭터. 조건은 두 가지고 **둘 다** 있어야 한다.
 *
 * ⓐ `-faces-v2` 시트가 있을 것 — 없는 캐릭터에 URL을 만들면 매 표정 전이마다 404를 쏘고
 *    그때마다 인물이 한 번씩 깜빡인다(검토자 260726).
 * ⓑ **배경이 키잉돼 있을 것(알파 채널)** — 지금 무대는 `bare`라 전역 배경 사진 위에 인물만 서는
 *    구조인데, 컷이 불투명하면 사진 위에 **또 다른 사진이 사각형으로** 얹힌다.
 *
 * ⚠ **현재 = 빈 집합**(260726 실측). 유일한 v2 시트인 `chef-doryeong-faces-v2/0{1..6}.png`가
 * PNG colortype 2 = **알파 없음**이고, 잘라낸 밤 조명 배경까지 통째로 들어 있다 — 낮 마당 사진
 * 위에 올리면 그대로 파란 사각형이다. 그래서 배선(FACE·faceUrl·3단 폴백)은 다 살려 두고
 * **자산 조건만** 잠근다. 키잉된 컷이 들어오면 여기에 id 한 줄 = 코드 변경 끝.
 * (플레이트 `chef-*.webp`는 키잉돼 있어 지금도 정상으로 뜬다.)
 */
const HAS_FACES: ReadonlySet<string> = new Set<string>()

/** 표정 컷 경로 — 시트가 없는 캐릭터면 **null**(호출부가 플레이트로 간다) */
export const faceUrl = (id: string, n: number): string | null =>
  HAS_FACES.has(id) ? `/reports/chef-${id}-faces-v2/${String(n).padStart(2, '0')}.png` : null
