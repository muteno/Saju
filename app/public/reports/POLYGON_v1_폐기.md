# ⛔ `chef-*-faces-v1` · `-poses-v1` · `-bg-v1` = 폐기본 (쓰지 마라)

260726 1차 생성분이다. **화풍이 오염돼 있다** — 생성 프롬프트에 아트 스타일을 글로
지시해서("또렷한 라인아트 + 평평한 셀 채색") 모델이 첨부 레퍼런스보다 그 문장을 따랐다.

실측(원본 대비): 선 강도 **+117~177%** · 굵은 선 비율 **+6~12%p** · 상위 8톤 점유
**+21~46%p**(그라데이션이 계단으로) · 색 수 −23~−44%. 설향은 **좌우 눈 색이 다른 설정**이
소실됐고, 알카사르는 보랏빛 실금 문양과 밤 조명 배경이 날아갔다.

격자도 어긋났다 — 5×3으로 잘랐는데 모델이 4×3으로 그린 시트가 12장이라
**생성 304컷 중 244컷(80%)이 어긋난 조각**이다.

## 쓸 것 = `-v2`

운영자 판정 260726: *"이정도면 합격이고, 스타일이 항상 이렇게 나올거니까 새로 나온거만 써."*

v2 = `input_fidelity: high` + 2×3 6칸(512 네이티브) + 자르기 전 격자 검증.
파일럿 실측: 선 −9% · 굵은 선 −0.4%p · 채도 +3% — 원본과 사실상 같은 화풍.

## 왜 안 지우나

재생성에 돈이 든 산출물이고, **다음에 같은 실수를 했는지 재판정할 대조군**이라 남긴다.
앱·문서 어디서도 참조하지 않는다. 정리하고 싶으면 운영자 지시로 폴더째 삭제하면 된다.
