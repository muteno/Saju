# 명식당 앱 UIUX 보완 청사진 v1

> 지시: [Q. 30] 2026-07-25 17:20 — "지금 사주 앱 흐름을 uiux가 완전 깨져있는데 어떻게 보완할지 청사진을 좀 세워줘"
> 근거: 전 11화면을 헤드리스 실렌더([E1]) + 픽셀 실측([E13]) — 추정 0건. 영수증 = `app/public/reports/20260725_172039_uiux-blueprint_v1/`
> 성격: **청사진(계획)** — 이 문서는 코드를 바꾸지 않는다. 실행 = 파도별 GO 후.

## 0. 한 줄 진단

깨진 건 취향이 아니라 **세 가지 구조**다: ① 라이트 단일 테마 전환 때 **가독 스크림이 고아가 된 회귀** ② `/result`가 앱 셸 밖에 있어 생긴 **막다른 흐름** ③ 화면마다 제각각 자란 **컨트롤 문법 드리프트**. 앞의 둘은 지금 당장 고칠 수 있고, 셋째는 게이트로 못을 박아야 재발이 멈춘다.

## 1. 실측 영수증 (전부 재현 가능 · `?qa=1` 대표데이터)

렌더 조건: 루트 `npm run build` → `app/dist` 서빙 → Chromium headless 390×844, `?qa=1&view=<라우트>`. 대표데이터 = 최장명 14자·23:59 야자시 경계·1999/12/31.

| # | 화면 | 실측값 | 판정 |
|---|---|---|---|
| A1 | `/result` | `--stage-scrim` = **`transparent`** · 플레이트 = `aisha.png` · 플레이트 위 텍스트 노드 **65개** | 가독 붕괴 |
| A2 | 하단 알약 네비 | 배경 `rgba(255,255,255,.28)` · blur 11px · 뒤 본문 텍스트 투과 **3건**("병오년의 환경 변화" 등) | §3-c 위반 |
| A3 | 홈 히어로 | 월블록 y=155 · 이름블록 y=111(2줄, h99) → **이름이 43px 위로 침범** | 레이아웃 붕괴 |
| B1 | `/result` | `MyeongShell` 미사용 = 하단 네비 **없음** · 이탈로 `‹ 홈` **1개** | 막다른 길 |
| B2 | 네비 | 하단 탭 **4** vs 드로어 메뉴 **5** — `내 원국`이 탭에 없고 홈 세그먼트로만 진입 | 문법 2중 |
| B3 | `/result` 진입 | Home 서클 · Home 칩 · Analysis CTA = **3경로**가 같은 화면으로 | 층위 불명 |
| B4 | 헤더 | 햄버거·아바타 = 셸 absolute(고정) / 세그먼트·상태바 = 스크롤 콘텐츠 → 스크롤 시 **세그먼트만 소멸** · 중앙Y 72 vs 74.5 = **2.5px** 이탈 | 레이어 분열 |
| B5 | 캐릭터 | 드로어 "주인 연리" / 홈·분석 `chef-default.jpg`(남신) / 결과 `aisha.png`+"아이샤"+🐴 = **이름 3 · 얼굴 2** | 정체성 분열 |
| C1 | 전역 | 문자 도형 `‹ ↗ ▼ ×` + 이모지 `🐴 🎴 🍀` 잔존(🎴 = 렌더 시 빨간 사각형) | §2 위반 |
| C2 | `/fun` | 한 리스트에 '준비 중' 배지 3 + 셰브론 `›` 1 혼재 · 비활성 dim 없어 눌러보게 됨 | §5 위반 |
| C3 | 폼 | 로그인 = 라벨 없이 placeholder / 가입·입력 = 섹션 라벨 | 문법 2종 |
| C4 | `/settings` | 프로필 카드 이름 중심Y **197** vs 수정 버튼 **206** = **9px** 이탈 | [E8] 위반 |
| C5 | `/settings` | 저장칩 249px + 추가칩 66 = 315 / 가용 350 → **여유 2자**, 16자 이름에서 붕괴 | 오버플로 임박 |
| C6 | `/input` | 마지막 섹션(보정값 적용)이 스크롤 클립 경계 아래로 잘림 — 라벨만 빼꼼 + 스크롤 힌트 0 | "여기가 끝"으로 오독 |
| C7 | `/result` 대운 레일 | 첫·마지막 항목 반쪽 잘림("001"·"52") · 페이드·마스크 없음 | 잘린 인상 |
| C8 | `/settings` | `--c-solar`(#e85e5e = **양력 의미색**)를 로그아웃 destructive로 재사용 | 의미축 충돌 |

### A1이 왜 생겼나 — 이게 청사진의 출발점
`--stage-scrim`은 260718에 "다크 스테이지 가독"으로 도입돼 **다크 전용**으로 값이 박혔다(`:root` = `transparent`, `[data-theme=dark]` = `rgba(10,10,14,.55)`). 그 뒤 260721에 **다크 모드가 폐지**되면서 라이트만 남았고 — 스크림은 영구히 `transparent`가 됐다. 즉 **고쳐놨던 것이 테마 축 변경으로 되살아난 회귀**다. 지금 값만 바꿔 고치면 다음 축 변경에서 또 되살아난다. 그래서 파도 0과 함께 **게이트(§4)가 반드시 붙어야 한다.**

## 2. 보완 청사진 — 4파도

파도마다 커밋을 쪼개 `git revert` 한 줄로 되돌릴 수 있게 한다(방식론 §8).

### 파도 0 — 가독 복구 (P0 · 사용자가 "깨졌다"고 느끼는 그것)
| ID | 할 일 | 손댈 곳 |
|---|---|---|
| W0-1 | 스테이지 스크림을 라이트에도 부여 — 텍스트가 적층되는 구간만(전면 딤 금지: 캐릭터를 죽이지 않는다) | `index.css` `--stage-scrim` · `CharacterStage.tsx` |
| W0-2 | 알약 네비·상단 유틸 유리를 "충분히 불투명 + 채도 차단"으로(§3-c) — blur는 유지 | `MyeongShell.tsx` PillNav·유틸 버튼 |
| W0-3 | 홈 히어로 날짜·이름을 2줄 이상에서 안 깨지는 구조로(현재 `alignItems:flex-end` 한 행 = 줄바꿈 시 침범) | `Home.tsx` 247–253 |

기존 토큰만 쓴다 — 새 색·새 값 창작 없음([4]).

### 파도 1 — 흐름 봉합 (P1 · "흐름이 깨졌다"의 실체)
| ID | 할 일 | 비고 |
|---|---|---|
| W1-1 | `/result`를 `MyeongShell`에 편입 → 하단 네비 상주, 막다른 길 해소 | 즉시 가능 |
| W1-2 | 정보구조 단일화: 하단 탭 5개 확장 **or** 세그먼트 폐지 | **D2 결정 필요** |
| W1-3 | 분석 ↔ 결과 층위 정리: 1화면 통합 **or** 두 층 명시 명명 | **D3 결정 필요** |
| W1-4 | 헤더 3요소를 sticky 단일 행으로(같은 레이어) + 중앙Y 1px 이내 | 즉시 가능 |
| W1-5 | 캐릭터 정체성 단일화 — `chefs.ts` 레지스트리 단일 경유 | **D1 결정 필요**(Q.16 미확정과 동일 축) |

### 파도 2 — 문법 통일 (P2 · 누적 부채)
W2-1 문자 도형·이모지 → `Pict` SVG 레지스트리 편입 · W2-2 '준비 중' 단일 표준 부품(dim+배지, 셰브론 제거) · W2-3 폼 문법 = 섹션 라벨 축으로 통일 · W2-4 형제 중앙 정렬 실측 스윕(1px) · W2-5 최장 이름 오버플로 방어(max-width+말줄임) · W2-6 스크롤 경계 어포던스(하단 페이드·안전 패딩) · W2-7 내부 라벨·의미축 정리(`solar` → destructive 분리).

⚠ [4-1] 준수: 소급 통일 캠페인이 아니다. **게이트에 등재된 축(토큰·색·공유 부품)만 강제**하고, 나머지는 그 파일을 만질 때 곁들인다(touch-then-align).

### 파도 3 — 재방문 루프 (P3 · 브랜드 핵심)
확정 태그라인은 「당신의 운명을 위해, 오늘도 한 상.」 = **매일 들르는 집**인데, 지금 매일 올 이유는 '오늘 점수' 하나다. W3-1 일진 다이어리 기록 UI(엔진 `diaryDayInfo` 이미 배선 = 코드측 준비 완료) · W3-2 `/fun` = 8칸 중 7칸 '준비 중'인 빈 방 → 축소 또는 재편.

## 3. 순서와 근거

```
파도 0 (가독)  →  파도 1 (흐름)  →  파도 2 (문법)  →  파도 3 (루프)
   ↑ 게이트 G1 동승        ↑ D1·D2·D3 선행        ↑ 게이트 G2·G3 동승
```
파도 0을 먼저 하는 이유: 읽히지 않는 화면 위에서는 흐름 개선의 효과를 눈으로 검증할 수 없다. 파도 1이 결정 대기라 막히더라도 파도 0·2는 독립 진행된다.

## 4. 기계 게이트 — 청사진의 핵심 (재발 방지)

문장으로 막지 말고 게이트로 막는다([8] "훅이 문장보다 먼저 막는다"). `npm run verify` 6게이트에 렌더 기하 검사를 얹는다.

| ID | 게이트 | 무엇을 assert |
|---|---|---|
| G1 | 렌더 기하 회귀 | ① 텍스트가 적층되는 스테이지의 스크림 알파 > 0 ② 유리 크롬 뒤 텍스트 투과 0 ③ 형제 중앙Y 차 ≤ 1px ④ 대표데이터(최장명)에서 오버플로 0 ⑤ 스크롤 클립 경계 아래 잘린 컨트롤 0 |
| G2 | 글리프 | 소스에 문자 도형(`‹ › ▶ ▼ ✓ ✎ ✉ ↗ □ ×`)·이모지 0 |
| G3 | 정체성 | 캐릭터 이름·플레이트 참조가 `chefs.ts` 단일 경유(하드코딩 `aisha.png` 류 0) |

기존 스모크(`scripts/smoke.mjs`)가 이미 실브라우저를 띄우고 있어 **G1은 그 위에 얹는 증분**이다 — 새 인프라 불필요.

## 5. 운영자 결정 필요 (파도 1 블로커)

설명 대신 **플레이그라운드로 시안 비교**해서 고르게 한다([5]·[E9]).

| ID | 갈림길 | 지금 상태 |
|---|---|---|
| D1 | 캐릭터 정체성 — 연리(주인장) · 아이샤 · 셰프단 기본캐(남신)의 관계 | Q.16 미확정 1건과 **동일 축** — 이게 안 풀리면 W1-5 못 감 |
| D2 | 정보구조 — 하단 탭 5개로 확장 vs 홈 세그먼트 유지 | 탭 4 / 드로어 5 불일치 |
| D3 | 분석 ↔ 결과 — 1화면 통합 vs 2층 유지(명명 정리) | 같은 목적지 3경로 |

## 6. [E5] 계획 표

| ① 순서 | ② 내용 | ③ 현황 | ④ 목표 | ⑤ 달성방안 | ⑥ 예상소요 | ⑦ 예상결과 | ⑧ 리스크 | ⑨ 완료여부 | ⑩ 아이데이션 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 기틀·정본 정독 | STATUS·README·방식론 미독 | 배경·규율 파악 | CLAUDE.md + STATUS + dosa-app/README + 디자인방식론 실열람 | 5분 | 판정 기준 확보 | 정본 누락 시 추측 유입 | ✅ 완료 | 스크림 회귀 계보를 결정 로그에서 역추적해 원인 확정 |
| 2 | 전 화면 실렌더 | 렌더 미확인(코드 추정) | 현물 전수 확인 | 루트 `npm run build` → `app/dist` 서빙 → Chromium 11화면 캡처 | 10분 | 스샷 18컷 | KB 없으면 골격만 렌더 | ✅ 완료 | `?qa=1&view=` 딥링크로 무로그인 전수 진입 |
| 3 | 픽셀 실측 | 눈짐작 단계 | 좌표·계산값 확정 | `getBoundingClientRect`·`getComputedStyle` 실측 스크립트 | 8분 | 결함 17건 수치 확정 | 셀렉터 취약 | ✅ 완료 | 겹침·투과·중앙Y차를 자동 판정식으로 — 그대로 G1 게이트 씨앗 |
| 4 | 3층 원인 분류 | 증상만 나열 | 근본 원인별 묶음 | 가독 회귀 / 흐름 구조 / 문법 드리프트로 층 분리 | 5분 | 파도 4개 도출 | 과분류 | ✅ 완료 | A1을 "값 오류" 아닌 "축 변경 회귀"로 규정 → 게이트 필연성 도출 |
| 5 | 청사진 작성 | 없음 | 실행 가능한 파도 계획 | 파도별 손댈 파일·결정 블로커·게이트 명시 | 12분 | 이 문서 | 계획 비대 | ✅ 완료 | 게이트를 계획의 부록이 아니라 **본체**로 승격 |
| 6 | 폰 열람본 | md 단독 | 링크 탭 = 끝 | 라이브 경로 HTML(영수증 스샷 내장·charset 선두) | 10분 | `/reports/…_v1/` | 이미지 용량 | ✅ 완료 | 결함 카드 옆에 실렌더 컷 나란히 = 설명 없이 납득 |
| 7 | 게이트 검증·머지 | 미실행 | 회귀 0 확인 후 머지 | `npm run verify` 6게이트 → main 머지 → read-back | 8분 | 게이트 초록 | 스모크 환경 의존 | ✅ 완료 | — |

## 7. 이 문서가 안 하는 것

- 코드를 안 고친다(청사진 = 계획). 파도 0은 GO 즉시 착수 가능한 상태로 명세만 확정.
- 새 색·새 값·새 컴포넌트를 창작하지 않는다 — 파도 전부 **기존 토큰·부품 계승**([4]·최우선 4).
- D1~D3을 세션이 결정하지 않는다([E12] · 3조1항 "디자인은 너가 임의로 설계하는 게 아니다").
