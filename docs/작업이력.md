# 작업이력 — 원장 (append-only)

> 완료한 작업을 아래로 쌓는다(최신 = 맨 아래). 한 항목 = `yymmdd — 제목` + 요지 · 전/후 · 결정근거 · 검증. **기존 항목은 고치지 않고 덧붙이기만 한다(append-only).** 목적: 세션·컨텍스트가 바뀌어도 여기만 읽으면 흐름이 이어지게.

## 260706 — 기본 기틀 이식: 플레이그라운드 표준 + 보고·기록 규칙

- **요지**: 노뮤트(no_mute) 쪽에서 확립된 작업 기틀 3종을 Yeul 크기에 맞게 이식.
  - ① 디자인 제안 = 만지는 HTML 플레이그라운드 — 스펙 원문 `docs/플레이그라운드_포터블.md`(업로드 원본과 바이트 동일 사본) + CLAUDE.md `🎛` 절에 개념규칙 블록(§1 A)을 바인딩 치환해 이식.
  - ② 실질 변경 결과 = 쉬운 설명 + HTML 보고서(`docs/reports/{yymmdd}_{라벨}.html`) — CLAUDE.md `📦` 절.
  - ③ 작업이력 원장(이 파일) + 선택 분기 = `AskUserQuestion` 버튼 — CLAUDE.md `📦` 절.
- **전/후**: 전 = 시안·보고·기록 표준 없음(대화에만 존재) → 후 = 레포 SSOT로 명문화, `docs/` 신설.
- **바인딩 실측값** (포터블 §2 표 기준):
  - `<SSOT>` = `css/styles.css`의 `:root` + `[data-theme="dark"]` (색·radius·그림자·모션 `--ease`/`--t`·폭·폰트 토큰)
  - `<보고서경로>` = `docs/reports/` (신설)
  - `<프레임>` = 반응형 원페이지 실사용 화면비 — 폰 세로 기본 + 데스크톱(`--maxw: 1080px`) 축이면 병치/토글 (분기점 실측: 460/680/760px)
  - `<컴포넌트정본>` = `js/knowledge/005-ui-kit.knowledge.js` + `css/styles.css` 컴포넌트 클래스 (별도 컴포넌트 인덱스 문서는 아직 없음) · 모션 = `js/knowledge/007-reveal.knowledge.js` (STAGGER 90ms · CAP 720)
  - `<게이트>` = `node --check js/**/*.js` + 정적 서버(`python3 -m http.server`) 자산 200 확인 — 자동 참조정합 게이트는 없음(사람 규율 보조)
  - `<확립본>` = 레포 내 플레이그라운드 확립본 사본 **없음** → 포터블 §3 골격 스펙만으로 제작(스펙 머리말이 보증하는 경로)
- **참조 사본**: `docs/참조/노뮤트_가이드HTML_수준참조.html` = 노뮤트 "클립보드 다운로더 — 사용 가이드 v5.3" 원문 사본. 자기완결 HTML 산출물의 **수준 참조**(키캡·코드블록·복사 버튼+`execCommand` 폴백·경고/확인 박스 패턴). 주의 2가지: (1) 플레이그라운드(프리셋·슬라이더·선택값 복사)가 아니라 가이드 문서다 — 시안 골격의 본은 아님. (2) Pretendard CDN `<link>` 1건 포함 = "외부 요청 0" 원칙의 예외 사례(시스템 폰트 폴백은 있어 오프라인에서도 읽힘).
- **결정근거**: 운영자가 포터블 스펙·노뮤트 라우터·가이드 HTML 3종을 직접 업로드하며 "기본 기틀" 요청 — 포터블 스펙은 자체 설치 절차(§0~§2)를 따랐고, 노뮤트 라우터에서는 작은 정적 레포에 맞는 저비용 3종(HTML 보고서·원장·버튼 분기)만 채택(과설계 경계 = CLAUDE.md 🧪 "효용 주의" 준수). 기틀 교차검증은 미발동 — 트리거 대상(js/core·knowledge 구조·설정 스크립트·빌드) 무변경, CLAUDE.md 문구·docs 신설은 명시된 제외 영역.
- **검증**: 업로드 원본 ↔ 레포 사본 `diff` 0(포터블 md·참조 HTML 둘 다) · `node --check` 전 js 통과 · CLAUDE.md 렌더 눈 확인.

## 260706 — PWA 설치: 웹을 앱처럼 (설치 버튼 + 오프라인)

- **요지**: 사이트를 브라우저에서 "앱 설치" → PC 바탕화면·폰 홈 화면 바로가기 + 오프라인 열람 가능하게. 신규 `sw.js`(서비스 워커) · 내비 설치 버튼(`index.html`+`app.js`) · `manifest.webmanifest` 보강(id/scope/lang/maskable) · FAQ 아이폰 안내 1건(`002-brand`) · CSS `[hidden]` 가드 1줄.
- **전/후**: 전 = 매니페스트·아이콘만 있고 설치 동선·오프라인 없음 → 후 = 설치 가능 신호 시 내비에 ⤓ 버튼 등장, 첫 방문 때 셸 20파일 캐시로 오프라인 완전 렌더.
- **결정근거**: 캐시 전략 = **네트워크 우선**(온라인이면 항상 최신 → 정적 사이트 최다 사고인 "옛 화면 눌어붙음" 원천 차단, 오프라인만 캐시 폴백). 설치 버튼은 기존 `.icon-btn` 계승(신규 디자인 값 0 = 플레이그라운드 불요 판단). 기틀 교차검증 미발동 — js/core·knowledge 구조/계약 무변경(FAQ는 콘텐츠 데이터 = 주변부, app.js는 부트스트랩).
- **검증(실기동)**: `node --check` 전 파일 통과 · 매니페스트 JSON 파스 OK · 헤드리스 크로미움 실측 — SW 등록·활성·페이지 제어 ✓, **서버 프로세스 실제 종료 후** 새로고침 = 전체 렌더 ✓(포스터 포함), 오프라인 딥 경로(`/no-such-path`) = index 폴백 200 ✓, 페이지 JS 에러 0 ✓, 설치 버튼 42px 원형 계승 스크린샷 ✓. 상세 = `docs/reports/260706_PWA설치.html`.
- **관리 포인트(다음 세션 주의)**: 새 js/css/핵심 이미지 추가 시 `sw.js` PRECACHE 목록에도 추가(CLAUDE.md 작업 메모 명문화). 헤드리스는 `beforeinstallprompt` 미발화라 실제 설치 UX는 배포 후 실기기 확인 필요.

## 260706 — PIN 잠금 화면 시안 (nomute 동그라미 뉘앙스 이식 + 미니멀 글래스)

- **요지**: nomute-editor 화면잠금(`viewer/index.html` `.lockscr`)의 PIN 동그라미 채움 뉘앙스를 Yeul 코발트로 이식한 잠금/PIN 화면 시안 = `docs/reports/260706_핀잠금_플레이그라운드.html`(자기완결 1파일 · 플레이그라운드). 동그라미(방금 친 숫자 강조색 노출 → 앞자리 꽉 찬 원+글로우+바운스 → 0.5초 자동 마스킹, 역행=백스페이스도 동일) · 숫자 키패드(∴ 키보드 입력 없음 = CapsLock 원천 없음) · 틀리면 빨강 흔들림·맞으면 테두리 스윕 1바퀴 후 블러 언락.
- **전/후**: 전 = 잠금/PIN 화면 없음 → 후 = 시안 1파일. **라이브 사이트엔 미배선**(운영자: 프로모 사이트를 지금 잠글 건 아님 · "나중에 쓸 수 있으니까" 보존).
- **글래스 선택값(운영자 확정)**: `blur 2px · alpha .1 · sat 1.1 · tint .02 · bord .04 · shadow .51 · radius 29px` = 얇고 투명한 미니멀 글래스. 시안 `:root --t-*` 기본값 + `내 선택 ★` 프리셋으로 박음(로딩 시 이 값).
- **계승/신규**: 색·모션(코발트 `--brand`·`--island`·`--ease`·모서리) = 계승 / 글래스 값 = 신규(Yeul 글래스 토큰 부재 → 새로 정함 · 아직 `css/styles.css` `:root` 미편입 = 시안 스코프 `--t-*`에만 · 라이브 배선 시 편입 예정).
- **결정근거**: 참고 스크린샷(윈도우 로그인)의 불만 2종 — 숫자 PIN인데 뜨는 CapsLock 경고 + 밋밋한 글래스 — 을, 숫자 키패드(키보드 입력 제거)로 CapsLock 원천 차단 + nomute 무채색 글래스 방식으로 해결. 시안 단계라 기틀 교차검증 미발동(js/core·knowledge·설정 무변경 · docs/reports 산출).
- **검증**: 헤드리스 크로미움 실측 — JS 에러 0 · 채움→언락(성공 스윕)→오류(흔들림)→프리셋 전환 렌더 ✓ · 0.5초 마스킹 입력·역행 assert 6종 PASS · `내 선택 ★` 로딩 적용·값 정합 ✓ · 한글 두부 없음.
- **다음(재사용 시)**: 라이브 게이트로 쓸 거면 (A) 소프트 연출 = 시안 로직 그대로 배선 / (B) 진짜 보호 = Cloudflare Access·Pages Function 서버검사 중 택1(정적 사이트 클라 PIN은 우회 가능 = 소프트 게이트). PIN 값·저장방식은 그때 결정. 참고 = draft PR `exitmaru/Yeul#14`.

## 260709 — 최상위 공통 법칙 3종: 백그라운드 먼저 묻기 · 완료 시점 예고 금지 · 머지 충돌 확인+확인 요청

- **요지**: 사용자 지시(지금 클론한 모든 레포 공통)로 CLAUDE.md에 ⚖️ 최상위 공통 법칙 절 신설 — ① 백그라운드 실행은 작업 초반 계획이 설 때 한 번 물어 승인 후에만(직접 시킨 건 그 말이 승인 · 끝나면 정리) ② "1~2분 뒤 완료" 같은 시간 예고 발화 금지(확인 후 보고만) ③ 머지 전 다른 세션 작업과 충돌 확인 · 머지 후 사용자에게 확인 요청.
- **전/후**: 전 = 세 규칙 모두 명문 없음(백그라운드 임의 실행 가능) → 후 = CLAUDE.md ⚖️ 절 상시 로드.
- **결정근거**: 사용자 직접 지시(전 레포 일괄). CLAUDE.md 문구 = 🧪 기틀 제외 영역(명시)이라 교차검증 미발동.
- **검증**: 마크다운 눈 확인 + 머지 후 `git show origin/main:CLAUDE.md` 대조.

## 260709 — MCP 권한 자동 허용: `.claude/settings.json` permissions.allow 4건 신설

- **요지**: 원격 세션마다 뜨던 MCP 도구 권한 확인 버튼 제거 — 프로젝트 설정 `.claude/settings.json` 신설, `permissions.allow`에 4건 등록: `mcp__Claude_Code_Remote` · `mcp__claude-code-remote`(Claude Code Remote 서버 전체 허용의 신/구 표기) + `mcp__github__subscribe_pr_activity` · `mcp__github__unsubscribe_pr_activity`(PR 활동 구독/해제가 github 서버 표기로 뜨는 세션 커버). CLAUDE.md 작업 메모에 정본 위치 한 줄 기록.
- **전/후**: 전 = `.claude/` 설정 파일 없음(해당 MCP 호출마다 승인 버튼) → 후 = 위 4건 자동 허용. 적용 = 다음 세션부터(설정은 세션 시작 때 읽힘).
- **결정근거**: 운영자 명시 승인·지시("정확히 이 범위만"). 기틀 교차검증 미발동 — js/core·knowledge·빌드 무변경의 신규 설정 파일이고, 절차 ①(사용자 확인)이 지시 자체로 충족 · 검증은 jq 파스 + 원격 실측이 담당.
- **검증**: `jq . .claude/settings.json` 파스 OK(문법 깨지면 그 파일 설정 전체가 조용히 무효 → 필수 게이트) · 머지 후 `git show origin/main:.claude/settings.json` 원격 실측으로 4건 실존 확인.

## 260710 — 백그라운드 승인 팝업 훅(bg_gate) — 기계 강제 추가

- **요지**: 어제 박은 "백그라운드 먼저 묻기"를 기계로도 강제 — `.claude/hooks/bg_gate.py` 신설 + `.claude/settings.json`에 PreToolUse 훅 등재(기존 permissions.allow 보존 병합). 백그라운드 실행 순간 무조건 승인 팝업이 떠서, 깜빡하고 안 물었어도 사용자 승인 없인 못 돈다(사용자 A안 선택 · 전 레포 공통 반영분).
- **전/후**: 전 = 말로만 지키는 약속(소프트 룰) → 후 = 실행 순간 기계가 팝업으로 잡음. 백그라운드가 아닌 보통 명령은 아무 영향 없음(무의견 통과).
- **결정근거**: 사용자 직접 지시("강제로 하면 좋겠고" → A안). 🧪 기틀 교차검증 미발동 — js/core·knowledge·빌드 무변경의 설정·훅 파일 신설이고, 절차 ①(사용자 확인)이 지시 자체로 충족 · 검증은 아래 실측이 담당.
- **검증**: 훅에 가짜 입력 3케이스를 흘려 출력 실측(백그라운드 = 팝업 지시 JSON 정확 · 그 외 = 무반응) · settings.json 재파스 OK · 머지 후 `git show origin/main` 대조.

## 260710 — 플레이그라운드 포터블 신판 이식: 축 3분류(기틀 인지) + 기틀 갱신 역제안

- **요지**: 운영자가 올린 포터블 신판을 `docs/플레이그라운드_포터블.md`에 반영하고 CLAUDE.md 🎛 절 ③을 동기화. 신설 내용 = ③ 미세조정 컨트롤 강화(옵션 평균 50개 안팎 · 축 3분류: 정렬·간격 자유 / 확립 축 = 자유 + 실존값 사다리 눈금 + 이탈 ⚠ 실시간 / 색 = 팔레트 폐쇄 셀렉트) + §3-2′ 기틀 갱신 역제안 규약(복사 시 ⚠ 축 있으면 알림창 → [역제안 포함 복사]/[계승분만]/[취소], 역제안 = 기틀 자체를 바꾸는 승인 정문 경로) + §6 안티패턴 2개(이탈 감지 없는 자유 슬라이더 금지 · 옵션 몇 개만 주고 끝 금지).
- **전/후**: 전 = ③ "미세조정 슬라이더 — 그룹 라벨 · tabular-nums" 한 줄(옵션 규모·기틀 이탈 처리 규정 없음) → 후 = 옵션 규모 기준 + 기틀 인지 3중 장치(사다리 눈금 · ⚠ 실시간 · 복사 알림창+역제안) 명문화. 코드·화면 무변경(문서만).
- **결정근거**: 운영자 파일 업로드 = 이식 지시. CLAUDE.md 문구 = 🧪 기틀 제외 영역이라 교차검증 미발동. 바인딩 실측 재확인 — `css/styles.css:11` `:root` · `:53` `[data-theme="dark"]` · `:44-45` `--ease`/`--t` · `:47` `--maxw:1080px` · `js/knowledge/005-ui-kit.knowledge.js` 실존 · `007-reveal.knowledge.js:15` STAGGER=90ms.
- **검증**: 포터블 파일 = 업로드본과 diff 0(바이트 동일) · CLAUDE.md ③ = 신판 (A) 블록 ③과 문자열 일치 확인.

## 260710 — 포터블에 §3-6 선례 실측 팁 5 편입 (통합 복제팩과 동기)

- **요지**: 운영자 지시("미리 팁 넣어놓으면 좋아 — 주말에 여기도 들어올 거라")로 `docs/플레이그라운드_포터블.md`에 §3-6 신설 — 검색UX 50축 플레이그라운드 선례에서 추출한 제작 잔기술 5종(범주형 축 = 세그 버튼 · 프리셋⇄슬라이더 양방향 동기 · 첫 화면 = 결정 대상 상태 · 미리보기 속 실물 트리거 배선 · 배선 대비 메모 2종). CLAUDE.md 🎛 포인터에 "제작 팁" 한 단어 보강.
- **전/후**: 전 = 팁이 채팅에만 존재(세션 끝나면 증발) → 후 = 포터블 정본에 상주(다음 시안 세션이 자동으로 봄). 텍스트 = 운영자에게 전달한 통합 복제팩 2부 §3-6에서 기계 추출(바이트 동일 — 레포 간 드리프트 0).
- **결정근거**: 운영자 명시 승인. CLAUDE.md 문구·포터블 = 🧪 기틀 제외 영역이라 교차검증 미발동. 통합 복제팩 쪽은 이미 적대 감사 1인(전 관점 OK) 통과분.
- **검증**: 포터블 vs 통합팩 2부 diff = 라우터 전용 주석 2건(Yeul 미해당이라 의도적 미반입)만 상이 · §3-6 = 바이트 동일 · 앵커 유일성 assert 통과.

## 260712 — GitHub Actions 시크릿 목록 CLAUDE.md에 기록 (이름만 · 다시 안 물어보기)

- **요지**: 운영자가 레포 Settings에 이미 등록해 둔 GitHub Actions 시크릿 10개를 CLAUDE.md 🔐 절 신설로 기록 — 다음 세션이 키를 또 조르지 않게. 목록: `CLAUDE_CODE_OAUTH_TOKEN_{EMS1130G·EMS1130N·MUTENO·MUTENONA·NOMUTEFB}`(5, Claude Code OAuth 토큰=봇별) · `GEMINI_API_KEY` · `OPENAI_API_KEY_NOMUTE` · `VAPID_PUBLIC_KEY`·`VAPID_PRIVATE_KEY`(웹푸시) · `YOUTUBE_API_KEY`. 각 이름에 용도 1줄. + OAuth 토큰 발급법(`claude setup-token`, Pro/Max 필요) + 워크플로 참조법(`${{ secrets.이름 }}`) 명기.
- **전/후**: 전 = 시크릿이 GitHub Settings에만 존재(세션엔 안 보임 → 매번 "그 키 뭐예요/등록해요" 물음) → 후 = CLAUDE.md 상시 로드 목록으로 상주. 새 토큰 생기면 **이름만** 표에 한 줄 추가하는 규약도 명문화.
- **⚠ 보안 철칙**: **이름만 기록, 값(실제 열쇠 문자열)은 어떤 파일에도 절대 안 적음** — 값은 깃 기록에 영구히 박혀 유출되므로 GitHub Settings 안에서만 살아있게 둔다. 이번 커밋도 이름·용도만 담고 값 0건.
- **결정근거**: 운영자 직접 지시("레포에 다 넣어놨으니 CLAUDE.md에 쌓아 다음에 청구 않게"). CLAUDE.md 문구 = 🧪 기틀 교차검증 **제외** 영역(js/core·knowledge·빌드 무변경의 문서 메모)이라 병렬 감사 미발동.
- **검증**: 발급 명령 `claude setup-token`·시크릿 이름 규칙을 커밋 전 웹 실측(docs.claude.com·anthropics/claude-code-action) 후 반영 · 표 10행 = 스크린샷과 이름 대조 1:1 일치 · 마크다운 표 렌더 눈 확인 · 값 미포함 재확인.

## 260712 — 사주 요약 '꼬미 언니' 페르소나 + 알고리즘 드래프트 (컨셉 단계 · 코드 무변경)

- **요지**: 검색 메인 요약을 '사주 봐주는 츤데레 언니(남 사주는 족집게·본인 일은 덜렁)' 목소리로 바꾸는 드래프트 산출(`docs/reports/260712_사주언니_요약_드래프트.md`) — 페르소나·어투 규칙·금칙·few-shot 예시 3쌍 + 요약 아키텍처(①무-LLM 템플릿 사다리 = 현행 무-LLM 존중·결정론적·즉시 출시 가능 ②LLM 승격 레이어 = 시스템 프롬프트 전문/출력 스키마/Gemini→OpenAI 폴백/음성 검증기/캐싱, 실패 시 ①로 자동 복귀) + 한국 웹툰풍 캐릭터 이미지 프롬프트(Gemini·OpenAI 각 1식).
- **운영자 결정(버튼 답)**: ① 지금은 컨셉만(코드 배선 나중) ② 이미지는 운영자가 다른 데서 직접 렌더(§4 프롬프트 사용) ③ 이름 = "수셰프 보조하는 보조 애들 이름"으로 → 주방 브리게이드 보조 직급 후보(★꼬미(commis)·데미·미장) 드래프트, 실존 캐릭터 목록 받으면 교체.
- **전/후**: 전 = 요약이 무-LLM·무-페르소나 → 후(설계) = 항상 언니 목소리 카드(LLM 없어도 템플릿으로 동일 컨셉 유지). 이 레포 코드 무변경(문서만) — Yeul엔 사주/검색/요약 기능 부재를 grep 실측으로 확인, 붙일 앱은 미확정.
- **결정근거**: 운영자 직접 지시("드래프트해서 전달"). 기틀 교차검증 미발동(문서만 · js/core·knowledge·설정 무변경).
- **검증**: 실행환경에 GEMINI/OPENAI 키 부재 = env 실측(그래서 직접 렌더 불가를 문서에 정직 표기) · 레포 내 사주/검색/셰프 관련 기능 부재 = grep 실측 · 결정 3건 = AskUserQuestion 버튼 답 원문 반영.

## 260712 — 사주앱 대화형 UX 데모 (미연시 × 토스) — 탭해보는 실물 HTML

- **요지**: 운영자 요청("대화형으로 판 열리면서, 미연시처럼, 토스처럼 하나씩 전환")에 UX 아이디어 10종을 내고, 말 대신 **실제로 탭하며 도는 데모**(`docs/reports/260712_사주앱_대화형UX_데모.html`) 산출. 핵심 반전 = 사주 플래그로 언니가 **먼저 단정**하는 족집게 모먼트("너, 회사 그만두고 싶지"). 구성: 미연시 씬(스탠딩 표정 5종·타이핑·선택지·호감도) + 토스식 입력(한 화면 한 질문·하단 CTA) + 만세력 카드 8장 플립(로딩의 연출화) + 팔자 8글자 헤더 프로그레스 + 허당 개그(계산 직후) + 결과 공유 카드(오행 바·금0 ⚠). 설정 드로어(프리셋 3 + 13축: 말풍선 박스/버블·츤 레벨·허당 빈도·테마·폐쇄 팔레트 3색 등). 아이디어 목록은 드래프트 문서 §8에 정본 기록.
- **전/후**: 전 = 컨셉이 글로만 존재 → 후 = 폰에서 탭해보는 자기완결 1파일(외부 요청 0). 데모 팔자 = 대사와 정합 설계(寅 역마·子/午 도화·金 0 금약). 코드(사이트) 무변경 — docs/ 산출물만.
- **결정근거**: 운영자 직접 지시. 신규 앱 컨셉이라 확립 기틀 부재 → 플레이그라운드 규약의 '단순 대상 축소' 적용(옵션 13축 + 프리셋 3, 색 = 폐쇄 팔레트, 사유는 데모 내 각주 ⓘ에 명기). 기틀 교차검증 미발동(docs만).
- **검증**: 헤드리스 크로미움 3런 실측 — A(기본 속도: 맞아→돈 분기 완주 + 씬별 스크린샷 5장 + 호감도 16 정합) · B(고속: 아니야→연애 분기 + 시주 미상 ? 카드 경로) · C(프리셋 전환 실적용 + 각주 모달) · **JS 에러 0**. 중간 발견 버그 2건 즉시 수정(결과 오버레이가 헤더 버튼 가림 → z-index · 팔자 슬롯 8개 폭 잘림 → 전용 줄 분리) 후 재검증 통과.

## 260712 — 사주앱 데모 테마 확정: 클로신(契路神) 기틀 계승 재이식

- **요지**: 운영자 지시("이 느낌에 테마만 바꾸면 됨" + 유추→좁히기 + 참고 레퍼런스 제공)로 데모(`docs/reports/260712_사주앱_대화형UX_데모.html`)를 클로신 세계관으로 전면 재이식. 색·글래스·라운드·모션 = **PR #21 브랜치 `manse/app/app.css :root` 토큰 실측 사본 그대로 계승**(베이스 #0b0b0d · 코랄 #d97757 · 글래스 .07/.14/blur20 · radius 22 · ease/480ms · **오행색 --el-* 5종**) — 신규 색 0. 운영자 제공 레퍼런스(FACETELLER류 스샷)에서 구조 차용: 만세력 = 時日月年 전통 배열(年부터 우→좌 플립) + 오행 색상원 + **십신 라벨**(고정 일간 戊土 기준 실규칙: 甲편관·子정재·丙편인·寅편관·戊비견·午정인·壬편재·戌비견 · 지지 = 본기 단순화) · 결과 카드 글리프 = 오행색 원 · 오행 바 = --el-* 색. 유추 스킨 5종안은 운영자가 레퍼런스로 수렴시켜 폐기(단일 기틀 계승), 설정 드로어는 연출·레이아웃 축만 유지.
- **전/후**: 전 = 밤 사주카페(자체 인디고·임의 색) → 후 = 클로신 글래스(기틀 토큰 계승 · 브랜딩 契路神 · 헤더 팔자 슬롯도 오행색). 흐름·대사·분기 무변경.
- **결정근거**: 운영자 레퍼런스 제시 = 테마 확정. 기틀 교차검증 미발동(docs 산출물만 · manse 브랜치는 읽기만).
- **검증**: 헤드리스 3런 재통과(맞아→돈 / 아니야→연애+시주미상 / 프리셋·각주) · JS 에러 0 · 십신 배정은 십신 생극 규칙으로 검산 후 기입.
