# 작업이력 — 원장 (append-only)

> 완료한 작업을 아래로 쌓는다(최신 = 맨 아래). 한 항목 = `yymmdd — 제목` + 요지 · 전/후 · 결정근거 · 검증. **기존 항목은 고치지 않고 덧붙이기만 한다(append-only).** 목적: 세션·컨텍스트가 바뀌어도 여기만 읽으면 흐름이 이어지게.

## 260706 — 기본 기틀 이식: 플레이그라운드 표준 + 보고·기록 규칙

- **요지**: 노뮤트(no_mute) 쪽에서 확립된 작업 기틀 3종을 Yeul 크기에 맞게 이식.
  - ① 디자인 제안 = 만지는 HTML 플레이그라운드 — 스펙 원문 `docs/플레이그라운드_포터블.md`(업로드 원본과 바이트 동일 사본) + CLAUDE.md `🎛` 절에 개념규칙 블록(§1 A)을 바인딩 치환해 이식.
  - ② 실질 변경 결과 = 쉬운 설명 + HTML 보고서(`docs/reports/{yymmdd}_{라벨}.html`) — CLAUDE.md `📦` 절.
  - ③ 작업이력 원장(이 파일) + 선택 분기 = `AskUserQuestion` 버튼 — CLAUDE.md `📦` 절.
- **전/후**: 전 = 시안·보고·기록 표준 없음(대화에만 존재) → 후 = 레포 SSOT로 명문화, `docs/` 신설.
- **바인딩 실측값** (포터블 §2 표 기준):
  - `<SSOT>` = `css/styles.css`의 `:root` + `[data-theme="dark"]` (색·radius·그림자·모션 `--ease`/`--t`·폭·폰트 토큰)
  - `<보고서경로>` = `docs/reports/` (신설)
  - `<프레임>` = 반응형 원페이지 실사용 화면비 — 폰 세로 기본 + 데스크톱(`--maxw: 1080px`) 축이면 병치/토글 (분기점 실측: 460/680/760px)
  - `<컴포넌트정본>` = `js/knowledge/005-ui-kit.knowledge.js` + `css/styles.css` 컴포넌트 클래스 (별도 컴포넌트 인덱스 문서는 아직 없음) · 모션 = `js/knowledge/007-reveal.knowledge.js` (STAGGER 90ms · CAP 720)
  - `<게이트>` = `node --check js/**/*.js` + 정적 서버(`python3 -m http.server`) 자산 200 확인 — 자동 참조정합 게이트는 없음(사람 규율 보조)
  - `<확립본>` = 레포 내 플레이그라운드 확립본 사본 **없음** → 포터블 §3 골격 스펙만으로 제작(스펙 머리말이 보증하는 경로)
- **참조 사본**: `docs/참조/노뮤트_가이드HTML_수준참조.html` = 노뮤트 "클립보드 다운로더 — 사용 가이드 v5.3" 원문 사본. 자기완결 HTML 산출물의 **수준 참조**(키캡·코드블록·복사 버튼+`execCommand` 폴백·경고/확인 박스 패턴). 주의 2가지: (1) 플레이그라운드(프리셋·슬라이더·선택값 복사)가 아니라 가이드 문서다 — 시안 골격의 본은 아님. (2) Pretendard CDN `<link>` 1건 포함 = "외부 요청 0" 원칙의 예외 사례(시스템 폰트 폴백은 있어 오프라인에서도 읽힘).
- **결정근거**: 운영자가 포터블 스펙·노뮤트 라우터·가이드 HTML 3종을 직접 업로드하며 "기본 기틀" 요청 — 포터블 스펙은 자체 설치 절차(§0~§2)를 따랐고, 노뮤트 라우터에서는 작은 정적 레포에 맞는 저비용 3종(HTML 보고서·원장·버튼 분기)만 채택(과설계 경계 = CLAUDE.md 🧪 "효용 주의" 준수). 기틀 교차검증은 미발동 — 트리거 대상(js/core·knowledge 구조·설정 스크립트·빌드) 무변경, CLAUDE.md 문구·docs 신설은 명시된 제외 영역.
- **검증**: 업로드 원본 ↔ 레포 사본 `diff` 0(포터블 md·참조 HTML 둘 다) · `node --check` 전 js 통과 · CLAUDE.md 렌더 눈 확인.

## 260706 — PWA 설치: 웹을 앱처럼 (설치 버튼 + 오프라인)

- **요지**: 사이트를 브라우저에서 "앱 설치" → PC 바탕화면·폰 홈 화면 바로가기 + 오프라인 열람 가능하게. 신규 `sw.js`(서비스 워커) · 내비 설치 버튼(`index.html`+`app.js`) · `manifest.webmanifest` 보강(id/scope/lang/maskable) · FAQ 아이폰 안내 1건(`002-brand`) · CSS `[hidden]` 가드 1줄.
- **전/후**: 전 = 매니페스트·아이콘만 있고 설치 동선·오프라인 없음 → 후 = 설치 가능 신호 시 내비에 ⤓ 버튼 등장, 첫 방문 때 셸 20파일 캐시로 오프라인 완전 렌더.
- **결정근거**: 캐시 전략 = **네트워크 우선**(온라인이면 항상 최신 → 정적 사이트 최다 사고인 "옛 화면 눌어붙음" 원천 차단, 오프라인만 캐시 폴백). 설치 버튼은 기존 `.icon-btn` 계승(신규 디자인 값 0 = 플레이그라운드 불요 판단). 기틀 교차검증 미발동 — js/core·knowledge 구조/계약 무변경(FAQ는 콘텐츠 데이터 = 주변부, app.js는 부트스트랩).
- **검증(실기동)**: `node --check` 전 파일 통과 · 매니페스트 JSON 파스 OK · 헤드리스 크로미움 실측 — SW 등록·활성·페이지 제어 ✓, **서버 프로세스 실제 종료 후** 새로고침 = 전체 렌더 ✓(포스터 포함), 오프라인 딥 경로(`/no-such-path`) = index 폴백 200 ✓, 페이지 JS 에러 0 ✓, 설치 버튼 42px 원형 계승 스크린샷 ✓. 상세 = `docs/reports/260706_PWA설치.html`.
- **관리 포인트(다음 세션 주의)**: 새 js/css/핵심 이미지 추가 시 `sw.js` PRECACHE 목록에도 추가(CLAUDE.md 작업 메모 명문화). 헤드리스는 `beforeinstallprompt` 미발화라 실제 설치 UX는 배포 후 실기기 확인 필요.

## 260706 — PIN 잠금 화면 시안 (nomute 동그라미 뉘앙스 이식 + 미니멀 글래스)

- **요지**: nomute-editor 화면잠금(`viewer/index.html` `.lockscr`)의 PIN 동그라미 채움 뉘앙스를 Yeul 코발트로 이식한 잠금/PIN 화면 시안 = `docs/reports/260706_핀잠금_플레이그라운드.html`(자기완결 1파일 · 플레이그라운드). 동그라미(방금 친 숫자 강조색 노출 → 앞자리 꽉 찬 원+글로우+바운스 → 0.5초 자동 마스킹, 역행=백스페이스도 동일) · 숫자 키패드(∴ 키보드 입력 없음 = CapsLock 원천 없음) · 틀리면 빨강 흔들림·맞으면 테두리 스윕 1바퀴 후 블러 언락.
- **전/후**: 전 = 잠금/PIN 화면 없음 → 후 = 시안 1파일. **라이브 사이트엔 미배선**(운영자: 프로모 사이트를 지금 잠글 건 아님 · "나중에 쓸 수 있으니까" 보존).
- **글래스 선택값(운영자 확정)**: `blur 2px · alpha .1 · sat 1.1 · tint .02 · bord .04 · shadow .51 · radius 29px` = 얇고 투명한 미니멀 글래스. 시안 `:root --t-*` 기본값 + `내 선택 ★` 프리셋으로 박음(로딩 시 이 값).
- **계승/신규**: 색·모션(코발트 `--brand`·`--island`·`--ease`·모서리) = 계승 / 글래스 값 = 신규(Yeul 글래스 토큰 부재 → 새로 정함 · 아직 `css/styles.css` `:root` 미편입 = 시안 스코프 `--t-*`에만 · 라이브 배선 시 편입 예정).
- **결정근거**: 참고 스크린샷(윈도우 로그인)의 불만 2종 — 숫자 PIN인데 뜨는 CapsLock 경고 + 밋밋한 글래스 — 을, 숫자 키패드(키보드 입력 제거)로 CapsLock 원천 차단 + nomute 무채색 글래스 방식으로 해결. 시안 단계라 기틀 교차검증 미발동(js/core·knowledge·설정 무변경 · docs/reports 산출).
- **검증**: 헤드리스 크로미움 실측 — JS 에러 0 · 채움→언락(성공 스윕)→오류(흔들림)→프리셋 전환 렌더 ✓ · 0.5초 마스킹 입력·역행 assert 6종 PASS · `내 선택 ★` 로딩 적용·값 정합 ✓ · 한글 두부 없음.
- **다음(재사용 시)**: 라이브 게이트로 쓸 거면 (A) 소프트 연출 = 시안 로직 그대로 배선 / (B) 진짜 보호 = Cloudflare Access·Pages Function 서버검사 중 택1(정적 사이트 클라 PIN은 우회 가능 = 소프트 게이트). PIN 값·저장방식은 그때 결정. 참고 = draft PR `exitmaru/Yeul#14`.

## 260709 — 최상위 공통 법칙 3종: 백그라운드 먼저 묻기 · 완료 시점 예고 금지 · 머지 충돌 확인+확인 요청

- **요지**: 사용자 지시(지금 클론한 모든 레포 공통)로 CLAUDE.md에 ⚖️ 최상위 공통 법칙 절 신설 — ① 백그라운드 실행은 작업 초반 계획이 설 때 한 번 물어 승인 후에만(직접 시킨 건 그 말이 승인 · 끝나면 정리) ② "1~2분 뒤 완료" 같은 시간 예고 발화 금지(확인 후 보고만) ③ 머지 전 다른 세션 작업과 충돌 확인 · 머지 후 사용자에게 확인 요청.
- **전/후**: 전 = 세 규칙 모두 명문 없음(백그라운드 임의 실행 가능) → 후 = CLAUDE.md ⚖️ 절 상시 로드.
- **결정근거**: 사용자 직접 지시(전 레포 일괄). CLAUDE.md 문구 = 🧪 기틀 제외 영역(명시)이라 교차검증 미발동.
- **검증**: 마크다운 눈 확인 + 머지 후 `git show origin/main:CLAUDE.md` 대조.

## 260709 — MCP 권한 자동 허용: `.claude/settings.json` permissions.allow 4건 신설

- **요지**: 원격 세션마다 뜨던 MCP 도구 권한 확인 버튼 제거 — 프로젝트 설정 `.claude/settings.json` 신설, `permissions.allow`에 4건 등록: `mcp__Claude_Code_Remote` · `mcp__claude-code-remote`(Claude Code Remote 서버 전체 허용의 신/구 표기) + `mcp__github__subscribe_pr_activity` · `mcp__github__unsubscribe_pr_activity`(PR 활동 구독/해제가 github 서버 표기로 뜨는 세션 커버). CLAUDE.md 작업 메모에 정본 위치 한 줄 기록.
- **전/후**: 전 = `.claude/` 설정 파일 없음(해당 MCP 호출마다 승인 버튼) → 후 = 위 4건 자동 허용. 적용 = 다음 세션부터(설정은 세션 시작 때 읽힘).
- **결정근거**: 운영자 명시 승인·지시("정확히 이 범위만"). 기틀 교차검증 미발동 — js/core·knowledge·빌드 무변경의 신규 설정 파일이고, 절차 ①(사용자 확인)이 지시 자체로 충족 · 검증은 jq 파스 + 원격 실측이 담당.
- **검증**: `jq . .claude/settings.json` 파스 OK(문법 깨지면 그 파일 설정 전체가 조용히 무효 → 필수 게이트) · 머지 후 `git show origin/main:.claude/settings.json` 원격 실측으로 4건 실존 확인.

## 260710 — 백그라운드 승인 팝업 훅(bg_gate) — 기계 강제 추가

- **요지**: 어제 박은 "백그라운드 먼저 묻기"를 기계로도 강제 — `.claude/hooks/bg_gate.py` 신설 + `.claude/settings.json`에 PreToolUse 훅 등재(기존 permissions.allow 보존 병합). 백그라운드 실행 순간 무조건 승인 팝업이 떠서, 깜빡하고 안 물었어도 사용자 승인 없인 못 돈다(사용자 A안 선택 · 전 레포 공통 반영분).
- **전/후**: 전 = 말로만 지키는 약속(소프트 룰) → 후 = 실행 순간 기계가 팝업으로 잡음. 백그라운드가 아닌 보통 명령은 아무 영향 없음(무의견 통과).
- **결정근거**: 사용자 직접 지시("강제로 하면 좋겠고" → A안). 🧪 기틀 교차검증 미발동 — js/core·knowledge·빌드 무변경의 설정·훅 파일 신설이고, 절차 ①(사용자 확인)이 지시 자체로 충족 · 검증은 아래 실측이 담당.
- **검증**: 훅에 가짜 입력 3케이스를 흘려 출력 실측(백그라운드 = 팝업 지시 JSON 정확 · 그 외 = 무반응) · settings.json 재파스 OK · 머지 후 `git show origin/main` 대조.

## 260710 — 플레이그라운드 포터블 신판 이식: 축 3분류(기틀 인지) + 기틀 갱신 역제안

- **요지**: 운영자가 올린 포터블 신판을 `docs/플레이그라운드_포터블.md`에 반영하고 CLAUDE.md 🎛 절 ③을 동기화. 신설 내용 = ③ 미세조정 컨트롤 강화(옵션 평균 50개 안팎 · 축 3분류: 정렬·간격 자유 / 확립 축 = 자유 + 실존값 사다리 눈금 + 이탈 ⚠ 실시간 / 색 = 팔레트 폐쇄 셀렉트) + §3-2′ 기틀 갱신 역제안 규약(복사 시 ⚠ 축 있으면 알림창 → [역제안 포함 복사]/[계승분만]/[취소], 역제안 = 기틀 자체를 바꾸는 승인 정문 경로) + §6 안티패턴 2개(이탈 감지 없는 자유 슬라이더 금지 · 옵션 몇 개만 주고 끝 금지).
- **전/후**: 전 = ③ "미세조정 슬라이더 — 그룹 라벨 · tabular-nums" 한 줄(옵션 규모·기틀 이탈 처리 규정 없음) → 후 = 옵션 규모 기준 + 기틀 인지 3중 장치(사다리 눈금 · ⚠ 실시간 · 복사 알림창+역제안) 명문화. 코드·화면 무변경(문서만).
- **결정근거**: 운영자 파일 업로드 = 이식 지시. CLAUDE.md 문구 = 🧪 기틀 제외 영역이라 교차검증 미발동. 바인딩 실측 재확인 — `css/styles.css:11` `:root` · `:53` `[data-theme="dark"]` · `:44-45` `--ease`/`--t` · `:47` `--maxw:1080px` · `js/knowledge/005-ui-kit.knowledge.js` 실존 · `007-reveal.knowledge.js:15` STAGGER=90ms.
- **검증**: 포터블 파일 = 업로드본과 diff 0(바이트 동일) · CLAUDE.md ③ = 신판 (A) 블록 ③과 문자열 일치 확인.

## 260710 — 포터블에 §3-6 선례 실측 팁 5 편입 (통합 복제팩과 동기)

- **요지**: 운영자 지시("미리 팁 넣어놓으면 좋아 — 주말에 여기도 들어올 거라")로 `docs/플레이그라운드_포터블.md`에 §3-6 신설 — 검색UX 50축 플레이그라운드 선례에서 추출한 제작 잔기술 5종(범주형 축 = 세그 버튼 · 프리셋⇄슬라이더 양방향 동기 · 첫 화면 = 결정 대상 상태 · 미리보기 속 실물 트리거 배선 · 배선 대비 메모 2종). CLAUDE.md 🎛 포인터에 "제작 팁" 한 단어 보강.
- **전/후**: 전 = 팁이 채팅에만 존재(세션 끝나면 증발) → 후 = 포터블 정본에 상주(다음 시안 세션이 자동으로 봄). 텍스트 = 운영자에게 전달한 통합 복제팩 2부 §3-6에서 기계 추출(바이트 동일 — 레포 간 드리프트 0).
- **결정근거**: 운영자 명시 승인. CLAUDE.md 문구·포터블 = 🧪 기틀 제외 영역이라 교차검증 미발동. 통합 복제팩 쪽은 이미 적대 감사 1인(전 관점 OK) 통과분.
- **검증**: 포터블 vs 통합팩 2부 diff = 라우터 전용 주석 2건(Yeul 미해당이라 의도적 미반입)만 상이 · §3-6 = 바이트 동일 · 앵커 유일성 assert 통과.

## 260712 — 만세력 분석 모듈유닛화 + 새 UI (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: manse.sajuplus.net(1024px 고정 표 UI, 계산 서버은닉)의 분석 체계를 `manse/` 랩으로 분리 구현. 지식모듈 5(간지·지장간·십신·운성/신살/공망·신강약/용신) + 계산코어 2(역법·사주엔진) + UI유닛 1 + index.html. 해석 문장은 배제, 판정 규칙(고전 통설)만 근거 주석과 함께 모듈화.
- **전→후**: 고정 표 → 반응형 카드(라이트/다크·오행 팔레트 3종·프리셋 4·튜닝 30축·선택값 복사).
- **결정근거**: 규칙은 knowledge, 계산은 core, 표현은 unit — 본 레포 파일명 규칙(NNN-kebab.kind.js) 준수. 신강약 가중치는 명시적 휴리스틱으로 선언(정본 아님 라벨).
- **검증**: node --check 전 파일 통과 · 헤드리스 렌더 3뷰포트 스크린샷 · 내장 자기검증 13/13(일진 앵커 2000-01-01=戊午, 월두법 1990-05=辛巳월, 운성·신살·공망 스팟) · EoT 공식 오류 실측 발견→9.87식 교체(서울 보정 −46분→−28분 정상화).
- **한계(정직)**: 절기 근사 ±수 분, 음력 입력·DST·통관용신 미구현, 라이브 사이트 '전' 캡처는 봇 차단으로 실패(HTML 실측 대체). sw.js PRECACHE는 의도적 미등재(별도 랩, 오프라인 대상 아님).

## 260712-2 — 만세 글래스 앱(간이 사이트) 추가 (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: `manse/app/` 신설 — @paper-design/shaders-react grainGradient 무채 배경(레드 톤 원탭 대안) + 글래스모피즘 + 모바일 앱 쉘(앱바·3탭·바텀시트). 지식모듈 5+코어 2 그대로 재사용(표현층 3번째).
- **번들**: react+shaders → esbuild IIFE 1파일(`manse/vendor/paper-bg.js`, 248KB) — 사이트는 빌드리스 유지.
- **검증**: node --check · Playwright 정밀 390×844 실측(자기검증 13/13 · JS에러 favicon 404 1건 외 0) · 4상태 캡처. 헤드리스 크로뮴 `--window-size` 최소 500px 클램프 발견(계측 ?debug=1로 확정) → 촬영기를 Playwright로 교체.
- **수정 이력**: 시트 h2 선택자 누락(32px 거대 타이포) 발견→수정, pkill 자기매칭 셸 종료 사고 2회(브래킷/재사용 서버로 해소).
- **한계(정직)**: 번들 248KB(정식화 시 vanilla 감량), reduced-motion 미대응, 다크 온리. sw.js PRECACHE 미등재(별도 랩).

## 260712-3 — 만세력 지식 DB화 (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: `manse/tools/build-db.mjs` 신설 — 지식모듈(SSOT)에서 정적 JSON DB 11테이블 + manifest 기계 생성(`manse/db/`, 총 172KB). 십신 10×10 · 운성 10×12 · 신살 12×12 · 60갑자+공망 · 지장간 · 기두법 · 절기 절입시각 1900~2050(1,812행) 등.
- **설계**: DB = 파생물(손편집 금지, README 명시). 규칙 수정 → 모듈 수정 → 재생성 단방향. 생성 시 앵커 7종 검증 실패 시 중단.
- **검증**: 생성 앵커 7/7 통과 · 샘플 실측(입춘 2026 = 2/4 04:58 KST ±수분, 庚辰 공망 申酉 = 앱 표시와 일치).
- **한계**: 절기 근사 ±수 분 그대로 상속(정밀 천문력 차기), 납음오행 미포함(차기), 신강약 가중치는 참고용 라벨 유지.

## 260712-4 — DB 정립 근거 보고 + FigJam 소스 추출 판정 (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: ① DB 11테이블 각각의 기틀 근거(file:line)·생성 방식·검증 앵커 매트릭스 문서화 ② main의 FigJam(15.8MB) 역해석 성공 — ZIP→canvas.fig→zstd 해제 5.35MB, 한글 텍스트 3,116런+이미지 214장 추출 ③ 접목 판정: 십신 키워드·궁위론·합충론·격국 패턴·궁합 규칙 = 지식모듈 006~010으로 확장 가능(운영자 저작이라 저작권 무風).
- **개인정보 결정**: 보드 내 팀원 실명 사주 명부는 레포 미커밋(스크래치 한정), 접목 시 익명화/비공개 저장 전제.
- **soong.kr**: 루트→앱 _redirects 선반영(커밋 48c13f4), Cloudflare Pages 신규 프로젝트(output=manse)+커스텀 도메인 절차 보고서에 수록.
- **검증**: 근거 라인 grep 실측, FigJam 통계(합충 83런·궁합 55런) 실측.

## 260712-5 — FigJam 접목 1차: 합충·궁위·패턴·궁합 모듈 + DB (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: FigJam 정리본에서 규칙·구조만 계승해 지식모듈 006~010 신설 — 006 십신키워드(참고), 007 궁위론, 008 합충형파해원진(계산표), 009 십신패턴, 010 일주궁합. 엔진에 지지관계·패턴 배선, 글래스 앱 원국 카드에 칩 노출. DB 3테이블 추가(hapchung 12×12·gungwi·patterns) → 총 14테이블.
- **저작권 원칙**: FigJam 내 외부 블로그 인용(초코서당·무성거사·목양정 등) 문장은 미수록, 명리 공통 규칙/구조만 인코딩. 팀원 실명 명부 미커밋 유지.
- **검증**: node --check 전 파일 · DB 앵커 11종(子午충·寅午반합·午午자형·子丑육합 추가) · 앱 자기검증 15/15 · Playwright 실측 JS에러 0 · 지지관계/패턴 칩 렌더 확인.
- **버그 수정**: 午午가 삼합으로 오판(같은 지지 삼합 제외 누락)→수정, 십신카운트 기대값 오타(8→7 일간 제외) 수정.
- **한계**: 십신키워드 3종만 확정 수록(2차 큐레이션 남음), 삼합 3지 완성 판정은 쌍단위라 반합까지만(엔진 4지 동시판정 차기), 午未 육합 화/토 유파차는 화 고정.

## 260712-6 — 게시판 주제지도 기반 천간론 모듈 + 페이월 확인 (branch: claude/manse-calendar-uiux-qlhtyf)
- **발견(정직)**: saju001001 게시판 본문은 포인트(복채) 페이월 — 개별 글 "열람 권한 없음". 공개는 제목 목록뿐. 유료 우회는 하지 않음(접근통제 회피 배제).
- **원칙 정립**: 명리 '법칙'은 공개 사실(저작권 대상 아님)이나, 특정인의 '해석 문장'은 표현물. → 유료 본문 미열람·미인용, 제목=공개 메타데이터만 주제 지도로 사용해 공개 통설을 우리 표현으로 재정리.
- **산출**: 011-cheongan-archetype(10천간 물상·성정 키워드, 통설/참고 라벨) + DB cheongan_archetype.json(로드맵 포함). 게시판 제목에서 확인된 커버리지: 천간론(1차 완료)·운성해석·삼형살(008 기존)·용신론·지지론(차기).
- **검증**: node --check · DB 앵커 13종(천간 10·甲목 추가) 통과.

## 260712-7 — 로드맵 구멍 메움: 지지론·운성의미·십신키워드 완비 (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: 게시판 기초연재 목차(saju001017)와 대조해 남은 구멍을 공개 통설로 채움. 012-지지 아키타입(12지지 물상+생왕고=역마/도화/화개), 013-십이운성 의미 태그(12단계), 006 십신키워드 3종→10종 완비. DB 15→18테이블.
- **저작권**: 전부 공개 명리 통설을 키워드로 재정리(유료 게시판 본문 미열람·미인용). 참고용 라벨 유지.
- **검증**: node --check · DB 앵커 18종(지지12·子왕지·생지寅申巳亥·운성12·십신10 추가) · 앱 자기검증 15/15 유지 · JS에러 0 · 새 테이블 샘플 실측(午=왕지, 제왕 키워드, 정관 과다).
- **커버리지**: 기초연재 커리큘럼(천간·지지·간지·지장간·합충·운성) 지식층 100% 커버. 남은 건 이 지식을 앱 화면에 노출(글자 탭/툴팁)뿐.

## 260712-8 — 글자 탭 → 사전 카드 (지식 DB 화면 노출) (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: 글래스 앱 원국의 천간/지지 글자를 탭하면 글래스 바텀시트로 "글자 사전" 표출. 지식모듈 006(십신키워드)·011(천간)·012(지지)·013(운성의미) 실시간 소비.
- **표출 항목**: 물상·오행/음양·(지지)자리 생왕고+역마/도화/화개·계절/월·키워드·지장간·십이운성+의미태그·신살·십신 관계/미약/과다. 하단 참고용 라벨 고정.
- **구현**: gl()에 data-pos/data-t 부여, #pillars 이벤트 위임(클릭+Enter/Space 접근성), last 결과 참조, #dict 글래스 시트 + scrim 공유.
- **검증**: node --check · Playwright 실클릭(辰 일주지지→사전 열림 확인, 내용 실측: 습토·용/고지·화개/지장간 乙癸戊/운성 양·양육/월살/편인) · 자기검증 15/15 유지 · JS에러 0 · 스크린샷.
- **다음**: 사용자가 지식 대량 투입 예정 → 사전 카드가 그 지식의 표시 슬롯이 됨. 픽토그램은 사용자 레포 클론 시 add_repo로 반입 가능(확인 완료).

## 260712-9 — FigJam 해석 노트 전량 반영 (본인 저작 확정 후) (branch: claude/manse-calendar-uiux-qlhtyf)
- **전제 변경**: 운영자가 FigJam=본인 강의요약(저작권 본인 소유) 확인 → 규칙만이 아니라 해석 노트까지 반영.
- **산출**: manse/tools/extract-figjam-notes.py(재현 생성기) → manse/db/figjam_notes.json(9카테고리 138노트). 개인정보(팀원 실명 명부) 제외 필터, 명시 인용(출처:)만 source 표기, prefix 조각 제거.
- **배선**: 글래스 앱 글자 사전 카드에 "해석 노트 · 본인 정리" 섹션 — mount에서 fetch, 글자의 십신/한자/한글에 매칭되는 노트 최대 5개 표시. 실패해도 앱 동작(옵셔널 로드).
- **검증**: node --check · JSON valid · Playwright 실클릭(壬 식신→노트 5개 렌더) · 자기검증 15/15 · JS에러 0 · 개인정보 누출 0(스크립트 검사).
- **한계**: 추출이 텍스트 런 기반이라 일부 노트가 길게 병합/거칠음(2차 정제 여지). 스티키 연결관계·이미지 214장 미반영.

## 260712-10 — 명리 해설책(주제별) + soong 배포 검증 + 픽토그램 가시성 확인 (branch: claude/manse-calendar-uiux-qlhtyf)
- **해설책**: manse/tools/build-haeseolchaek.py → docs/reports/260712_명리_해설책.html. figjam_notes.json 138노트를 9장(천간·지지·십신·합충·궁위·운성·신살·격국·용신) 주제별로 엮음. 내용·말투는 본인 노트 그대로, 스크립트는 구성·정렬·'#' 조각 나누기·중복 제거만. 다크/골드 테마, 목차, 폰 친화.
- **soong 배포 검증**: manse/를 사이트 루트로 서빙 시 _redirects(/→/app/)·/app/·/db/·/vendor/ 전부 200 실측. Cloudflare Pages 신규 프로젝트 output=manse로 붙이면 soong.kr 동작 확정. 도메인 연결 클릭은 운영자 몫(계정 권한).
- **픽토그램**: list_repos로 세션 가시 레포 6개 확인(muteno/*, yeulmaru/*, exitmaru/Yeul). 픽토그램 레포 생성 시 add_repo로 반입 가능 실증.

## 260712-11 — 클로드 브랜딩: 코랄 강조색 + 해설책 오프닝 요약 (branch: claude/manse-calendar-uiux-qlhtyf)
- **앱 강조색**: 무채 톤 --accent를 Claude 코랄 #d97757로 교체(버튼·활성탭·acc칩). 다크 글래스 위 코랄 = 클로드 결.
- **해설책**: 제목 "클로드 神 선생님의 요약"(神 코랄) + 야무진 오프닝 박스(읽는 길: 천간지지→십신합충→궁위운성신살→격국용신) 신설. 악센트 골드→코랄 통일. 푸터에 정직 표기(내용=본인 강의정리, 구성·엮음=클로드).
- **대량 유입 대비**: extract-figjam-notes.py + build-haeseolchaek.py 파이프라인은 재실행만으로 확장(노트 늘면 해설책·앱 사전 자동 반영). 카테고리 키워드 사전만 넓히면 됨.
- **검증**: 앱 자기검증 15/15·JS에러 0, 해설책 렌더 실측(제목·오프닝·목차·9장).

## 260712-12 — 클로신 해석 화면 플레이그라운드 드래프트 (branch: claude/manse-calendar-uiux-qlhtyf)
- **요지**: 사주 해석 요약(Opus 4.8 --effort max 산출물)이 글래스 앱에 앉을 자리를 만지는 HTML 시안으로 제안. 백엔드 프록시는 보류, UI/UX만 클라우드로.
- **전/후**: (전) 앱에 해석 화면 없음(원국/운/오늘) → (후) '해석' 탭 신설 시안 — 오프닝 카드(契 글리프·클로신의 해석·契路神⊗契路新) + 관계 섹션(원국뼈대/십신/대운/용신) + 로딩 스트리밍/현행없음 상태. 톤 무채↔레드.
- **규약 준수**: 플레이그라운드 5요소 전부(폰 프레임 실물재현·프리셋4[현행+낭독+집약+★사색]·미세조정 44컨트롤·선택값복사[계승/갱신 자동]·재현한계 각주). 글래스모피즘·기틀색 계승. 색 = 팔레트/토큰 폐쇄 셀렉트(자유 hex 금지), 역할 재지정 = ⚠. 확립 축(글자·라운드) 실존값 사다리 + 이탈 ⚠ 실시간 + [복사] 시 「기틀 갱신 역제안」 모달.
- **결정근거**: 사용자 지시 "글래스·기틀색 유지 + 팔레트로 색" → 기틀 토큰(app.css :root) 사본을 시안에 그대로 계승, 신규색 0. 본문은 샘플 텍스트(실 해석 아님 명시) — 창작 단정 회피.
- **정정(측정)**: openrad 사다리에 현행값 18 누락 → 현행이 ⚠로 오판 → [10,14,16,18,22]로 수정. ★프리셋 leadsize 17(사다리밖) → 18(사다리 위)로 교체해 추천안을 계승(⚠0)으로.
- **검증**: 헤드리스(Playwright 390폭) 실측 — JS에러 0 · 현행/★프리셋 ⚠0(계승) · 색 역할 재지정 시 ⚠1 + 역제안 문구 정상 · 복사 모달 발화. 상태 3종 스크린샷 확인.
- **클라우드**: 브랜치 푸시 → Cloudflare Pages 자동 배포 성공(commit 01c5f5a). 라이브: https://claude-manse-calendar-uiux-q.yeul.pages.dev/docs/reports/260712_클로신_해석_플레이그라운드 (실측 200).
- **산출**: docs/reports/260712_클로신_해석_플레이그라운드.html(시안) + 260712_클로신_해석_축인덱스.md(20열×56행 배선 스펙).
- **다음**: 운영자가 시안 만져 [선택값 복사] → 축인덱스 배선위치 기준으로 app.css/유닛 반영. 이후 백엔드 프록시(functions/api/interpret → Opus 4.8, scout → Sonnet 5, image → Gemini/GPT Image 2 예비) — 키는 Cloudflare 암호화 시크릿, 레포 금지.
## 260712 — 아이콘·픽토그램 정렬 기틀 이식 (문서+CLAUDE 포인터+전수 실측 감사)

- **요지**: 노뮤트에서 확립된 「아이콘 정렬 이식팩」을 §8 절차로 Yeul에 이식. ① `docs/아이콘정렬_기틀.md` 신설(§0~§8 포터블 원문 유지 · §7만 Yeul 실측 베이스라인으로 교체) ② CLAUDE.md `🎯 아이콘·픽토그램 정렬` 절 신설(§3 대원칙 요약 + §6-1 정렬값 변경 시 §5 실측 재검증 규칙 + 정본 포인터) + 구조 docs 목록 등재 ③ 현행 아이콘 전수 헤드리스 실측 감사 ④ HTML 보고서 `docs/reports/260712_아이콘정렬기틀_이식.html`(20열×155행 대장·전수 실측표·교차검증·권고, 스크린샷 base64 임베드).
- **전/후**: 전 = 아이콘 정렬 규칙 없음(어긋나면 개별 수정) → 후 = 정렬 기틀 SSOT 명문화 + 결함0 베이스라인 확보. **코드·화면 무변경**(아이콘 코드 미수정 — 없는 문제 안 만듦).
- **실측 결론(핵심)**: **Yeul은 현재 아이콘 정렬 결함이 없다.** 독립 아이콘 버튼(`.icon-btn` 42·`.socials` 34·`.fab` 52·`.aud-ico` 56)은 전부 `place-items:center` 고정 셀 → eccX ≤0.17px(§3-4 이미 충족). 원본 이식팩 간판 사례(리스트 팝업 헤더X↔행버튼 세로열)는 **Yeul에 존재하지 않음(N/A)**. 방향성 아이콘 편심(§2-3) = `arrowUpRight`↗·`arrow`→·`close`✕ **기하 대칭(편심0)**, `play`▶만 +1.0px 우측(글자 이끄는 단독이라 유지), `draft`📄 −0.56px 경계선. **유일 실질 발견 = `.scene-open`(↗)이 자기 박스 없음**(width/height/display/place-items 전무 → 인라인 크기18+카드 26px 패딩 우연에 의존, 크기없는 SVG 넣으면 터짐). 권고 = `inline-grid+place-items:center+line-height:0`(시각 no-op) — **운영자 버튼 승인 대기**(이 커밋 미포함).
- **바인딩 실측값**: `<정본문서>`=`docs/아이콘정렬_기틀.md` · `<규칙요약>`=CLAUDE.md 🎯 절 · `<실측도구>`=`/opt/pw-browsers/chromium` 헤드리스 직접구동(playwright npm 부재 → 자기완결 하네스 `file://` · 서버·백그라운드 프로세스 0) · `<대상클래스>`=`.icon-btn`(styles.css:103) `.scene-open`(:246) `.socials a`(:281) `.aud-ico`(:227) `.fab`(:333) `.pill-btn`(:319) `.btn`(:194).
- **결정근거**: 운영자 파일 업로드(이식팩) = 이식 지시. 원본 §8 절차 그대로 수행. **아이콘 코드 미수정** = CLAUDE.md 🧪 "효용 주의"·"과설계 경계" 준수(실측상 결함0이라 고칠 대상 없음 · scene-open 하드닝은 UI no-op이나 신규 변경이라 버튼 승인 정문). 🧪 기틀 교차검증 미발동 — 트리거(js/core 구조·knowledge SSOT 계약·설정 스크립트·빌드) 전부 무변경, docs 신설·CLAUDE 문구는 명시적 제외 영역. 대신 프레임워크 §5·§6 정신대로 **독립 Opus 분신 2기(기하 편심 / §4 레이아웃)를 병렬로 적대 감사 → 동일 결론 수렴**으로 보강.
- **검증(실측)**: `node --check` 전 js 통과(무변경) · 헤드리스 크로미움 전수 렌더 — 편심 좌표 RESULT_JSON 실측 + 스크린샷 육안 정렬 확인(JS 에러 0) · 보고서 렌더 실측(표 155행 = 문서 51·아이콘감사 50·CLAUDE이력검증 54, 각 ≥50 · 20열 · JS 에러 0) · 분신 2기 교차검증 수렴.
- **다음 세션 주의**: (1) 새 툴바·리스트·팝업(특히 헤더 닫기 ↔ 행 액션 세로열) 만들 때 `docs/아이콘정렬_기틀.md` §3~§5 돌리기 — 이때부터 간판 사례 발동. (2) 정렬 지배값(컨테이너·헤더·행 padding·셀 폭) 건드리면 §5 픽셀 실측(±1px) 재검증. (3) `.scene-open` 하드닝은 승인 시 30초(styles.css:246 한 줄) + 재실측. (4) 새 docs 파일이지만 앱셸 아니라 `sw.js` PRECACHE 불요.
## 260712 — 사주 캐릭터(십이지×오행) 프롬프트 팩 + 기획안 HTML

- **요지**: 운영자 요청("사주 컨텐츠 관련 캐릭터 귀여운 컨셉 + 프롬프트 오지게")으로 캐릭터 세트 기획. 업로드된 `Untitled (1).jam`(=FigJam) 안의 사주 분석 카드·기존 캐릭터(갑신 원숭이·태강 선생님)를 뜯어 스타일 역설계 → **십이지 12종 × 오행 색 시스템** 확정. 산출 = ① `docs/사주캐릭터_프롬프트팩.md`(마스터 프롬프트 + 12종 완성 프롬프트 + 태강 + 60갑자 확장 + Gemini 앱 통일법, 정본) ② `docs/reports/260712_사주캐릭터_기획안.html`(전/후·오행팔레트·20열 스펙표·84행 결정원장·장단점·리스크·아이데이션, 자기완결) ③ `.gitignore` 신설(API 키 커밋 차단).
- **전/후**: 전 = 캐릭터 컨셉이 FigJam에만(갑신 원숭이 1종 + 태강) → 후 = 12지 전종 색·소품·표정·카피 규격화 + 복붙 프롬프트. 코드(js/css) 무변경 — 문서·리포트·gitignore만.
- **키 보안 결정(중요)**: 운영자가 GitHub Actions **Secrets**(암호화)에 GEMINI/OPENAI 키 등록 = 안전한 방식 확인. 다만 이 세션(비-Actions)에선 미노출(정상) → 세션에 키 넣지 않고 **프롬프트만 전달, 운영자가 Gemini 구독형에서 생성**하는 경로로 확정(키가 세션·레포에 안 들어감). `.gitignore`로 평문 키 실수 커밋 이중 차단.
- **결정근거**: 색 = 각 지지의 오행 본기(寅卯=木, 巳午=火, 丑辰未戌=土, 申酉=金, 子亥=水) → 브랜드(#302cff)와 조화되게 hex 정리. 스타일 = 기존 갑신 원숭이 역설계(진회색 라인·파스텔 플랫·반짝 눈·노란 나비넥타이·정면 앉음). 🧪 기틀 교차검증 미발동 — js/core·knowledge 구조·스크립트 무변경(문서·에셋 계열)이라 대상 아님.
- **검증**: 기획안 HTML을 **헤드리스 크로미움(1194) 라이트/다크 2회 실렌더** — JS 에러 0, 이미지 3장(보드·원숭이·태강 base64) 표시 확인, 84행 원장 생성 확인. **다크모드 프롬프트 박스 흰-온-흰 대비 버그 1건 발견→수정**(pre 배경 `var(--ink)`가 다크에서 밝아짐 → 고정 `#12122e`)후 재렌더로 가독 확인. `node --check js/**/*.js` 무변경 통과.

## 260712 (후속) — 계절 오방색 전환 + 컨택트 시트 프롬프트

- **요지**: 운영자 피드백 2건 반영. ① "기본적인 계절이 있으니 대표색은 따로" → 색 기준을 **오행 본기 → 계절 오방색(五方色)**으로 전환(봄=청/여름=적/가을=백/겨울=흑, 中央=황). 결과 **4계절 × 3마리**로 색이 3:3:3:3 균등 — 흙(土) 4마리(용·양·개·소) 노랑 겹침 문제 해소, 노랑(中央)은 안내자 태강에 배정. ② "한 장에 여러 마리 뽑아 잘라 쓰기(이모지 용도)" → 프롬프트 팩에 **컨택트 시트 프롬프트** 신설(1-A 계절 시트 4장×3마리[추천] · 1-B 전체 12장 3열×4행 · 1-C 60갑자 12×5 확장 · 자르기 가이드).
- **전/후**: 전 = 오행 본기색(土 4마리 동일 골드) + 개별 1마리 프롬프트만 → 후 = 계절 오방색(균등 4색) + 다중 배치 시트 프롬프트. HTML 로스터도 계절 그룹(봄/여름/가을/겨울 소제목)으로 재편.
- **결정근거**: 오방색-방위-계절 정합(청東봄·적南여름·백西가을·흑北겨울·황中앙). 지지 계절 배속(寅卯辰=봄, 巳午未=여름, 申酉戌=가을, 亥子丑=겨울) 기준. 土 4지지는 각 계절 끝자락(辰늦봄·未늦여름·戌늦가을·丑늦겨울)이라 계절색 채택, 본기 오행은 표에 병기. 60갑자(천간색)와 12지(계절색)는 별개 체계로 명시. 🧪 기틀 교차검증 미발동(문서·리포트만, js/core·knowledge·스크립트 무변경).
- **검증**: 계절 버전 헤드리스 크로미움 재렌더 — 봄=에메랄드/민트/제이드, 여름=코랄/버밀리언/살몬, 가을=실버/아이보리/웜실버, 겨울=플럼/코발트/슬레이트로 계절 그룹 시각 확인 · txt() 명도기반 대비 자동선택으로 밝은 카드(가을) 글자 가독 OK · 84행 원장 유지 · JS 에러 0.

## 260712 (후속2) — 12지 캐릭터 실물 생성·컷아웃·배치

- **요지**: 운영자가 프롬프트로 **Gemini 계절 시트 4장(3마리씩)** 생성 후 레포에 업로드(`Gemini_Generated_Image_*.png`, main). 이를 **자동 컷아웃 → `assets/images/characters/` 12종 배치**. 파이프라인 = PIL/scipy 스크립트(`scratchpad/split2.py`): ① 세로 여백 감지로 3분할 → ② 가장자리 flood(바깥 배경 제거) + 외곽 링 흰색 seed flood(카드 안쪽 흰색 제거) → ③ `ndimage.label` 최대 연결덩어리만 유지(얇은 카드테두리·부유 소품·얼룩 제거) → ④ 투명 정사각(640) + 손실 webp q90. 기획안 HTML 로스터도 이모지 → **실물 컷아웃 이미지**로 교체(계절색 틴트 배경).
- **전/후**: 전 = 프롬프트만(플레이스홀더 이모지) → 후 = 투명 webp 12종 실물(`001-인호-tiger`…`012-축우-ox`, 합 601KB/평균 50KB) + README(색·네이밍표). HTML 후(後) 섹션 = 실제 생성물 증빙.
- **결정근거**: 좌→우 = 프롬프트 (1)(2)(3) 순 = 계절순 001~012로 확정 매핑. 무손실 webp(2.4MB)는 이모지 다중표시에 과중 → q90(601KB) 채택(플랫 일러 육안차 무). 최대 연결덩어리 방식 = 카드테두리/얼룩 있는 시트(봄·여름 카드, 겨울 회색배경)까지 균일 컷아웃하려는 선택(캐릭터 내부 흰색은 외곽선으로 격리돼 보존됨). **sw.js PRECACHE 미등재 결정** — 신규 에셋이 아직 어떤 view에도 미배선이라 첫화면 셸에 넣으면 설치 과중 → UI 배선 시 등재+캐시버전 상향(README·각주에 명시). 🧪 기틀 교차검증 미발동(에셋·문서, js/core·knowledge·스크립트 무변경).
- **검증**: 12장 컷아웃 미리보기(체커보드)로 투명·크롭·소품보존 육안 확인(원숭이 은백/닭 아이보리 등 캐릭터 내부 흰색 보존, 봄·여름 흰 카드/겨울 회색배경 제거) · opaque 비율 0.32~0.49 균일(v1의 겨울 0.12~0.24 bbox 팽창 → 최대덩어리 방식으로 해결) · 개별 webp 1장 풀해상 스팟체크(q90 아티팩트 없음) · 기획안 재렌더 실물 로스터·KPI(12 컷아웃) 표시 확인.

## 260712 (후속3) — 캐릭터 파일 ASCII 리네임 (사이트 배선은 방향 변경으로 보류)

- **요지**: 캐릭터 12종 파일명 `NNN-<한글>-<en>` → **ASCII `NNN-<en>.webp`**(`001-tiger`…`012-ox`)로 `git mv` 리네임 — `000-convention`(kebab ASCII) 준수 + URL 인코딩 리스크 제거. README·프롬프트 팩·기획안 HTML의 파일명 표기도 동기화.
- **배선 보류(방향 정정)**: '띠별 퇴사 사주' 섹션 초안(지식 008 + 랜딩 `#saju` + `.saju` CSS + sw.js 등재)까지 만들었으나 운영자 정정으로 **배선 코드는 되돌리고 미커밋**: ⓐ '퇴사'는 주제 아님 → 사주는 **'직장운'에 섞는 한 콘텐츠**로 재프레이밍 예정, ⓑ 현 탈예울/퇴사 사이트는 **잔재**(추후 제거)라 그 위 배선은 무의미, ⓒ 디자인·컨셉은 별도 확정본에 **캐릭터만 병합**. 재활용: 12지 데이터(지지·오행·계절색·`zodiacOfYear`)는 프롬프트 팩/README에 정본으로 남아 즉시 재구성 가능.
- **전/후**: 전 = 한글 섞인 파일명 → 후 = ASCII 12종. `js/`·`css/`·`sw.js` 무변경(되돌림). 이번 커밋 = 리네임 + 문서 동기화만.
- **검증**: `git mv`로 히스토리 보존 · 문서 내 잔여 한글 파일명 grep 0 · 기획안 HTML ASCII 임베드 재빌드 OK · `git restore`로 배선 3파일 HEAD 복구 확인.

## 260712 — GitHub Actions 시크릿 목록 CLAUDE.md에 기록 (이름만 · 다시 안 물어보기)

- **요지**: 운영자가 레포 Settings에 이미 등록해 둔 GitHub Actions 시크릿 10개를 CLAUDE.md 🔐 절 신설로 기록 — 다음 세션이 키를 또 조르지 않게. 목록: `CLAUDE_CODE_OAUTH_TOKEN_{EMS1130G·EMS1130N·MUTENO·MUTENONA·NOMUTEFB}`(5, Claude Code OAuth 토큰=봇별) · `GEMINI_API_KEY` · `OPENAI_API_KEY_NOMUTE` · `VAPID_PUBLIC_KEY`·`VAPID_PRIVATE_KEY`(웹푸시) · `YOUTUBE_API_KEY`. 각 이름에 용도 1줄. + OAuth 토큰 발급법(`claude setup-token`, Pro/Max 필요) + 워크플로 참조법(`${{ secrets.이름 }}`) 명기.
- **전/후**: 전 = 시크릿이 GitHub Settings에만 존재(세션엔 안 보임 → 매번 "그 키 뭐예요/등록해요" 물음) → 후 = CLAUDE.md 상시 로드 목록으로 상주. 새 토큰 생기면 **이름만** 표에 한 줄 추가하는 규약도 명문화.
- **⚠ 보안 철칙**: **이름만 기록, 값(실제 열쇠 문자열)은 어떤 파일에도 절대 안 적음** — 값은 깃 기록에 영구히 박혀 유출되므로 GitHub Settings 안에서만 살아있게 둔다. 이번 커밋도 이름·용도만 담고 값 0건.
- **결정근거**: 운영자 직접 지시("레포에 다 넣어놨으니 CLAUDE.md에 쌓아 다음에 청구 않게"). CLAUDE.md 문구 = 🧪 기틀 교차검증 **제외** 영역(js/core·knowledge·빌드 무변경의 문서 메모)이라 병렬 감사 미발동.
- **검증**: 발급 명령 `claude setup-token`·시크릿 이름 규칙을 커밋 전 웹 실측(docs.claude.com·anthropics/claude-code-action) 후 반영 · 표 10행 = 스크린샷과 이름 대조 1:1 일치 · 마크다운 표 렌더 눈 확인 · 값 미포함 재확인.

## 260712 — 사주 요약 '꼬미 언니' 페르소나 + 알고리즘 드래프트 (컨셉 단계 · 코드 무변경)

- **요지**: 검색 메인 요약을 '사주 봐주는 츤데레 언니(남 사주는 족집게·본인 일은 덜렁)' 목소리로 바꾸는 드래프트 산출(`docs/reports/260712_사주언니_요약_드래프트.md`) — 페르소나·어투 규칙·금칙·few-shot 예시 3쌍 + 요약 아키텍처(①무-LLM 템플릿 사다리 = 현행 무-LLM 존중·결정론적·즉시 출시 가능 ②LLM 승격 레이어 = 시스템 프롬프트 전문/출력 스키마/Gemini→OpenAI 폴백/음성 검증기/캐싱, 실패 시 ①로 자동 복귀) + 한국 웹툰풍 캐릭터 이미지 프롬프트(Gemini·OpenAI 각 1식).
- **운영자 결정(버튼 답)**: ① 지금은 컨셉만(코드 배선 나중) ② 이미지는 운영자가 다른 데서 직접 렌더(§4 프롬프트 사용) ③ 이름 = "수셰프 보조하는 보조 애들 이름"으로 → 주방 브리게이드 보조 직급 후보(★꼬미(commis)·데미·미장) 드래프트, 실존 캐릭터 목록 받으면 교체.
- **전/후**: 전 = 요약이 무-LLM·무-페르소나 → 후(설계) = 항상 언니 목소리 카드(LLM 없어도 템플릿으로 동일 컨셉 유지). 이 레포 코드 무변경(문서만) — Yeul엔 사주/검색/요약 기능 부재를 grep 실측으로 확인, 붙일 앱은 미확정.
- **결정근거**: 운영자 직접 지시("드래프트해서 전달"). 기틀 교차검증 미발동(문서만 · js/core·knowledge·설정 무변경).
- **검증**: 실행환경에 GEMINI/OPENAI 키 부재 = env 실측(그래서 직접 렌더 불가를 문서에 정직 표기) · 레포 내 사주/검색/셰프 관련 기능 부재 = grep 실측 · 결정 3건 = AskUserQuestion 버튼 답 원문 반영.

## 260712 — 사주앱 대화형 UX 데모 (미연시 × 토스) — 탭해보는 실물 HTML

- **요지**: 운영자 요청("대화형으로 판 열리면서, 미연시처럼, 토스처럼 하나씩 전환")에 UX 아이디어 10종을 내고, 말 대신 **실제로 탭하며 도는 데모**(`docs/reports/260712_사주앱_대화형UX_데모.html`) 산출. 핵심 반전 = 사주 플래그로 언니가 **먼저 단정**하는 족집게 모먼트("너, 회사 그만두고 싶지"). 구성: 미연시 씬(스탠딩 표정 5종·타이핑·선택지·호감도) + 토스식 입력(한 화면 한 질문·하단 CTA) + 만세력 카드 8장 플립(로딩의 연출화) + 팔자 8글자 헤더 프로그레스 + 허당 개그(계산 직후) + 결과 공유 카드(오행 바·금0 ⚠). 설정 드로어(프리셋 3 + 13축: 말풍선 박스/버블·츤 레벨·허당 빈도·테마·폐쇄 팔레트 3색 등). 아이디어 목록은 드래프트 문서 §8에 정본 기록.
- **전/후**: 전 = 컨셉이 글로만 존재 → 후 = 폰에서 탭해보는 자기완결 1파일(외부 요청 0). 데모 팔자 = 대사와 정합 설계(寅 역마·子/午 도화·金 0 금약). 코드(사이트) 무변경 — docs/ 산출물만.
- **결정근거**: 운영자 직접 지시. 신규 앱 컨셉이라 확립 기틀 부재 → 플레이그라운드 규약의 '단순 대상 축소' 적용(옵션 13축 + 프리셋 3, 색 = 폐쇄 팔레트, 사유는 데모 내 각주 ⓘ에 명기). 기틀 교차검증 미발동(docs만).
- **검증**: 헤드리스 크로미움 3런 실측 — A(기본 속도: 맞아→돈 분기 완주 + 씬별 스크린샷 5장 + 호감도 16 정합) · B(고속: 아니야→연애 분기 + 시주 미상 ? 카드 경로) · C(프리셋 전환 실적용 + 각주 모달) · **JS 에러 0**. 중간 발견 버그 2건 즉시 수정(결과 오버레이가 헤더 버튼 가림 → z-index · 팔자 슬롯 8개 폭 잘림 → 전용 줄 분리) 후 재검증 통과.

## 260712 — 사주앱 데모 테마 확정: 클로신(契路神) 기틀 계승 재이식

- **요지**: 운영자 지시("이 느낌에 테마만 바꾸면 됨" + 유추→좁히기 + 참고 레퍼런스 제공)로 데모(`docs/reports/260712_사주앱_대화형UX_데모.html`)를 클로신 세계관으로 전면 재이식. 색·글래스·라운드·모션 = **PR #21 브랜치 `manse/app/app.css :root` 토큰 실측 사본 그대로 계승**(베이스 #0b0b0d · 코랄 #d97757 · 글래스 .07/.14/blur20 · radius 22 · ease/480ms · **오행색 --el-* 5종**) — 신규 색 0. 운영자 제공 레퍼런스(FACETELLER류 스샷)에서 구조 차용: 만세력 = 時日月年 전통 배열(年부터 우→좌 플립) + 오행 색상원 + **십신 라벨**(고정 일간 戊土 기준 실규칙: 甲편관·子정재·丙편인·寅편관·戊비견·午정인·壬편재·戌비견 · 지지 = 본기 단순화) · 결과 카드 글리프 = 오행색 원 · 오행 바 = --el-* 색. 유추 스킨 5종안은 운영자가 레퍼런스로 수렴시켜 폐기(단일 기틀 계승), 설정 드로어는 연출·레이아웃 축만 유지.
- **전/후**: 전 = 밤 사주카페(자체 인디고·임의 색) → 후 = 클로신 글래스(기틀 토큰 계승 · 브랜딩 契路神 · 헤더 팔자 슬롯도 오행색). 흐름·대사·분기 무변경.
- **결정근거**: 운영자 레퍼런스 제시 = 테마 확정. 기틀 교차검증 미발동(docs 산출물만 · manse 브랜치는 읽기만).
- **검증**: 헤드리스 3런 재통과(맞아→돈 / 아니야→연애+시주미상 / 프리셋·각주) · JS 에러 0 · 십신 배정은 십신 생극 규칙으로 검산 후 기입.

## 260712 — 데모 증축: 책 표지 오프닝 + 한지·수묵 🎨 토글 + 정보 확인 씬

- **요지**: 운영자 추가 레퍼런스 4세트(토정비결·냉정한 신년운세·명식 앱·타로 — 전부 한지·전통 결) + 지시("표지를 책처럼") 반영. ① **책 표지 오프닝**(丙午年 눈썹·겹테두리·「꼬미의 사주첩」·契 인장 원·인용구·[책 펼치기] — fast 모드 자동 통과) ② **한지·수묵 테마**를 🎨 토글로 병존(수묵 산 3겹·인주 #b0402b·종이 카드 — 클로신 글래스 ⇄ 한지 비교용) ③ **정보 확인 씬**(토정비결 패턴 — 생년월일·시간·호칭 확인 [맞아]/[다시 적을래]). 명식 레퍼런스의 십신+오행색 원 구조 = 기제작분과 일치(방향 검증).
- **버그 수정**: 한지에서 제목·대사 글자 옅음 — body 상속이라 `.app` 토큰 재정의 미반영 → `.app{color:var(--ink)}` 1줄.
- **검증**: 헤드리스 3런 재통과(표지→확인 씬 경유·한지 토글 assert) · JS 에러 0 · 한지 대사 계산색 rgb(58,49,40) 실측.

## 260712 — 타이포 기준 확정: Pretendard + 자간 -10 (사주앱 컨셉 전역)

- **요지**: 운영자 확정("글자체 = Pretendard · 자간 -10 항상 기준"). 데모 본문 전역 `letter-spacing:-0.01em`(툴 트래킹 -10 = -10/1000em) 적용 + 각주 명기. 눈썹 라벨(광자간)·한자 인장(세리프)만 의도적 예외. 데모는 자기완결이라 폰트 임베드 없음 — Pretendard 스택 1순위, 실앱 배선 시 앱 로드를 탐.
- **검증**: 헤드리스 계산값 실측 — .dtext letter-spacing = -0.16px(16px×-0.01em).
