# 노뮤트 전사본(도화도레 사주) → 해석본(KB) 통합 전략 v1

> 작성 2026-07-18 · 지시 = 원장 Q.09 · 절차 = `/feed` v2(블라인드 시험 루프, 제1법칙) · 평의회 = 페이블 11인 소환(T0 17:04, 완주 3 · 10분 컷 강제 정지 8 — 운영자 실시간 지시로 컷 집행, 미완주 렌즈는 메인 세션 실측으로 대체)

## 1. 입력 실측 (zip: 노뮤트_전사본_20260718)

| 항목 | 값 |
|---|---|
| 도화도레 사주 md | 261편(+categories.tsv·dashboard.html·_progress.log) — vid 보유 260(`.asr.md` 이중 확장 7 포함) + index.md(목차) |
| 접근 / 시스템 | 공개 188 · 멤버십 71 / 사주 250 · 관상 3 · 꿈해몽 3 · 자미두수 3 |
| 자막 품질 | **Whisper(large-v3·미검수) 84 · YouTube 자동생성 175 · 스텁 2** — 운영자가 Whisper 재전사 진행 중(완료 후 재전달 예정, 압축 명세 명기) |
| 기보유 겹침 | fulltext(309) 대비 **겹침 61 · 신규 192**(공개 121·멤버십 70) — 겹침 61 중 zip=Whisper는 24편뿐 |
| 신규∩Whisper | **54편** = C07이론 11 · C08신살 10 · C04재물 9 · C10인물 7 · C02연간 5 · 기타 12 |
| 오랑 판타지 txt | 6편 — 음모론·밀리터리(딥스테이트·발리스타·일루미나티…) = **사주 무관** |

## 2. 결정 — 3분할 게이트 (결정 로그 등재)

- **지금 주입 트랙**: Whisper **신규 54편**만 파도 대상. Whisper도 미검수라 **오전사 정규화 매핑표**(도화살→도와쌀·재고귀인→제고기인·사주팔자→사춘 팔자 실측) 전단 배치 후 주입 — 축자 인용은 정규화 표기+원문 병기(A5).
- **대기 트랙**: 자동자막 175편 — 지금 넣으면 '십성→씹성' 급 오전사가 축자 인용으로 KB에 박제(A2 실측). **재전사 완료본 재전달 시** 파도 편입.
- **동결·교체 트랙**: 기보유 61편은 **동결**(기존 증류 축자 인용 223건·시험은행 리플레이가 fulltext 원문에 묶여 있음). 교체는 zip=Whisper 24편 한정, 별도 교체 파도에서 `validate_distilled`+`replay_exams`(verify 게이트4) 통과 조건부(A8: 게이트4가 파손 자동 검출).
- **제외·등급 트랙**: 오랑 판타지 6편 = KB 제외(운영자 로컬 보관만, 전사 파이프라인 시험분 추정) · 스텁 2편 제외 · 관상3/꿈3/자미두수3 = folk_etc 등급(관점차이 병기용, yt_filter 자동) · 월간운세는 §5 회색지대.

## 3. /feed v2 로컬 적용 변형 (스킬 정본 준수)

1. **0단계 수집 생략** — zip 전사본 = 답안지. 스킬 조항 "비유튜브 입력도 제1법칙 대상: stage 직접 등록" 적용(vid는 실제 유튜브 id 유지).
2. **블라인드 격리(불변)**: 답안지(zip 원문·**categories.tsv 원본**)는 레포 커밋 금지 — tsv `file` 칸 제목이 답 자체("돈이 크게 들어오는 10년은 정해져 있습니다…")라 블라인드 학생이 grep으로 주움(A8). 커밋 가능 = **제목 제거 파도계획 TSV**(id·access·system·primary·asr·duration만). 원본 보존 축 = 운영자 로컬(세션 휘발 시 재전달 요청 — 파도 GO마다 zip 재업로드 가능성 고지).
3. **채널명 함정(A2 실측)**: `--channel`은 기존 등재명 **"도화도르- 사주팔자 쉽게 풀어주는 남자"** 를 정확히 줘야 core 흡수 — md 헤더의 '도화도레 사주'를 주면 folk_etc 강등+제목 탈락.
4. **포맷 변환(A2)**: Whisper md = 캡션 1줄씩 → 문단 재조립 필수(scan 체인은 15·20자 이상 행만 색인 — 그대로 넣으면 유실). 헤더(제목·⚠️경고) 박리, `업로드`→`--date`, `길이`→duration(초), 자막태그→manifest flags 보존.
5. 주입 정본 경로 = `ingest_transcript.py`(멱등) → 색인 3종(`yt_filter`→`scan_interactions`→`scan_impressions`) 파도 끝 1회 → `npm run verify`(6게이트). `check_new_video --save` 금지(기존 결정 로그).

## 4. 파이프라인 신설 (파도 1 첫 작업 — A8 설계)

| 신규 파일 | 역할 |
|---|---|
| `dosa-app/kb-tools/stage_from_md.py` | md 디렉터리→`stage/<vid>.txt`+`manifest.json`(fetch_transcripts.py collect() 스키마 자구 일치 필수 — 불일치 시 ingest가 조용히 메타 누락) |
| `dosa-app/kb-tools/make_wave_plan.py` | categories.tsv→제목·file칸 제거 파도계획 생성 |
| `dosa-app/kb/plans/dohwadore_waves.tsv` | 생성물(커밋 대상 — 재생성으로만 갱신, 손편집 금지) |

## 5. 파도 로드맵 (승인 단위 = GO 1회 · 파도 1개, 기존 합의 계승)

| 파도 | 대상 | 편수 | 게이트 |
|---|---|---|---|
| 0 (완료) | 평의회·통합 전략·정본 3종 머지 | — | 본 문서 main 머지 |
| **1 (GO 대기)** | 신규 Whisper **C07 이론 11 + C04 재물 9** | **20** | 변환기 2종 구현→시트 PASS(채점 '동등'+비자기 축자)→verify→머지→파도 보고(판정표+회색지대 3~5문+표본 시트 2~3) |
| 2 | 신규 Whisper C08 신살 10 + C10 인물 7 + C05 연애 3 | 20 | 동일 |
| 3 | 신규 Whisper 잔여(C02 연간 5 포함 — 시효성 판정 반영) | ~14 | 동일 |
| 4+ | 자동자막 175 재전사 도착분(재전달 시 재계획) | — | 동일 |
| 교체(별도) | 기보유 61 중 zip=Whisper 24 | 24 | validate+replay 통과 조건부 |

- 파도 1 우선 검증 대상(A5 probe 실측 — KB 0건 신규 체계): 재물대운 조건 체계 · 만세력 운 읽기 순서/운 교체 징조(p1pEx1tkdQE) · 대운 5종 분류(xOcikQJ6j_U) · 십성별 도화 5분류(6mbUhGY5MQ0) · 재고귀인 독립 유닛(WFKgoJef0uM). 신살류는 신규 유닛 남발 금지 — 기존 sinsal/* 키에 관점차이 병기(A5).
- sdGUxQ-eiI4(재물대운 7조건 — 최고 가치이나 자동자막) = **재전사 1순위 지정**으로 운영자에게 요청.

## 6. 회색지대 (운영자 판단 대기 — 파도 1 보고에 재상정)

1. **멤버십 71편** — KB 주입은 운영자 전달분이라 진행 기본값. 단 앱(공개 서비스)에서 유료 콘텐츠 축자 인용 노출 수위는 결정 필요.
2. **월간·연간 운세 79편**(전체 30%) — 시효 지난 예보 vs 보편 통변 원리·L4 화법 자산. 등급(정식 주입/화법 자산/보관) 결정 필요.
3. **오전사 정규화 매핑표** — 등재 위치·절차(단순 파도 산출물 vs kb-tools 상수) 결정 필요.

## 7. 평의회 기록 (페이블 11인 · T0 17:04:02 · 운영자 컷 집행)

- **완주 3**: A2 KB매핑(stage 계약·채널명 함정·포맷 변환 확정) · A5 이론가치(체계·프로토콜 지식 = KB 0건 신규 실증, 우선순위 C07→C04→C08) · A8 파이프라인(신규 파일 2종·tsv 커밋 불가 판정·verify 게이트 성립).
- **강제 정지 8**(A1 품질·A3 파도·A4 교체·A6 운세·A7 경계·A9 적대·A10 로드맵·A11 스킬확인) — 10분 컷. 해당 렌즈는 메인 세션 실측(§1 수치·§2 판정)으로 대체. A11의 독립 확인 미완 — 역검증 스킬 = `/feed` v2(`.claude/skills/feed/SKILL.md`)로 메인 세션이 정본 직접 로드로 식별(스킬 발현 완료).
